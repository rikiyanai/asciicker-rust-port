---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine-port/Cargo.toml
  - engine-port/src/main.rs
  - engine-port/src/lib.rs
  - engine-port/src/core/mod.rs
  - engine-port/src/core/coords.rs
  - engine-port/src/asset_loader/mod.rs
  - engine-port/src/world/mod.rs
  - engine-port/src/terrain/mod.rs
  - engine-port/src/render/mod.rs
  - engine-port/src/output/mod.rs
  - engine-port/src/physics/mod.rs
  - engine-port/src/character/mod.rs
  - engine-port/src/game/mod.rs
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "`cargo build` succeeds in engine-port/ with Bevy 0.18.0, default-features=false"
    - "`cargo run` opens a Bevy window without panic"
    - "All 8 plugins register without error"
    - "Z-up convention enforced via const UP: Vec3 = Vec3::Z"
  artifacts:
    - path: "engine-port/Cargo.toml"
      provides: "Project manifest with Bevy 0.18.0"
      contains: "default-features = false"
    - path: "engine-port/src/main.rs"
      provides: "Entry point registering all 8 plugins"
      contains: "add_plugins"
    - path: "engine-port/src/core/coords.rs"
      provides: "Z-up coordinate convention"
      contains: "const UP: Vec3 = Vec3::Z"
  key_links:
    - from: "engine-port/src/main.rs"
      to: "all 8 plugin modules"
      via: "add_plugins tuple"
      pattern: "add_plugins.*Plugin"
---

<objective>
Create engine-port/ project with Bevy 0.18.0, 8 stub plugins, and Z-up coordinate module.

Purpose: Establish the compiling foundation all subsequent phases build on.
Output: A clean `cargo build` + `cargo run` project with correct architecture.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project with Cargo.toml, main.rs, lib.rs</name>
  <files>engine-port/Cargo.toml, engine-port/src/main.rs, engine-port/src/lib.rs</files>
  <action>
    Create `engine-port/` and initialize. Cargo.toml: name=asciicker-engine, edition=2024, bevy 0.18.0 with default-features=false and features=[2d_api, bevy_render, bevy_core_pipeline, bevy_shader, default_app, default_platform]. Add thiserror 2.0 and anyhow 1.0. No cdylib crate type. lib.rs: declare 9 pub modules (core, render, output, asset_loader, world, terrain, physics, character, game). main.rs: import all 8 plugins, build App with DefaultPlugins and all 8 custom plugins via tuple.
  </action>
  <verify>`cd engine-port && cargo check` compiles. Grep Cargo.toml for default-features = false.</verify>
  <done>Cargo.toml has Bevy 0.18.0 with correct minimal features. lib.rs declares all modules. main.rs registers all plugins.</done>
</task>

<task type="auto">
  <name>Task 2: Create Z-up coordinate convention module</name>
  <files>engine-port/src/core/mod.rs, engine-port/src/core/coords.rs</files>
  <action>
    src/core/mod.rs: pub mod coords with re-exports. src/core/coords.rs: const UP=Vec3::Z, FORWARD=Vec3::Y, RIGHT=Vec3::X.

    GameVec3 implementation:
    - `pub struct GameVec3(pub Vec3)` -- newtype wrapping Vec3
    - Implements `Deref<Target=Vec3>` so Vec3 methods are accessible
    - `game_to_bevy(v: GameVec3) -> Vec3` -- delegates to `GameVec3::to_bevy()`
    - `bevy_to_game(v: Vec3) -> GameVec3` -- delegates to `GameVec3::from_bevy()`
    - Conversion methods live on the GameVec3 type, not as standalone functions

    Unit tests: UP==Vec3::Z, roundtrip identity, game_to_bevy(UP)==Vec3::Y.
  </action>
  <verify>`cd engine-port && cargo test core::coords` -- all tests pass.</verify>
  <done>const UP: Vec3 = Vec3::Z exists. Conversion functions tested with roundtrip.</done>
</task>

<task type="auto">
  <name>Task 3: Create 8 stub plugins implementing Plugin trait</name>
  <files>engine-port/src/asset_loader/mod.rs, engine-port/src/world/mod.rs, engine-port/src/terrain/mod.rs, engine-port/src/render/mod.rs, engine-port/src/output/mod.rs, engine-port/src/physics/mod.rs, engine-port/src/character/mod.rs, engine-port/src/game/mod.rs</files>
  <action>
    Each module: pub struct XxxPlugin; impl Plugin with build() logging info!("XxxPlugin registered"). 8 plugins: AssetLoaderPlugin, WorldPlugin, TerrainPlugin, CpuRasterizerPlugin, AsciiOutputPlugin, PhysicsPlugin, CharacterPlugin, GamePlugin.
  </action>
  <verify>`cd engine-port && cargo build` succeeds. All 8 plugin structs compile.</verify>
  <done>All 8 plugins implement Plugin trait and are registered in main.rs.</done>
</task>

</tasks>

<verification>
1. `cd engine-port && cargo build` -- zero errors
2. `cd engine-port && cargo test` -- coordinate tests pass
3. `cd engine-port && cargo fmt -- --check` -- formatted
4. Verify Cargo.toml has default-features = false
5. Verify const UP: Vec3 = Vec3::Z in coords.rs
6. Verify all 8 plugins registered in main.rs
</verification>

## Contract Check

| Requirement | Plan Coverage | Verification |
|-------------|---------------|--------------|
| FOUND-01: Bevy 0.18 with default-features=false | Task 1: Cargo.toml with exact feature set | `cargo build` succeeds |
| FOUND-02: Plugin-per-subsystem architecture | Task 3: 8 stub plugins with Plugin trait | All plugins compile and register |
| FOUND-03: Z-up coordinate convention enforced | Task 2: const UP, newtype struct GameVec3, conversion fns | `cargo test core::coords` passes |

## Consistency Diff

- CONTEXT.md says "start fresh in engine-port/" -- Plan creates engine-port/ from scratch (consistent)
- CONTEXT.md says "single crate, not workspace" -- Plan creates one Cargo.toml (consistent)
- CONTEXT.md says "8 plugins" (lists 8) -- Plan creates exactly 8 (consistent)
- CONTEXT.md says "Terrain is SEPARATE from World" -- Plan has separate TerrainPlugin and WorldPlugin (consistent)
- CONTEXT.md says "use glam types directly" -- Plan uses Vec3 via GameVec3 newtype with Deref<Target=Vec3>, satisfying the spirit of direct use (consistent)
- RESEARCH.md recommends features [2d_api, bevy_render, bevy_core_pipeline, bevy_shader, default_app, default_platform] -- Plan uses exact set (consistent)
- ROADMAP success criteria #1: cargo build with specific features -- Task 1 implements this (consistent)
- ROADMAP success criteria #2: window opens, plugins register -- Task 3 implements this (consistent)
- ROADMAP success criteria #3: Z-up enforced with const and newtype struct -- Task 2 implements this (consistent)

## Blocking Checklist

- [x] All FOUND-01, FOUND-02, FOUND-03 requirements mapped to tasks
- [x] No deferred ideas included (none exist)
- [x] No Phase 2+ dependencies included
- [x] Plugin count matches CONTEXT.md (8 plugins)
- [x] Feature set matches RESEARCH.md recommendation
- [x] No file ownership conflicts with other plans

<success_criteria>
- `cargo build` succeeds with Bevy 0.18.0, default-features=false
- `cargo test` passes coordinate convention tests
- All 8 plugins compile and register
- Z-up convention enforced with const + GameVec3 newtype struct + conversion functions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
