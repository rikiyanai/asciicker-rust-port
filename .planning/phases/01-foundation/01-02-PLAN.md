---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - engine-port/src/render/mod.rs
  - engine-port/src/output/mod.rs
  - engine-port/src/render/sample_buffer.rs
  - engine-port/src/output/ascii_cell_grid.rs
  - engine-port/src/render/config.rs
  - engine-port/tests/resource_flow.rs
autonomous: true
requirements: [FOUND-04]

must_haves:
  truths:
    - "SampleBuffer exists as a Bevy Resource with 484x274 samples (formula: 2*ascii_width+4 x 2*ascii_height+4, supersampled from 240x135)"
    # **P1-103 FIX (HIGH):** Original text said "480x270". Actual dimension formula is `2*ascii+4`, giving 484x274. Updated directly in the truth statement.
    # **P1-005 FIX:** Actual SampleBuffer dimensions are 484x274 (formula: `2*ascii+4`), not 480x270.
    - "AsciiCellGrid exists as a Bevy Resource with 240x135 cells and separate char_index, fg, bg arrays"
    - "A test system can write to SampleBuffer and read from AsciiCellGrid in the same frame"
    - "RenderConfig resource controls dimensions, SampleBuffer derives from it"
  artifacts:
    - path: "engine-port/src/render/sample_buffer.rs"
      provides: "SampleBuffer resource with flat Vec<Sample>"
      contains: "derive(Resource)"
    - path: "engine-port/src/output/ascii_cell_grid.rs"
      provides: "AsciiCellGrid with separate arrays for GPU"
      contains: "derive(Resource)"
    - path: "engine-port/src/render/config.rs"
      provides: "RenderConfig with default 240x135"
      contains: "ascii_width: 240"
    - path: "engine-port/tests/resource_flow.rs"
      provides: "Integration test for resource coexistence"
      contains: "fn sample_buffer_and_grid_coexist"
  key_links:
    - from: "engine-port/src/render/mod.rs"
      to: "RenderConfig, SampleBuffer"
      via: "init_resource in CpuRasterizerPlugin::build"
      pattern: "init_resource::<(RenderConfig|SampleBuffer)>"
    - from: "engine-port/src/output/mod.rs"
      to: "AsciiCellGrid"
      via: "init_resource in AsciiOutputPlugin::build"
      pattern: "init_resource::<AsciiCellGrid>"
---

<objective>
Implement SampleBuffer and AsciiCellGrid as Bevy Resources with TDD, proving they coexist and can be accessed in the same frame.

Purpose: Establish the data contract between CPU rasterizer (writes SampleBuffer) and GPU output (reads AsciiCellGrid). This is the core data pipeline all rendering flows through.
Output: Two resources with tests, registered by their owning plugins.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<feature>
  <name>ECS Resources: SampleBuffer, AsciiCellGrid, RenderConfig</name>
  <files>
    engine-port/src/render/config.rs
    engine-port/src/render/sample_buffer.rs
    engine-port/src/output/ascii_cell_grid.rs
    engine-port/tests/resource_flow.rs
  </files>
  <behavior>
    **P1-105 FIX (HIGH):** RenderConfig has only TWO fields: `ascii_width: u32` (default 240) and `ascii_height: u32` (default 135). There is NO `supersample_factor` field. The factor of 2 and the +4 border are encoded in the formula `2*ascii+4` inside `SampleBuffer::new()`, not as a config field.

    <!-- **P1-203 FIX (MEDIUM):** The line below is WRONG original plan text left as live (non-commented) text. A reader copy-pasting it would implement a field that does not exist. The correct spec is stated in P1-105 FIX immediately above. DO NOT IMPLEMENT the supersample_factor field. -->
    <!-- WRONG (DO NOT IMPLEMENT): RenderConfig holds ascii_width (240), ascii_height (135), supersample_factor (2). -->
    <!-- WRONG: "supersample_factor (2)" — this field does not exist in the implementation. See P1-105 FIX above. -->

    **P1-104 FIX (HIGH):** The Sample struct fields listed below are WRONG. Actual fields are: `visual: u16, diffuse: u8, spare: u8, height: f32`. The names "depth", "color_rgb555", "glyph", and "material_id" are NOT field names in the implemented Sample struct.

    **P1-104+P1-105 CORRECT spec:**
    - RenderConfig: `ascii_width: u32` (default 240), `ascii_height: u32` (default 135). No supersample_factor field.
    - SampleBuffer: width=484, height=274 (formula: `2*ascii_width+4` x `2*ascii_height+4`), flat Vec of Sample structs.
    - Sample struct: `{ visual: u16, diffuse: u8, spare: u8, height: f32 }`.
    - Methods: sample_at(x, y) -> &Sample, sample_at_mut(x, y) -> &mut Sample, clear() resets all samples.

    <!-- SUPERSEDED: Original spec "SampleBuffer: width=480, height=270, Sample has depth/color_rgb555/glyph/material_id" removed. See P1-104+P1-105 CORRECT spec above for actual implementation. -->

    AsciiCellGrid: width=240, height=135 (from RenderConfig). Separate arrays for GPU 4-texture approach: char_indices (Vec&lt;u16&gt;), fg_colors (Vec&lt;[u8; 4]&gt; RGBA), bg_colors (Vec&lt;[u8; 4]&gt; RGBA). Methods: cell_at(x, y) returns (char_index, fg, bg), set_cell(x, y, char_index, fg, bg).

    Integration test: create App with MinimalPlugins + CpuRasterizerPlugin + AsciiOutputPlugin, call update(), verify both resources exist with correct dimensions, write a sample, read a cell.
  </behavior>
  <implementation>
    RED: Write tests first for RenderConfig defaults, SampleBuffer dimensions and indexing, AsciiCellGrid dimensions and cell access, integration resource flow.
    GREEN: Implement structs with derive(Resource), FromWorld for SampleBuffer (reads RenderConfig), register in plugin build() methods.
    REFACTOR: Clean up, ensure no pub fields that should be private, add doc comments.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for resources</name>
  <files>engine-port/src/render/config.rs, engine-port/src/render/sample_buffer.rs, engine-port/src/output/ascii_cell_grid.rs, engine-port/tests/resource_flow.rs</files>
  <action>
    Create stub files with struct definitions but no implementation. Write unit tests in each module: RenderConfig default values (240, 135) **P1-105 FIX (HIGH): RenderConfig has only two fields — ascii_width and ascii_height. The value "2" (supersample_factor) is NOT a field; do not test for it.**, SampleBuffer dimensions (484x274) **P1-106 FIX (MEDIUM): original text said "(480x270)" — correct value is 484x274 (formula: 2*ascii+4). Tests were implemented asserting 484 and 274.**, SampleBuffer sample_at indexing, AsciiCellGrid dimensions (240x135), AsciiCellGrid separate array lengths. Write integration test in tests/resource_flow.rs: build App with plugins, verify both resources exist with correct dimensions after update(). All tests must FAIL (RED phase).
  </action>
  <verify>`cd engine-port && cargo test 2>&1 | grep "FAILED"` -- tests exist and fail.</verify>
  <done>Test files exist with assertions for all resource behaviors. Tests compile but fail.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement resources to pass tests</name>
  <files>engine-port/src/render/config.rs, engine-port/src/render/sample_buffer.rs, engine-port/src/render/mod.rs, engine-port/src/output/ascii_cell_grid.rs, engine-port/src/output/mod.rs</files>
  <action>
    **P1-104 FIX (HIGH):** The Sample struct field names below are WRONG. Actual fields: `visual: u16, diffuse: u8, spare: u8, height: f32`. Do NOT implement with "depth", "color_rgb555", "glyph", "material_id" as field names.
    **P1-105 FIX (HIGH):** RenderConfig Default is (240, 135) — two fields only. The "2" (supersample_factor) is NOT a field. Do NOT add supersample_factor to RenderConfig.

    Implement RenderConfig with Default (240, 135) [no supersample_factor field]. Implement Sample struct (`visual: u16, diffuse: u8, spare: u8, height: f32`) [NOT: depth f32, color_rgb555 u16, glyph u16, material_id u8] with Default. Implement SampleBuffer with FromWorld reading RenderConfig, flat Vec, sample_at/sample_at_mut/clear methods. Implement AsciiCellGrid with FromWorld reading RenderConfig, separate char_indices/fg_colors/bg_colors Vecs, cell_at/set_cell methods. Update CpuRasterizerPlugin::build to init_resource RenderConfig then SampleBuffer. Update AsciiOutputPlugin::build to init_resource AsciiCellGrid. Ensure RenderConfig is registered before SampleBuffer (ordering matters for FromWorld).
  </action>
  <verify>`cd engine-port && cargo test` -- all tests pass (GREEN).</verify>
  <done>All unit tests and integration test pass. Resources register correctly via plugins.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR - Clean up and add documentation</name>
  <files>engine-port/src/render/config.rs, engine-port/src/render/sample_buffer.rs, engine-port/src/output/ascii_cell_grid.rs</files>
  <action>
    Add doc comments to all public types and methods. Ensure Sample fields use appropriate visibility. Run cargo clippy and cargo fmt. Verify all tests still pass after refactor.
  </action>
  <verify>`cd engine-port && cargo test && cargo clippy -- -D warnings && cargo fmt -- --check`</verify>
  <done>Code is documented, clean clippy, formatted, all tests green.</done>
</task>

</tasks>

<verification>
1. `cd engine-port && cargo test` -- all unit + integration tests pass
2. `cd engine-port && cargo test --test resource_flow` -- integration test passes
3. SampleBuffer dimensions are 484x274 (formula: 2*ascii+4; verified by test) **P1-107 FIX (MEDIUM):** Original text said "480x270". Corrected to 484x274. <!-- **P1-005 FIX:** Actual dimensions are 484x274 (formula: `2*ascii+4`). Tests updated to assert 484x274. -->
4. AsciiCellGrid dimensions are 240x135 (verified by test)
5. Both resources accessible in same Bevy App frame (integration test)
6. `cargo clippy -- -D warnings` clean
7. `cargo fmt -- --check` clean
</verification>

## Contract Check

| Requirement | Plan Coverage | Verification |
|-------------|---------------|--------------|
| FOUND-04: SampleBuffer and AsciiCellGrid as Resources | Task 2: both implemented with derive(Resource) | Integration test proves coexistence |
| FOUND-04: test system can write/read in same frame | Task 2: integration test does write + read | `cargo test --test resource_flow` |
| CONTEXT: SampleBuffer dimensions from RenderConfig | Task 2: FromWorld reads RenderConfig | Unit test checks 484x274 **P1-108 FIX (MEDIUM): original said "480x270"** |
| CONTEXT: AsciiCellGrid GPU-ready (separate arrays) | Task 2: separate char_indices, fg, bg Vecs | Unit test checks array lengths |

## Consistency Diff

- CONTEXT.md says "SampleBuffer dimensions configurable via RenderConfig" -- Plan creates RenderConfig resource (consistent)
- CONTEXT.md says "default 240x135 ASCII, SampleBuffer 484x274 at 2x+4" -- Tests verify these exact dimensions (consistent)
- CONTEXT.md says "SampleBuffer: flat Vec with index methods" -- Plan uses Vec<Sample> with sample_at (consistent)
- CONTEXT.md says "AsciiCellGrid GPU-ready: separate char_index, fg, bg arrays" -- Plan uses separate Vecs (consistent)
- CONTEXT.md says "both unit tests AND integration tests" -- Plan has both (consistent)
- ROADMAP success criteria #4: both resources exist, test system reads/writes in same frame -- Integration test covers this (consistent)
- Plan depends_on [01] -- correct, needs project structure from Plan 01

## Blocking Checklist

- [x] FOUND-04 requirement mapped to tasks
- [x] RenderConfig registered before SampleBuffer (FromWorld dependency)
- [x] CpuRasterizerPlugin owns SampleBuffer, AsciiOutputPlugin owns AsciiCellGrid
- [x] No file conflicts with Plan 01 (different files)
- [x] TDD RED-GREEN-REFACTOR cycle followed

<success_criteria>
- SampleBuffer and AsciiCellGrid exist as Bevy Resources
- Dimensions match spec: SampleBuffer 484x274 (formula: 2*ascii+4), AsciiCellGrid 240x135
  **P1-102 FIX (HIGH):** Original said "SampleBuffer 480x270". Corrected to 484x274. Multiple FIX comments in the body were correct; this acceptance gate was the last location with the wrong value.
- Integration test proves both accessible in same frame
- All tests pass, clean clippy, formatted code
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
