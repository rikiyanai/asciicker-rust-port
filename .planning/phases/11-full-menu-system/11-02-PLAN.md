---
phase: 11-full-menu-system
plan: 02
type: execute
wave: 2
depends_on: ["11-01", "07-04"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/menu_render.rs
autonomous: true
requirements:
  - FMENU-02

must_haves:
  truths:
    - "Background images are rendered with aspect-correct scaling."
    - "Dither fade-in/out transitions between menu screens are functional."
    - "Menu text is rendered hierarchically using Font1 skins (Gold for selected)."
    - "Smooth linear interpolation (±1 per frame) is used for scrolling."
  artifacts:
    - path: "engine-port/src/game/menu_render.rs"
      provides: "Menu background and text rendering systems"
      min_lines: 200
---

**Requirements**: FMENU-02

<objective>
Implement high-fidelity menu rendering, including dithered background transitions and hierarchical text overlays.

Purpose: Matches the unique visual aesthetic of the original Asciicker menus. Uses custom scaling and dithering algorithms to render high-resolution menu graphics into the ASCII grid with smooth transition animations.

Output: `src/game/menu_render.rs` (Menu rendering systems).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/11-full-menu-system/11-RESEARCH.md

Key references:
- C++ mainmenu.cpp: `ScaleImg()` and dithering logic
- C++ mainmenu.cpp: `mainmenu_dither` animation counter
- Phase 7: Font1 system with Grey/Gold/Pink skins
</context>

<tasks>
  <task id="11.2.1" title="Implement Aspect-Correct Scaling" est="M">
    Implement `render_menu_background` system.
    Requirement: FMENU-02.
    Scales the `menu_back.png` image to fill the `AsciiCellGrid`.
    Maintains aspect ratio with cropping (Center-Fill).
    Port C++ `ScaleImg` algorithm if necessary for exact fidelity.
  </task>
  <task id="11.2.2" title="Implement Dither Transitions" est="M">
    Implement `update_menu_dither_system`.
    Requirement: FMENU-02.
    Decrements a `mainmenu_dither` timer when entering/exiting.
    Uses timer value to apply half-tone dithering density.
    Ensures smooth fade-in from transparent to opaque.
  </task>
  <task id="11.2.3" title="Implement Hierarchical Text Overlay" est="L">
    Implement `render_menu_text_system`.
    Requirement: FMENU-02.
    Iterates the `MenuStack` and paints strings using `Font1`.
    Uses GOLD skin for the selected item, GREY for unselected, PINK for sub-menu titles.
    Applies `smooth_scroll` offset to the item list.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 11.2.A:** Background Scaling.
- *Action:* Resize the window while in the main menu.
- *Requirement:* The background image must stay centered and fill the screen without stretching.
- *Submission:* Provide screenshots of 16:9 and 4:3 window aspect ratios.

### Layer 2: Integration (ECS Contract)
Run the dither transition test:
```bash
cargo test --test menu_dither_transition -- --nocapture
```
- *Input:* 10 ticks elapsed.
- *Expected Output:* `dither_value: 10` (starting from 20).
- *Agent Action:* Log the exact dither alpha values.

### Layer 1: Logic (Data-Only)
Run the scaling math test:
```bash
cargo test menu_render::tests::test_aspect_fill -- --nocapture
```
- *Input:* Img 100x100, Dst 200x100.
- *Expected Output:* `crop_rect: [25, 0, 50, 100]`.
</verification>

<success_criteria>
- Background images are rendered with aspect-correct scaling.
- Dither fade-in/out transitions between menu screens are functional.
- Menu text is rendered hierarchically using Font1 skins (Gold for selected).
- Smooth linear interpolation (±1 per frame) is used for scrolling.
</success_criteria>

## Contract Check

- FMENU-02: Task 11.2.1, 11.2.2, and 11.2.3 implement background scaling, dithering, and text overlays.

## Consistency Diff

- Scaling and dithering match the original engine's `ScaleImg` algorithm to ensure 1:1 visual parity.

## Blocking Checklist

- [ ] FMENU-02
- [ ] Dither transition timing verified by test
- [ ] Aspect-correct cropping verified

<risks>
  <risk id="R79" severity="MEDIUM" mitigation="Optimize inner loop">
    Per-cell scaling and dithering on the CPU every frame can be slow; ensure the rendering system is well-optimized.
  </risk>
</risks>

<notes>
Linear ±1 interpolation for scroll ensures predictability at variable framerates.
</notes>

<output>
After completion, create `.planning/phases/11-full-menu-system/11-02-SUMMARY.md`
</output>
