---
phase: 11-full-menu-system
plan: 04
type: execute
wave: 4
depends_on: ["11-02"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/gamepad_ui.rs
autonomous: true
requirements:
  - FMENU-04
# Mark as OPTIONAL
---

**Requirements**: FMENU-04

<objective>
Implement the visual gamepad configuration UI, allowing custom button and axis mapping.

Purpose: (OPTIONAL) Provides a user-friendly way to configure gamepads using a visual representation of the controller. Matches the deep customization functionality found in `gamepad.cpp`.

Output: `src/game/gamepad_ui.rs` (Mapping UI).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/11-full-menu-system/11-RESEARCH.md

Key references:
- C++ gamepad.cpp: Visual drag-drop mapping logic
- C++ gamepad.cpp: Inverse lookup tables (`button_mapping`, `axis_mapping`)
</context>

<tasks>
  <task id="11.4.1" title="Implement Visual Mapping UI" est="L">
    Implement `render_gamepad_config_system`.
    Requirement: FMENU-04.
    Draws the `gamepad.xp` sprite as a reference.
    Draws draggable "input slots" and fixed "output positions".
    Implements drag-and-drop state machine for mapping creation.
  </task>
  <task id="11.4.2" title="Implement Inverse Mapping Rebuild" est="M">
    Port `InvertMap` logic from `gamepad.cpp`.
    Requirement: FMENU-04.
    Automatically rebuilds lookup tables whenever a mapping changes.
    Ensures efficient event routing for the input system.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 11.4.A:** Gamepad UI.
- *Action:* Run game, open gamepad config, drag an input button to the 'A' slot.
- *Requirement:* The slot must update to show the mapped button index.
- *Submission:* Screenshot of the mapped gamepad UI.

### Layer 2: Integration (ECS Contract)
Run the mapping rebuild test:
```bash
cargo test --test gamepad_mapping_rebuild -- --nocapture
```
- *Input:* Change mapping for ID 5.
- *Expected Output:* `button_mapping[0]` contains `5`.

### Layer 1: Logic (Data-Only)
Run the inverse lookup test:
```bash
cargo test gamepad_ui::tests::test_invert_logic -- --nocapture
```
- *Expected Data:* Final inverse array dump.
</verification>

<success_criteria>
- Gamepad mapping UI displays the controller sprite and interactive slots.
- Drag-and-drop mapping creation is functional and updates lookup tables.
- Inverse mapping rebuild correctly propagates changes to the input system.
</success_criteria>

## Contract Check

- FMENU-04: Task 11.4.1 and 11.4.2 implement the visual configuration interface.

## Consistency Diff

- The visual mapping UI is an optional high-fidelity feature that matches `gamepad.cpp` for controller-first players.

## Blocking Checklist

- [ ] FMENU-04
- [ ] Drag-drop logic verified by integration test
- [ ] Lookup table rebuild verified

<risks>
  <risk id="R81" severity="MEDIUM" mitigation="Defer if necessary">
    Visual mapping is high-complexity for low-priority MVP value; defer if timeline is tight.
  </risk>
</risks>

<notes>
This plan is marked optional and should only be executed if all other Phase 11 requirements are met.
</notes>

<output>
After completion, create `.planning/phases/11-full-menu-system/11-04-SUMMARY.md`
</output>
