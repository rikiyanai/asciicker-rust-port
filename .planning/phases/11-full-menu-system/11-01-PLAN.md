---
phase: 11-full-menu-system
plan: 01
type: execute
wave: 1
depends_on: ["07-02"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/menu_logic.rs
autonomous: true
requirements:
  - FMENU-01

must_haves:
  truths:
    - "Hierarchical menu navigation supports root and sub-menu levels."
    - "Navigation state includes selected index, scroll position, and parent stack."
    - "Menu supports both keyboard (arrows/enter) and mouse selection."
    - "State machine correctly transitions between different menu screens."
  artifacts:
    - path: "engine-port/src/game/menu_logic.rs"
      provides: "Hierarchical menu navigation and state management"
      min_lines: 150
---

**Requirements**: FMENU-01

<objective>
Implement the full hierarchical menu state machine and navigation logic, extending the initial skeleton.

Purpose: Provides the core logic for navigating the game's settings, level selection, and main menu. The system uses a stack-based hierarchy to allow deep nesting of sub-menus (Video, Audio, Controls) while maintaining user orientation.

Output: `src/game/menu_logic.rs` (Menu tree + Navigation).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/11-full-menu-system/11-RESEARCH.md

Key references:
- Phase 7: Initial GameState and Menu skeleton (07-02)
- C++ mainmenu.cpp: Hierarchical state variables (`menu_depth`, `menu_stack`)
- C++ mainmenu.cpp: Navigation input handling logic
</context>

<tasks>
  <task id="11.1.1" title="Define Hierarchical Menu Tree" est="M">
    Implement a data-driven `MenuTree` structure.
    Requirement: FMENU-01.
    Include nodes for: Root → [Start, Level Select, Video, Controls, Audio, Exit].
    Sub-menus: Video → [Resolution, Perspective, Dithering].
    Support toggle items, slider items, and action items.
  </task>
  <task id="11.1.2" title="Implement MenuStack Resource" est="S">
    Define `MenuStack` Resource.
    Requirement: FMENU-01.
    Stores a stack of `(selected_index, menu_id)` tuples.
    Manages `menu_depth` and transitions between levels.
  </task>
  <task id="11.1.3" title="Implement Navigation System" est="M">
    Implement `menu_navigation_system` in `Update`.
    Requirement: FMENU-01.
    Handles Arrow keys (Up/Down for select, Right/Enter for push, Left/Esc for pop).
    Handles Mouse hover and click events.
    Calculates `menu_scroll` target based on selection visibility.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 11.1.A:** Menu Navigation.
- *Action:* Navigate from Root -> Video -> Resolution.
- *Requirement:* The menu stack must push/pop correctly, and the display must update to show the sub-menu items.
- *Submission:* Console log showing the `MenuStack` contents at each step.

### Layer 2: Integration (ECS Contract)
Run the selection wrap test:
```bash
cargo test --test menu_selection_wrap -- --nocapture
```
- *Input:* Press DOWN at last item.
- *Expected Output:* `selection_index: 0`.

### Layer 1: Logic (Data-Only)
Run the stack logic unit tests:
```bash
cargo test menu_logic::tests::test_stack_depth -- --nocapture
```
- *Input:* Push 5 levels.
- *Expected Output:* `depth: 4` (clamped).
</verification>

<success_criteria>
- Hierarchical menu navigation supports root and sub-menu levels.
- Navigation state includes selected index, scroll position, and parent stack.
- Menu supports both keyboard (arrows/enter) and mouse selection.
- State machine correctly transitions between different menu screens.
</success_criteria>

## Contract Check

- FMENU-01: Task 11.1.1, 11.1.2, and 11.1.3 implement the menu hierarchy and navigation logic.

## Consistency Diff

- The hierarchical menu state machine is a direct port of mainmenu.cpp's `menu_depth` and `menu_stack` architecture.

## Blocking Checklist

- [ ] FMENU-01
- [ ] Hierarchy stack logic verified by test
- [ ] Navigation input verified

<risks>
  <risk id="R78" severity="LOW" mitigation="Limit depth to 4">
    Deep menu nesting can be complex; cap at 4 levels as in C++ original.
  </risk>
</risks>

<notes>
This plan covers logic only; rendering and transitions are handled in 11-02.
</notes>

<output>
After completion, create `.planning/phases/11-full-menu-system/11-01-SUMMARY.md`
</output>
