---
phase: 11-full-menu-system
plan: 03
type: execute
wave: 3
depends_on: ["11-02", "02-04"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/settings.rs
  - engine-port/src/game/level_select.rs
autonomous: true
requirements:
  - FMENU-03

must_haves:
  truths:
    - "Level selection screen lists all available .a3d files in the assets directory."
    - "Selecting a level triggers the Loading state and initiates world loading."
    - "Game settings (Video, Audio, Controls) are persisted to a JSON config file."
    - "Settings are loaded at startup and applied to the corresponding subsystems."
  artifacts:
    - path: "engine-port/src/game/settings.rs"
      provides: "Settings serialization and persistence logic"
      min_lines: 100
    - path: "engine-port/src/game/level_select.rs"
      provides: "Level selection menu population and logic"
      min_lines: 80
---

**Requirements**: FMENU-03

<objective>
Implement level selection and persistent settings management.

Purpose: Allows players to choose which world to load and customize their game experience. Settings persistence ensures that user preferences (volume, resolution, keybinds) are remembered across sessions.

Output: `src/game/settings.rs` (Config), `src/game/level_select.rs` (Menu logic).
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/11-full-menu-system/11-RESEARCH.md

Key references:
- Phase 2: .a3d world loaders and asset server
- C++ mainmenu.cpp: Level selection and manifest logic
- Bevy: config directory patterns for desktop platforms
</context>

<tasks>
  <task id="11.3.1" title="Implement Level File Browser" est="M">
    Implement `list_level_files` utility using `std::fs` or `glob`.
    Requirement: FMENU-03.
    Scans `assets/a3d/` for files with `.a3d` extension.
    Populates the 'Level Select' sub-menu dynamically.
  </task>
  <task id="11.3.2" title="Implement Settings Persistence" est="M">
    Define `UserSettings` struct with `serde::Serialize/Deserialize`.
    Requirement: FMENU-03.
    Include: `master_volume`, `resolution_scale`, `perspective_toggle`, `keybinds`.
    Implement `save_settings` and `load_settings` using `serde_json`.
    Determine platform-appropriate config path (e.g., `~/.config/asciicker/`).
  </task>
  <task id="11.3.3" title="Apply Settings at Runtime" est="S">
    Implement `apply_settings_system` that monitors `UserSettings` changes.
    Requirement: FMENU-03.
    Updates `AudioMixer`, `RenderConfig`, and `InputSettings` resources accordingly.
  </task>
</tasks>

<verification>
### Layer 4: Manual Verification (Authoritative)
**GATE 11.3.A:** Settings Persistence.
- *Action:* Change master volume to 0.2, exit game, restart.
- *Requirement:* The volume must remain at 0.2.
- *Submission:* Copy the contents of `settings.json` from the config directory.

### Layer 2: Integration (ECS Contract)
Run the runtime application test:
```bash
cargo test --test settings_application_runtime -- --nocapture
```
- *Input:* Change perspective toggle.
- *Expected Output:* `GameCamera.perspective` changes within 1 frame.

### Layer 1: Logic (Data-Only)
Run the serialization unit tests:
```bash
cargo test settings::tests::test_json_roundtrip -- --nocapture
```
- *Expected Data:* JSON string matching the expected field set.
</verification>

<success_criteria>
- Level selection screen lists all available .a3d files in the assets directory.
- Selecting a level triggers the Loading state and initiates world loading.
- Game settings (Video, Audio, Controls) are persisted to a JSON config file.
- Settings are loaded at startup and applied to the corresponding subsystems.
</success_criteria>

## Contract Check

- FMENU-03: Task 11.3.1, 11.3.2, and 11.3.3 implement file browsing and persistence.

## Consistency Diff

- Configuration is stored in JSON format for modern portability, replacing potential binary blobs or registry keys.

## Blocking Checklist

- [ ] FMENU-03
- [ ] Config file save/load verified by test
- [ ] Runtime setting application verified

<risks>
  <risk id="R80" severity="LOW" mitigation="Use default fallback">
    Malformed or missing config files should not crash the game; provide safe defaults.
  </risk>
</risks>

<notes>
Level loading uses the Phase 7 `GameState::Loading` flow.
</notes>

<output>
After completion, create `.planning/phases/11-full-menu-system/11-03-SUMMARY.md`
</output>
