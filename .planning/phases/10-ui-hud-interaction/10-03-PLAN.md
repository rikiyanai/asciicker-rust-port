---
phase: 10-ui-hud-interaction
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "08-01"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/minimap.rs
autonomous: true
requirements:
  - HUD-03

must_haves:
  truths:
    - "Minimap renders in the top-right corner of the screen (32x16 cells)."
    - "Terrain height is sampled and mapped to brown/green/white color ramps."
    - "NPCs appear as red/green dots based on alliance."
    - "Player orientation is indicated by a directional arrow."
  artifacts:
    - path: "engine-port/src/game/minimap.rs"
      provides: "Minimap rendering system and sampling logic"
      min_lines: 120
---

**Requirements**: HUD-03

<objective>
Implement the HUD Minimap, providing a top-down view of the surrounding terrain and entities.

Purpose: Facilitates navigation by showing terrain elevation and nearby NPC locations. The minimap samples the world geometry at a coarse resolution and renders it as a colored cell grid in the HUD.

Output: `src/game/minimap.rs` (Minimap system).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/10-ui-hud-interaction/10-RESEARCH.md

Key references:
- Phase 5: RuntimeTerrain height sampling (interpolate_height)
- Phase 8: NPC positions and SpatialGrid
- C++ game.cpp: minimap rendering logic
</context>

<tasks>
  <task id="10.3.1" title="Implement Terrain Height Sampling" est="M">
    Implement `sample_minimap_terrain`.
    Requirement: HUD-03.
    Iterates a 32x16 grid around the player position.
    Queries `RuntimeTerrain` for height at each point.
    Maps height to colors: Green (Low), Brown (High), White (Peaks).
  </task>
  <task id="10.3.2" title="Implement Entity Dot Rendering" est="S">
    Query `SpatialGrid` for characters within minimap range.
    Requirement: HUD-03.
    Map world coordinates to 32x16 grid coordinates.
    Paint red dots for enemies and green dots for buddies.
  </task>
  <task id="10.3.3" title="Implement Minimap UI" est="M">
    Implement `render_minimap_system` in `PostUpdate`.
    Requirement: HUD-03.
    Draws the sampled terrain and entity dots to the top-right `AsciiCellGrid`.
    Draws a white arrow at the center representing player facing direction (`yaw`).
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 10.3.A:** Minimap Fidelity.
- *Action:* Walk from a low-altitude area (green) to a high-altitude area (brown/white).
- *Requirement:* The minimap colors must update in real-time to reflect the terrain.
- *Submission:* Screenshot of the minimap showing a mountain peak.

### Layer 2: Integration (ECS Contract)
Run the entity dot projection test:
```bash
cargo test --test minimap_dot_projection -- --nocapture
```
- *Input:* NPC at [50, 50]. Player at [0, 0].
- *Expected Data:* `Dot at grid (20, 10)` (scaled position).
- *Agent Action:* Predict the grid coordinate based on the 32x16 map scale before running.

### Layer 1: Logic (Data-Only)
Run the height-to-color mapping test:
```bash
cargo test minimap::tests::test_color_ramp -- --nocapture
```
- *Input:* height = 4000 (raw).
- *Expected Output:* `color: BROWN`.
</verification>

<success_criteria>
- Minimap renders in the top-right corner of the screen (32x16 cells).
- Terrain height is sampled and mapped to brown/green/white color ramps.
- NPCs appear as red/green dots based on alliance.
- Player orientation is indicated by a directional arrow.
</success_criteria>

## Contract Check

- HUD-03: Task 10.3.1, 10.3.2, and 10.3.3 implement terrain sampling and minimap dots.

## Consistency Diff

- Minimap samples the quadtree directly instead of using a separate texture, which is idiomatic for the ASCII software rasterizer architecture.

## Blocking Checklist

- [ ] HUD-03
- [ ] Terrain color mapping verified
- [ ] Entity dot projection verified

<risks>
  <risk id="R76" severity="LOW" mitigation="Sample only visible area">
    Sampling the full world quadtree every frame might be heavy; limit to a fixed radius around player.
  </risk>
</risks>

<notes>
The minimap is a 32x16 cell overlay; use Font1 characters or ASCII blocks for high-fidelity elevation detail.
</notes>

<output>
After completion, create `.planning/phases/10-ui-hud-interaction/10-03-SUMMARY.md`
</output>
