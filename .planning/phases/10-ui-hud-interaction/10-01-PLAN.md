---
phase: 10-ui-hud-interaction
plan: 01
type: execute
wave: 1
depends_on: ["07-04", "08-04"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/hud_bars.rs
autonomous: true
requirements:
  - HUD-01

must_haves:
  truths:
    - "Health and Mana bars render at the bottom-left of the screen."
    - "Bar length scales proportionally with current/max values."
    - "Status bars update dynamically as HP/MP values change."
    - "Rendering uses Font1 set_cell() methods on the AsciiCellGrid."
  artifacts:
    - path: "engine-port/src/game/hud_bars.rs"
      provides: "Systems for rendering HP/MP status bars"
      min_lines: 100
---

**Requirements**: HUD-01

<objective>
Implement the HP and MP status bars, providing critical gameplay feedback on the player's health and resources.

Purpose: Provides a clear visual indicator of player health and mana. The bars use character-based graphics rendered directly onto the `AsciiCellGrid`, matching the original engine's HUD aesthetic.

Output: `src/game/hud_bars.rs` (HUD rendering systems).
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/10-ui-hud-interaction/10-RESEARCH.md

Key references:
- Phase 7: Font1 system for text/character rendering
- Phase 8: CombatState with HP/MP values
- C++ game.cpp: HUD rendering sections
</context>

<tasks>
  <task id="10.1.1" title="Define HudState Resource" est="S">
    Create `HudState` Resource to track UI-related flags (visibility, position offsets).
    Requirement: HUD-01.
  </task>
  <task id="10.1.2" title="Implement Bar Segment Logic" est="M">
    Calculate bar width in cells based on `(current / max) * max_width`.
    Requirement: HUD-01.
    Use CP437 block characters (e.g., 219, 176, 177, 178) for the bar fill.
    Handle horizontal fill direction (left-to-right).
  </task>
  <task id="10.1.3" title="Implement HP/MP Rendering System" est="M">
    Implement `render_hud_bars_system` in `PostUpdate`.
    Requirement: HUD-01.
    Reads `Query<&CombatState, With<Player>>`.
    Paints HP bar (red) and MP bar (blue) at bottom-left corner.
    Include percentage text or "HP: X/Y" overlays using `Font1`.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 10.1.A:** HUD Layout.
- *Action:* Run game and take damage.
- *Requirement:* The health bar at the bottom-left must visually decrease cell-by-cell.
- *Submission:* Provide two screenshots: one at Full HP and one at 50% HP.

### Layer 2: Integration (ECS Contract)
Run the bar scaling integration test:
```bash
cargo test --test hud_bars_scaling -- --nocapture
```
- *Input:* 75/100 HP. Max bar width 20 cells.
- *Expected Output:* `Bar Width: 15 cells`.
- *Agent Action:* Verify the CP437 glyph used for the "fill" matches the C++ engine (e.g., char 219).

### Layer 1: Logic (Data-Only)
Run the math unit tests:
```bash
cargo test hud_bars::tests::test_percentage_calc -- --nocapture
```
- *Input:* current=3, max=10.
- *Expected Output:* `width: 30%`.
</verification>

<success_criteria>
- Health and Mana bars render at the bottom-left of the screen.
- Bar length scales proportionally with current/max values.
- Status bars update dynamically as HP/MP values change.
- Rendering uses Font1 set_cell() methods on the AsciiCellGrid.
</success_criteria>

## Contract Check

- HUD-01: Task 10.1.1, 10.1.2, and 10.1.3 implement the HP/MP bars and rendering.

## Consistency Diff

- HUD elements are rendered as overlays on the existing AsciiCellGrid, matching the C++ software-compositing approach.

## Blocking Checklist

- [ ] HUD-01
- [ ] Bar scaling logic verified by test
- [ ] Font1 integration verified

<risks>
  <risk id="R74" severity="LOW" mitigation="Use anchored coordinates">
    Screen resizing may move the bars; ensure coordinates are relative to `AsciiCellGrid` bounds.
  </risk>
</risks>

<notes>
Keep the rendering logic decoupled from combat logic; it should only read the current state.
</notes>

<output>
After completion, create `.planning/phases/10-ui-hud-interaction/10-01-SUMMARY.md`
</output>
