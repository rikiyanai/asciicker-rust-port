---
phase: 10-ui-hud-interaction
plan: 04
type: execute
wave: 3
depends_on: ["10-03", "09-03", "05-03"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/targeting.rs
  - engine-port/src/game/floaters.rs
autonomous: true
requirements:
  - HUD-04

must_haves:
  truths:
    - "Screen-to-world unprojection correctly converts mouse clicks to world positions."
    - "Damage numbers (floaters) appear at hit locations and float upwards."
    - "Floating text fades out and despawns automatically."
    - "Mouse hover identifies interactive items and NPCs."
  artifacts:
    - path: "engine-port/src/game/targeting.rs"
      provides: "Unprojection and mouse targeting systems"
      min_lines: 100
    - path: "engine-port/src/game/floaters.rs"
      provides: "Damage floater management and rendering"
      min_lines: 80
---

**Requirements**: HUD-04

<objective>
Implement screen-to-world unprojection for mouse targeting and world-space damage floaters.

Purpose: Enables mouse interaction with the 3D world (picking items, selecting targets) and provides visual combat feedback. Unprojection is the mathematical inverse of the rendering pipeline's projection matrix.

Output: `src/game/targeting.rs` (Mouse logic), `src/game/floaters.rs` (Visual feedback).
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/10-ui-hud-interaction/10-RESEARCH.md

Key references:
- Phase 5: View and Projection matrices in `GameCamera`
- C++ render.cpp: UnprojectCoords2D/3D functions
- Phase 8: Combat system for triggering damage floaters
</context>

<tasks>
  <task id="10.4.1" title="Implement Screen-to-World Unprojection" est="M">
    Implement `unproject_screen_to_world` using the inverse view-projection matrix.
    Requirement: HUD-04.
    Handle both 2D (using depth buffer value) and 3D (intersecting a fixed Z-plane) cases.
    Ensure compatibility with `AsciiCellGrid` coordinate scaling.
  </task>
  <task id="10.4.2" title="Implement Mouse Targeting System" est="M">
    Implement `mouse_targeting_system`.
    Requirement: HUD-04.
    When mouse hovers/clicks, unproject to find world-space position.
    Query `SpatialGrid` for items or NPCs near the unprojected position.
    Update `HudState` with the current target entity.
  </task>
  <task id="10.4.3" title="Implement Damage Floaters" est="M">
    Define `DamageFloater` Component with world position, velocity, and lifetime.
    Requirement: HUD-04.
    Implement `spawn_damage_floater` triggered by `CombatHitEvent`.
    Implement `update_floaters_system` to move and fade them.
    Render floaters in `PostUpdate` using `project_world_to_screen` and `Font1`.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 10.4.A:** Mouse Targeting.
- *Action:* Hover mouse over an NPC.
- *Requirement:* A damage floater or highlight should indicate the target is selected.
- *Submission:* Screenshot showing the "hover" state.

### Layer 2: Integration (ECS Contract)
Run the unprojection accuracy test:
```bash
cargo test --test mouse_unprojection_accuracy -- --nocapture
```
- *Input:* Click at screen center [120, 67].
- *Expected Output:* `World Pos: [X, Y, Z]` matching the terrain height at the camera's focus point.
- *Agent Action:* Perform a manual `interpolate_height` check at the resulting X,Y to verify the Z accuracy.

### Layer 1: Logic (Data-Only)
Run the floater movement test:
```bash
cargo test floaters::tests::test_upward_velocity -- --nocapture
```
- *Input:* lifetime 0.5s.
- *Expected Output:* `pos.z increased by 1.0` (approx).
</verification>

<success_criteria>
- Screen-to-world unprojection correctly converts mouse clicks to world positions.
- Damage numbers (floaters) appear at hit locations and float upwards.
- Floating text fades out and despawns automatically.
- Mouse hover identifies interactive items and NPCs.
</success_criteria>

## Contract Check

- HUD-04: Task 10.4.1, 10.4.2, and 10.4.3 implement unprojection and combat feedback.

## Consistency Diff

- Unprojection uses the same mathematical inverse as C++ to ensure mouse interaction matches the visual scene perfectly.

## Blocking Checklist

- [ ] HUD-04
- [ ] Unprojection accuracy verified by test
- [ ] Floater despawn verified by test

<risks>
  <risk id="R77" severity="MEDIUM" mitigation="Use epsilon for depth">
    Depth buffer precision may cause unprojection artifacts; use small epsilon when sampling near geometry.
  </risk>
</risks>

<notes>
Floaters should be very lightweight entities that exist only for ~1 second.
</notes>

<output>
After completion, create `.planning/phases/10-ui-hud-interaction/10-04-SUMMARY.md`
</output>
