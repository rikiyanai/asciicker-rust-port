---
phase: 031-audit-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine-port/src/output/gpu_plugin.rs
  - engine-port/src/core/coords.rs
  - engine-port/src/core/mod.rs
  - engine-port/src/asset_loader/xp_sprite.rs
  - engine-port/src/asset_loader/a3d_world.rs
  - engine-port/src/asset_loader/error.rs
  - engine-port/src/output/gpu_types.rs
  - engine-port/tests/plugin_ordering.rs
  - engine-port/src/render/sample_buffer.rs
  - engine-port/src/render/quantize.rs
  - engine-port/src/render/material.rs
  - engine-port/src/render/resolve.rs
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - GAP-02
  - GAP-03
  - GAP-06
  - GAP-10
  - GAP-11

must_haves:
  truths:
    - "TextureView objects stored in AsciiGpuTextures persist alongside BindGroup"
    - "GameVec3 is a newtype wrapper rejecting implicit Vec3 assignment at compile time"
    - "XP parser returns InvalidDimensions on overflow sprite dimensions"
    - "A3D world parser returns InvalidTransform on NaN/Inf matrix values"
    - "Font atlas miss emits warn! instead of silent black screen"
    - "Glyph index u16-to-u8 has debug_assert for range check"
    - "Integration test verifies plugin init order without panic"
  artifacts:
    - path: "engine-port/src/output/gpu_plugin.rs"
      provides: "TextureView fields in AsciiGpuTextures"
    - path: "engine-port/src/core/coords.rs"
      provides: "GameVec3 newtype with Deref and conversion methods"
    - path: "engine-port/src/asset_loader/xp_sprite.rs"
      provides: "checked_mul on sprite dimensions"
    - path: "engine-port/src/asset_loader/a3d_world.rs"
      provides: "is_finite validation on transform matrix"
    - path: "engine-port/tests/plugin_ordering.rs"
      provides: "3 integration tests for plugin init order"
  key_links:
    - from: "gpu_plugin.rs AsciiGpuTextures"
      to: "BindGroup creation"
      via: "TextureView fields referenced in bind group entries"
    - from: "core/coords.rs GameVec3"
      to: "core/mod.rs re-export"
      via: "pub use GameVec3 struct"
---

<objective>
Fix 5 code-level risks from the Phases 1-4 audit: TextureView lifetime, GameVec3 type safety, parser robustness, GPU error logging, and plugin ordering verification.

Purpose: Harden existing code before Phase 5 pipeline integration.
Output: 7 modified + 1 new file, ~100-200 LOC total change.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/RISK-ASSESSMENT.md
@engine-port/src/output/gpu_plugin.rs
@engine-port/src/core/coords.rs
@engine-port/src/asset_loader/xp_sprite.rs
@engine-port/src/asset_loader/a3d_world.rs
@engine-port/src/output/gpu_types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 5 audit items (AUDIT-01 through AUDIT-04)</name>
  <!-- P31-001 FIX: Task title says "5 items" but covers only 4 AUDIT items (AUDIT-01 through AUDIT-04), not 5. AUDIT-05 is handled separately in Task 2. -->
  <!-- P31-I01 (INFO): Audit confirmed this existing P31-001 FIX tag is sufficient and accurate. No further correction needed. -->
  <files>engine-port/src/output/gpu_plugin.rs, engine-port/src/core/coords.rs, engine-port/src/core/mod.rs, engine-port/src/asset_loader/xp_sprite.rs, engine-port/src/asset_loader/a3d_world.rs, engine-port/src/asset_loader/error.rs, engine-port/src/output/gpu_types.rs</files>
  <action>
    **AUDIT-01**: In `gpu_plugin.rs`, add `fore_view: Option<TextureView>`, `back_view: Option<TextureView>`, `text_view: Option<TextureView>` fields to `AsciiGpuTextures`. In `prepare_ascii_textures`, create views into struct fields after texture upload, before bind group creation. Reference stored views when building BindGroup.

    **IMPORTANT (resize path gap):** Plan 03-03 Task 1 (commit `0dfe33d`) added a resize-triggered BindGroup recreation path that also creates local TextureView objects. The executor MUST fix BOTH code paths:
    1. The original prepare path (initial texture creation)
    2. The resize path (texture recreation when `extracted.width != textures.last_width`)
    Both paths must store views in AsciiGpuTextures fields before building BindGroup. ~5 extra LOC beyond the original prepare path fix.

    **AUDIT-02**: In `coords.rs`, replace `pub type GameVec3 = Vec3` with `pub struct GameVec3(pub Vec3)` newtype. Add `new()`, `to_bevy()`, `from_bevy()`, `inner()`, `ZERO` const, and `Deref<Target=Vec3>`. Update `game_to_bevy`/`bevy_to_game` signatures. Update tests. Verify `core/mod.rs` re-export works.

    **AUDIT-03 (R10)**: In `xp_sprite.rs` line 138, replace `width * height` with `width.checked_mul(height).ok_or(AssetError::InvalidDimensions(...))`. Add unit test with 65536x65536 dimensions.

    **AUDIT-03 (R11)**: Add `InvalidTransform(usize)` to `error.rs`. In `a3d_world.rs`, after `read_f64x16` call (line 190), add `if !tm.iter().all(|v| v.is_finite()) { return Err(...) }`. Add unit test.

    **AUDIT-04 (R13)**: In `gpu_plugin.rs`, change font atlas guard to emit `warn!("ASCII GPU: font atlas not ready, skipping frame")`.

    **AUDIT-04 (R16)**: In `gpu_types.rs` `extract_grid_data`, add `debug_assert!(idx <= 255)` before `idx as u8`.
  </action>
  <verify>`cargo test --lib` passes (existing + new tests). `cargo clippy -- -D warnings` clean.</verify>
  <done>All 4 audit items implemented. TextureView stored, GameVec3 is newtype, checked_mul present, is_finite present, warn! present, debug_assert present.</done>
</task>

<task type="auto">
  <name>Task 2: Plugin ordering integration test (AUDIT-05)</name>
  <files>engine-port/tests/plugin_ordering.rs</files>
  <action>
    Create `engine-port/tests/plugin_ordering.rs` with 3 tests:
    1. `correct_plugin_order_succeeds`: MinimalPlugins + CpuRasterizerPlugin + AsciiOutputPlugin, verify RenderConfig and AsciiCellGrid exist.
    2. `all_plugins_init_in_main_order`: MinimalPlugins + all 8 plugins in main.rs order, verify no panic.
    3. `missing_render_config_panics`: `#[should_panic]` test with only AsciiOutputPlugin (no CpuRasterizerPlugin). Adjust expected panic substring to match Bevy 0.18 message.
    - **P31-004 FIX:** The panic originates from `SampleBuffer::from_world` calling `world.resource::<RenderConfig>()` when `RenderConfig` is absent — not directly from `AsciiOutputPlugin`. `CpuRasterizerPlugin` is the plugin that registers `RenderConfig`; without it, the `from_world` call panics. The expected panic substring must match Bevy's missing-resource message, not AsciiOutputPlugin's output.
    - **R6-M04 FIX:** Use `#[should_panic]` WITHOUT an `expected` attribute — the exact Bevy 0.18 panic message text is version-dependent and may change. The test validates that accessing a missing resource panics; the specific message format is not part of the contract.
    - **L-01 NOTE (Round 4):** The test name `missing_render_config_panics` is accurate. However: the expected panic substring is Bevy's missing-resource message (e.g., `"Resource requested by ... does not exist"`), NOT a message from `AsciiOutputPlugin` or `CpuRasterizerPlugin` directly. The panic is emitted by Bevy's ECS `world.resource::<T>()` call inside `SampleBuffer::from_world`. A reader seeing only this test name might expect the panic to originate from plugin code — it does not.
  </action>
  <verify>`cargo test --test plugin_ordering` passes all 3 tests. `cargo test` full suite passes.</verify>
  <done>3 integration tests verify plugin ordering: correct order, full order, missing dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 4 execution gap fixes (GAP-02, GAP-03, GAP-06, GAP-10, GAP-11)</name>
  <files>engine-port/src/render/sample_buffer.rs, engine-port/src/render/quantize.rs, engine-port/src/render/material.rs, engine-port/src/render/resolve.rs</files>
  <action>
    **GAP-06 (R40)**: In `sample_buffer.rs`, remove dead `unsafe` unchecked accessor methods that are never called anywhere in the codebase. If they exist as `pub(crate)` helpers, delete them. If they're used internally, add safety comments documenting the invariant.

    **GAP-10 (R43)**: In `sample_buffer.rs`, add boundary tests:
    - `test_sample_buffer_zero_size`: SampleBuffer::new(0, 0) does not panic
    - `test_sample_buffer_border_pixels`: Verify border pixels (+2 on each side) are accessible and initialized
    - `test_sample_buffer_last_valid_index`: Write and read the last valid sample at (2*w+3, 2*h+3)

    **GAP-02 (R36)**: In `quantize.rs`, add exhaustive RGB555 range validation test:
    - `test_rgb2pal_all_32768_values_return_valid_index`: Loop over all 32768 RGB555 values (0..32768), call `rgb2pal()` for each, assert result is in 16..=231 (xterm-256 color cube range). This validates the full domain without needing C++ reference data.
    - `test_rgb555_roundtrip_all_values`: For all 32768 values, verify `rgb888_to_rgb555(rgb555_to_rgb888(v))` == v

    **GAP-03 (R37)**: In `material.rs`, add auto_mat LUT consistency test:
    - `test_auto_mat_lut_full_table_consistency`: After `init_auto_mat()`, iterate all 32768 entries. For each, verify: fg palette index is in 16..=231, bg palette index is in 16..=231, glyph is non-zero (>0). This validates internal consistency without C++ reference.
    - `test_auto_mat_lut_symmetry_spot_checks`: For 10 specific RGB555 values (black, white, pure R/G/B, mid-greys), verify the returned dither pair produces visually plausible fg/bg contrast.

    **GAP-11 (R41)**: In `resolve.rs`, add reflection palette path test:
    - `test_resolve_material_reflection_path`: Create a SampleBuffer with samples that trigger the reflection path (diffuse divisor 400 vs 255). Verify the resolved AnsiCell has darker palette indices than the non-reflection path for the same material. The reflection path should produce visibly dimmer output.
    - **P31-M01 FIX:** NOTE: The "verify darker palette indices" requirement above is technically unachievable as written. `diffuse_divisor=400` is only consumed by `resolve_mesh()` — for terrain/material samples, `resolve_material()` is called and does not use `diffuse_divisor` at all. Therefore no "darker output" can be verified for material-path reflections. The actual test (`test_resolve_material_reflection_path`) verifies render completeness only: `spare=0xFF` and valid palette indices for triggered reflection samples. The "darker output" property is only observable in mesh reflections, but mesh + REFLECTION bits are mutually exclusive via the `is_reflection` guard (`MESH_FLAG==0` required). This is a known semantic limitation documented in the test file comments.
  </action>
  <verify>`cargo test --lib -- sample_buffer` passes. `cargo test --lib -- quantize` passes. `cargo test --lib -- material` passes. `cargo test --lib -- resolve` passes. `cargo clippy -- -D warnings` clean.</verify>
  <done>Phase 4 execution gaps closed: dead unsafe removed, boundary tests added, RGB555 full-domain validation, auto_mat LUT consistency check, reflection path tested.</done>
</task>

</tasks>

<verification>
1. `cargo test` -- all tests pass (existing + new)
2. `cargo clippy -- -D warnings` -- zero warnings
3. `cargo fmt -- --check` -- clean
4. GameVec3 is struct, TextureView stored, checked_mul present, is_finite present, warn! present, debug_assert present
5. RGB555 all 32768 values validated, auto_mat LUT consistent, reflection path tested, SampleBuffer boundary tests pass
</verification>

<success_criteria>
All 5 AUDIT requirements + 5 Phase 4 execution gaps addressed. Zero test regressions. 10+ new tests. Clippy clean.
</success_criteria>

## Contract Check

All 10 requirement IDs appear in the plan's `requirements` frontmatter:
- AUDIT-01: TextureView lifetime (Task 1)
- AUDIT-02: GameVec3 newtype (Task 1)
- AUDIT-03: Parser robustness -- checked_mul + is_finite (Task 1)
- AUDIT-04: GPU hardening -- warn! + debug_assert (Task 1)
- AUDIT-05: Plugin ordering integration test (Task 2)
- GAP-02 (R36): RGB555 exhaustive range validation (Task 3)
- GAP-03 (R37): auto_mat LUT consistency check (Task 3)
- GAP-06 (R40): Remove dead unsafe SampleBuffer accessors (Task 3)
- GAP-10 (R43): SampleBuffer boundary tests (Task 3)
- GAP-11 (R41): Reflection palette path test (Task 3)

No requirement is missing. No deferred items are implemented.

## Consistency Diff

- ROADMAP.md Phase 3.1 lists AUDIT-01 through AUDIT-05. Plan covers all 5.
- RISK-ASSESSMENT.md R04/R08/R09/R10/R11/R13/R16 mapped to AUDIT-01 through AUDIT-05.
- STATE.md shows Phase 3 and 4 complete. This plan modifies existing Phase 1-4 code only.
- No conflict with Phase 5 plans (05-01 through 05-06) -- those files are untouched.
- GameVec3 newtype change is backward-compatible via Deref; Phase 5 converters will use the newtype.

## Blocking Checklist

- [x] Every AUDIT requirement ID appears in plan's `requirements` field
- [x] Tasks have files, action, verify, done
- [x] No file overlap between this plan and other active plans
- [x] Wave 1 single-wave execution (all fixes independent)
- [x] Scope is surgical: ~100-200 LOC, 8 files

<output>
After completion, create `.planning/phases/03.1-audit-remediation/031-01-SUMMARY.md`
</output>
