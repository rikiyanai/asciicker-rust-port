---
phase: 07.1-physics-character-polish
plan: 01
type: execute
wave: 1
depends_on: ["06-03"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/physics/mod.rs
autonomous: true
requirements:
  - PHYS-FIX-01
  - CHAR-FIX-01

must_haves:
  truths:
    - "Camera position is synchronized to player position from physics output."
    - "WASD forces are rotated by camera yaw before physics integration."
    - "Block input key (KeyF) is functional and triggers state transition."
  artifacts:
    - path: "engine-port/src/game/mod.rs"
      provides: "sync_camera_to_player system and yaw-relative WASD logic"
      contains: "sync_camera_to_player"
---

**Requirements**: PHYS-FIX-01, CHAR-FIX-01

<objective>
Fix critical integration gaps from Phase 6 identified during audit: sync camera to player, implement camera-relative movement, and wire the Block action.

Purpose: Ensures the character moves relative to where the camera is looking (standard 3D game feel) and that the camera follows the player.

Output: Updated `game/mod.rs` with integration systems.
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md
 
Key references:
- R54: Camera stays at origin while character moves.
- R57: WASD forces not rotated by facing direction.
- R56: No Block input key defined.
</context>

<tasks>
  <task id="7.1.1" title="Implement Camera-Following-Player" est="S">
    Implement `sync_camera_to_player` system in `PostUpdate`.
    Requirement: PHYS-FIX-01.
    Updates `GameCamera.pos` from `PhysicsIO.pos` (physics output).
    Ensures camera projects correctly around the player.
  </task>
  <task id="7.1.2" title="Implement Camera-Relative Movement" est="S">
    Update `accumulate_player_input` in `character/input.rs`.
    Requirement: CHAR-FIX-01.
    Rotate `x_force` and `y_force` based on `GameCamera.yaw` before writing to `PhysicsIO`.
    Ensures 'W' always moves forward relative to camera.
  </task>
  <task id="7.1.3" title="Wire Block Input" est="S">
    Define `KeyCode::KeyF` as the Block action trigger.
    Requirement: CHAR-FIX-01.
    Transition `ActionState` to `Block` when key is held.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 7.1.1.A:** Sync Verification.
- *Action:* Run the game, move the character using WASD.
- *Requirement:* The camera must stay centered on the CP437 character glyph at all times.
- *Submission:* Provide a screenshot showing the character at a non-origin coordinate (e.g., [100, 100]) with the camera successfully projected at that location.

### Layer 2: Integration (ECS Contract)
Run the sync test and provide the coordinate trace:
```bash
cargo test --test character_camera_sync -- --nocapture
```
- *Input:* Move character to [10.0, 20.0, 5.0].
- *Expected Output:* `camera.pos: [10.0, 20.0, 80.0]` (Z scaled by 16.0).
- *Agent Action:* Verify the Z-coordinate scaling factor (16.0) is applied correctly in the printed log.

### Layer 1: Logic (Data-Only)
Run the rotation math test:
```bash
cargo test tests::test_rotation_frame_rate_independent -- --nocapture
```
- *Expected Output:* `yaw_30: 45.0, yaw_60: 45.0`.
- *Agent Action:* Confirm the delta between 30fps and 60fps simulation is < 0.001.
</verification>

<success_criteria>
- Camera follows player movements.
- Character moves forward in the direction the camera is facing.
- Holding 'F' transitions character to Block state.
</success_criteria>

## Contract Check

- PHYS-FIX-01: Task 7.1.1 syncs camera position.
- CHAR-FIX-01: Task 7.1.2 and 7.1.3 refine movement and actions.

## Consistency Diff

- Fixes gaps R54-R57 identified in Phase 6 audit.

## Blocking Checklist

- [ ] PHYS-FIX-01
- [ ] CHAR-FIX-01

<risks>
  <risk id="R90" severity="LOW" mitigation="Use lerp if jittery">
    Hard camera snapping might cause jitter; use soft lerp if needed.
  </risk>
</risks>

<notes>
This is a small polish phase before starting Phase 8.
</notes>

<output>
After completion, create `.planning/phases/07.1-physics-character-polish/07.1-01-SUMMARY.md`
</output>
