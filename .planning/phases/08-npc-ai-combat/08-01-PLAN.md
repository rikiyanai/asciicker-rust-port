---
phase: 08-npc-ai-combat
plan: 01
type: execute
wave: 1
depends_on: ["07-04"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/spatial_grid.rs
  - engine-port/src/physics/mod.rs
  - engine-port/src/physics/collision.rs
autonomous: true
requirements:
  - NPC-01
  - NPC-02

must_haves:
  truths:
    - "SpatialGrid Resource provides O(1) entity insertion and removal."
    - "Nearby entity queries check exactly 27 cells (3x3x3 neighborhood)."
    - "Ray-vs-grid walk (DDA) returns the first hit entity."
    - "BSP raycasting through static world geometry is functional."
  artifacts:
    - path: "engine-port/src/game/spatial_grid.rs"
      provides: "SpatialGrid Resource and SpatialGridCell component"
      min_lines: 100
    - path: "engine-port/src/physics/collision.rs"
      provides: "Ray-vs-BSP intersection logic"
      contains: "HitWorld"
---

**Requirements**: NPC-01, NPC-02

<objective>
Implement the SpatialGrid resource for dynamic entity indexing and the raycasting infrastructure required for NPC AI, line-of-sight, and combat.

Purpose: The SpatialGrid replaces the C++ engine's dynamic BSP rebuilding, providing high-performance proximity and raycast queries for NPCs, items, and projectiles. Raycasting through the BSP provides line-of-sight for AI and targeting.

Output: `src/game/spatial_grid.rs` (SpatialGrid + systems), updated `physics/collision.rs` with raycast logic.
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/08-npc-ai-combat/08-RESEARCH.md

Key references:
- game.cpp: lines 6120-6300 (NPC physics/AI update loop)
- world.cpp: ray intersection logic (HitWorld, HitTerrain)
- Decision D1: Two-Tier Spatial System (Static BSP + Dynamic SpatialGrid)
</context>

<tasks>
  <task id="8.1.1" title="Implement SpatialGrid Resource and Component" est="M">
    Create `engine-port/src/game/spatial_grid.rs`.
    Requirement: NPC-01.
    Implement `SpatialGrid` struct with `HashMap<(i32, i32, i32), SmallVec<[Entity; 8]>>`.
    Implement `SpatialGridCell(i32, i32, i32)` component.
    Set `cell_size` to 8.0 (matching `VISUAL_CELLS`).
  </task>
  <task id="8.1.2" title="Implement SpatialGrid Sync and Cleanup" est="S">
    Implement `sync_spatial_grid` system in `PostUpdate`.
    Requirement: NPC-01.
    Implement `cleanup_spatial_grid` using `RemovedComponents<SpatialGridCell>` to prevent dangling entity IDs in the resource.
  </task>
  <task id="8.1.3" title="Implement Proximity and Raycast Queries" est="M">
    Implement `nearby_entities(pos, radius)` query checking 3x3x3 neighborhood.
    Requirement: NPC-02.
    Implement `raycast_entities(origin, direction, max_dist)` using DDA grid walk.
    Return `Option<(Entity, f32)>` for the first hit.
  </task>
  <task id="8.1.4" title="Port BSP Raycasting (HitWorld/HitTerrain)" est="L">
    Implement `ray_vs_bsp` in `physics/collision.rs` (or `world/mod.rs`).
    Requirement: NPC-02.
    Port C++ `HitWorld` using Plucker coordinates for triangle intersection.
    Port C++ `HitTerrain` for quadtree heightmap intersection.
    Provide a unified `raycast_static(origin, direction)` API.
  </task>
</tasks>

<verification>
### Layer 4: Manual Verification (Authoritative)
**GATE 8.1.A:** SpatialGrid Integrity.
- *Action:* Spawn 3 entities at [4.0, 4.0, 4.0] and 1 at [40.0, 40.0, 40.0].
- *Requirement:* Inspect the `SpatialGrid` resource dump in the console.
- *Submission:* Copy the `SpatialGrid` cell map showing `(0,0,0) -> [E1, E2, E3]` and `(5,5,5) -> [E4]`.

### Layer 2: Integration (ECS Contract)
Run the raycast test and provide the hit data:
```bash
cargo test --test spatial_raycast_integration -- --nocapture
```
- *Input:* Ray from [0,0,0] toward [1,1,1].
- *Expected Data:* `Hit { entity: E1, distance: 6.92 }`.
- *Agent Action:* Predict the exact distance based on cell size (8.0) before running.

### Layer 1: Logic (Data-Only)
Run the grid coordinate unit tests:
```bash
cargo test spatial_grid::tests::test_world_to_grid -- --nocapture
```
- *Input:* [-1.0, -8.1, 0.0].
- *Expected Output:* `grid_pos: (-1, -2, 0)`.
- *Agent Action:* Explicitly check that negative values floor correctly (not truncate).
</verification>

<success_criteria>
- SpatialGrid Resource provides O(1) entity insertion and removal.
- Nearby entity queries check exactly 27 cells (3x3x3 neighborhood).
- Ray-vs-grid walk (DDA) returns the first hit entity.
- BSP raycasting through static world geometry is functional.
</success_criteria>

## Contract Check

- NPC-01: Task 8.1.1 and 8.1.2 implement the spatial index and sync system.
- NPC-02: Task 8.1.3 and 8.1.4 implement dynamic and static raycasting.

## Consistency Diff

- SpatialGrid uses a 3D uniform spatial hash, which deviates from C++'s dynamic BSP but preserves UX fidelity (Decision D1).
- Raycasting through BSP uses the existing RuntimeWorld data structure from Phase 5.

## Blocking Checklist

- [ ] NPC-01
- [ ] NPC-02
- [ ] O(1) performance verified by test
- [ ] 27-cell neighborhood query verified by test

<risks>
  <risk id="R66" severity="MEDIUM" mitigation="Use 3D grid with coarse Y">
    World has multi-level terrain; 2D grid is insufficient for proximity.
  </risk>
</risks>

<notes>
The SpatialGrid is critical for Phase 8 AI and Phase 9 item interaction.
</notes>

<output>
After completion, create `.planning/phases/08-npc-ai-combat/08-01-SUMMARY.md`
</output>
