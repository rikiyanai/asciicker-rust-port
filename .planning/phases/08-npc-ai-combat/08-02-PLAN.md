---
phase: 08-npc-ai-combat
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/npc.rs
  - engine-port/src/asset_loader/a3d_world.rs
autonomous: true
requirements:
  - NPC-03
  - NPC-04

must_haves:
  truths:
    - "NPCs spawn at world load from .a3d spawn point data."
    - "NpcBundle includes PhysicsIO as a Component (Decision D2)."
    - "NPC equipment is randomized using C++ probability distributions."
    - "NPCs are correctly registered in the SpatialGrid upon spawning."
  artifacts:
    - path: "engine-port/src/game/npc.rs"
      provides: "NpcBundle and spawning logic"
      min_lines: 100
---

**Requirements**: NPC-03, NPC-04

<objective>
Implement the NPC spawning system that reads EnemyGen data from world files and initializes NPCs with randomized equipment and physics.

Purpose: Populates the world with interactive AI entities. NPCs use the same physics and character logic as the player but with their own state components.

Output: `src/game/npc.rs` (NpcBundle + Spawner), updated `.a3d` world loader to populate `EnemyGenRegistry`.
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/08-npc-ai-combat/08-RESEARCH.md

Key references:
- Decision D2: NPC Physics as ECS Components
- C++ enemygen.cpp: NPC spawn system and probability logic
- C++ game.cpp: lines 3703-3845 (NPC initialization)
</context>

<tasks>
  <task id="8.2.1" title="Define NpcBundle and Components" est="S">
    Define `NpcBundle` in `game/npc.rs`.
    Requirement: NPC-03.
    Include: `Transform`, `NpcState`, `NpcEquipment`, `PhysicsIO`, `SpatialGridCell`, `Character`, `AnimationState`, `SpriteReq`, `CombatState`.
    `PhysicsIO` must be an ECS Component for NPCs (Decision D2).
  </task>
  <task id="8.2.2" title="Implement EnemyGenRegistry" est="S">
    Create `EnemyGenRegistry` Resource.
    Requirement: NPC-04.
    Stores spawn point data (position, counts, equipment weights) parsed from `.a3d`.
    Update `a3d_world.rs` to populate this registry during world load.
  </task>
  <task id="8.2.3" title="Implement NPC Spawner System" est="M">
    Implement `spawn_npcs` system.
    Requirement: NPC-04.
    Iterates `EnemyGenRegistry` and spawns up to `alive_max` entities per spawn point.
    Registers NPCs in `SpatialGrid`.
  </task>
  <task id="8.2.4" title="Port Equipment Randomization Logic" est="M">
    Port C++ 0-10 probability scale from `enemygen.cpp`.
    Requirement: NPC-04.
    `fast_rand() % 11 < value` logic for independent armor/helmet/shield rolls.
    Weighted random choice for weapon (Sword vs Crossbow).
    Apply generated `SpriteReq` to `NpcBundle`.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 8.2.A:** NPC Spawn Variety.
- *Action:* Run the game with a world containing 1 `EnemyGen` (max 7).
- *Requirement:* Observe the spawned NPCs.
- *Submission:* Provide a screenshot showing 7 NPCs with varying equipment (some with shields, some with helmets) as per the probability weights.

### Layer 2: Integration (ECS Contract)
Run the NPC composition test:
```bash
cargo test --test npc_spawning_integration -- --nocapture
```
- *Expected Output:* Component list for one NPC entity.
- *Agent Action:* Verify `PhysicsIO` is present as a Component, NOT a Resource.

### Layer 1: Logic (Data-Only)
Run the probability weight tests:
```bash
cargo test npc::spawning::tests::test_equipment_distribution -- --nocapture
```
- *Input:* eg.armor = 5 (45%). 1000 iterations.
- *Expected Output:* `count: 450 +/- 50`.
- *Agent Action:* Confirm the count falls within the statistical variance.
</verification>

<success_criteria>
- NPCs spawn automatically at world load from .a3d EnemyGen data.
- NpcBundle includes PhysicsIO as a Component (Decision D2).
- NPC equipment is randomized using C++ probability distributions.
- NPCs are correctly registered in the SpatialGrid upon spawning.
</success_criteria>

## Contract Check

- NPC-03: Task 8.2.1 defines the NPC entity archetype.
- NPC-04: Task 8.2.2, 8.2.3, and 8.2.4 implement the spawner and randomization.

## Consistency Diff

- PhysicsIO is a Component for NPCs (Decision D2) vs Resource for player (Phase 6).
- Randomized equipment rolls match C++ `fast_rand() % 11` logic exactly.

## Blocking Checklist

- [ ] NPC-03
- [ ] NPC-04
- [ ] Component Composition verified by integration test
- [ ] SpatialGrid registration verified on spawn

<risks>
  <risk id="R67" severity="LOW" mitigation="Use rand crate">
    `fast_rand` has specific distributions; must ensure `rand` crate matches behavior.
  </risk>
</risks>

<notes>
NPCs are entities with many components; refer to `game-mechanics.md` skill pack for bundle contents.
</notes>

<output>
After completion, create `.planning/phases/08-npc-ai-combat/08-02-SUMMARY.md`
</output>
