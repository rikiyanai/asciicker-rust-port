---
phase: 08-npc-ai-combat
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/combat.rs
autonomous: true
requirements:
  - NPC-07
  - NPC-08

must_haves:
  truths:
    - "Melee hit detection occurs exactly on animation frame 21."
    - "Damage and knockback impulses are applied correctly to targets."
    - "Death triggers death animation and removes entity from SpatialGrid."
    - "Respawn timer correctly re-spawns NPCs after death."
  artifacts:
    - path: "engine-port/src/game/combat.rs"
      provides: "Combat systems for hit detection and damage"
      min_lines: 120
---

**Requirements**: NPC-07, NPC-08

<objective>
Implement the melee combat system and the death/respawn lifecycle for players and NPCs.

Purpose: Completes the basic gameplay loop. Combat provides interactive feedback via damage, knockback, and death animations. Respawning ensures the world stays populated.

Output: `src/game/combat.rs` (Combat systems).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/08-npc-ai-combat/08-RESEARCH.md

Key references:
- C++ game.cpp: lines 7383-7495 (Combat hit detection)
- C++ game.cpp: Death and Respawn handling
- C++ enemygen.cpp: Respawn timer (2^n)
</context>

<tasks>
  <task id="8.4.1" title="Implement Animation-Driven Hit Detection" est="M">
    Implement `combat_hit_system`.
    Requirement: NPC-07.
    Monitors `AnimationState` for attack actions.
    Triggers a one-time hit test on frame 21 of the attack animation.
    Uses `SpatialGrid` proximity query to find targets within 3.0 units.
  </task>
  <task id="8.4.2" title="Apply Damage and Knockback" est="S">
    Apply damage to `CombatState`.
    Requirement: NPC-07.
    Calculate knockback impulse vector `(15.0 / distance)`.
    Write impulse to target's `PhysicsIO` component.
    (Optional) Trigger blood decals by modifying `RuntimeTerrain` material grid.
  </task>
  <task id="8.4.3" title="Implement Death and Despawn" est="S">
    Implement `death_system`.
    Requirement: NPC-08.
    Triggers when `HP <= 0`.
    Plays death animation (`ACTION::FALL`).
    Removes entity from `SpatialGrid` and despawns after a delay.
  </task>
  <task id="8.4.4" title="Implement NPC Respawn Timer" est="M">
    Implement `npc_respawn_system`.
    Requirement: NPC-08.
    Tracks dead NPCs from `EnemyGenRegistry`.
    Port C++ `2^revive_min` to `2^revive_max` seconds random delay.
    Spawns new NPC entity after timer expires.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 8.4.A:** Melee Hit Fidelity.
- *Action:* Trigger player attack against NPC.
- *Requirement:* The NPC must take damage exactly when the player's weapon swing reaches the bottom of the arc.
- *Submission:* A frame-by-frame log of the player's `animation_frame` vs the NPC's `HP` change.

### Layer 2: Integration (ECS Contract)
Run the death/despawn integration test:
```bash
cargo test --test npc_death_lifecycle -- --nocapture
```
- *Input:* Reduce NPC HP to 0.
- *Expected Output:* `Entity Despawned`, `SpatialGrid entry removed`.
- *Agent Action:* Confirm the entity ID is no longer present in the `SpatialGrid` resource.

### Layer 1: Logic (Data-Only)
Run the knockback math test:
```bash
cargo test combat::tests::test_knockback_impulse -- --nocapture
```
- *Input:* distance = 2.0.
- *Expected Output:* `impulse: 7.5` (15.0 / 2.0).
</verification>

<success_criteria>
- Melee combat hits connect exactly on animation frame 21.
- Damage and knockback impulses are applied correctly to targets.
- Death triggers death animation and removes entity from SpatialGrid.
- Respawn timer correctly re-spawns NPCs after death.
</success_criteria>

## Contract Check

- NPC-07: Task 8.4.1 and 8.4.2 implement hit detection and damage.
- NPC-08: Task 8.4.3 and 8.4.4 implement the death lifecycle and respawn timer.

## Consistency Diff

- Melee combat matches C++ frame-driven logic (frame 21 hit) for visual fidelity.
- Respawn timers use the same exponential backoff logic (2^n) from enemygen.cpp.

## Blocking Checklist

- [ ] NPC-07
- [ ] NPC-08
- [ ] Hit frame timing verified by test
- [ ] Respawn cycle verified by test

<risks>
  <risk id="R69" severity="LOW" mitigation="Use explicit hit_tested flag">
    Animation frame can be re-entered; must ensure hit fires only once per attack.
  </risk>
</risks>

<notes>
Melee combat is very simple in the original engine; focus on matching the animation frame timing.
</notes>

<output>
After completion, create `.planning/phases/08-npc-ai-combat/08-04-SUMMARY.md`
</output>
