---
phase: 08-npc-ai-combat
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/npc_ai.rs
autonomous: true
requirements:
  - NPC-05
  - NPC-06

must_haves:
  truths:
    - "NPCs can chase the nearest player or return to spawn points."
    - "4-phase stuck detection (reverse, go around, jump, reset) is functional."
    - "Companion (buddy) following logic works correctly."
    - "NPCs write movement forces to their local PhysicsIO component."
  artifacts:
    - path: "engine-port/src/game/npc_ai.rs"
      provides: "AI systems for target selection and navigation"
      min_lines: 150
---

**Requirements**: NPC-05, NPC-06

<objective>
Implement the core NPC AI behaviors, including target selection, path chasing, and stuck recovery logic.

Purpose: Makes NPCs "alive" and interactive. The AI drives movement and combat decisions by writing to the NPC's `PhysicsIO` component, which is then processed by the physics system.

Output: `src/game/npc_ai.rs` (AI systems).
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/08-npc-ai-combat/08-RESEARCH.md

Key references:
- C++ game.cpp: lines 7028-7331 (NPC AI logic)
- C++ game.cpp: GetNearbyCharacters proximity queries
</context>

<tasks>
  <task id="8.3.1" title="Implement AI Target Selection" est="M">
    Implement `npc_target_selection_system`.
    Requirement: NPC-05.
    Enemies find nearest player using `SpatialGrid` proximity query.
    If no player nearby, return to spawn point or idle.
    Buddies (companions) follow player at an offset.
  </task>
  <task id="8.3.2" title="Implement AI Movement Forces" est="M">
    Implement `npc_ai_movement_system`.
    Requirement: NPC-05.
    Calculates direction vector to target.
    Writes `x_force`, `y_force` to NPC's `PhysicsIO` component.
    Applies avoidance force from nearby NPCs (using `SpatialGrid`).
  </task>
  <task id="8.3.3" title="Port Stuck Detection Logic" est="L">
    Port 4-phase stuck detection from `game.cpp`.
    Requirement: NPC-06.
    Phase 1: Reverse direction.
    Phase 2: Go around (perpendicular move).
    Phase 3: Jump.
    Phase 4: Reset position to spawn if still stuck after delay.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 8.3.A:** NPC Chasing behavior.
- *Action:* Spawn a player entity and an NPC enemy.
- *Requirement:* The NPC must move toward the player until within 3.0 units.
- *Submission:* Log the NPC's `x_force` and `y_force` values while chasing.

### Layer 2: Integration (ECS Contract)
Run the AI stuck detection test:
```bash
cargo test --test npc_ai_stuck_recovery -- --nocapture
```
- *Input:* NPC blocked by static mesh for 100 ticks.
- *Expected Output:* `stuck_phase: Jump` (after Reverse and GoAround fail).
- *Agent Action:* Log the sequence of `stuck_phase` transitions.

### Layer 1: Logic (Data-Only)
Run the direction math test:
```bash
cargo test npc_ai::tests::test_target_vector -- --nocapture
```
- *Input:* NPC [0,0], Target [10, 10].
- *Expected Output:* `dir: [0.707, 0.707]`.
</verification>

<success_criteria>
- NPCs autonomously chase the nearest player or return to spawn points.
- 4-phase stuck detection (reverse, go around, jump, reset) is functional.
- Companion (buddy) following logic works correctly.
- NPCs write movement forces to their local PhysicsIO component.
</success_criteria>

## Contract Check

- NPC-05: Task 8.3.1 and 8.3.2 implement target selection and movement forces.
- NPC-06: Task 8.3.3 implements the 4-phase stuck detection logic.

## Consistency Diff

- AI movement logic is ported directly from C++ game.cpp but integrated with Bevy's SpatialGrid for performance.
- AI runs in Update/FixedUpdate before the physics simulation.

## Blocking Checklist

- [ ] NPC-05
- [ ] NPC-06
- [ ] Buddy follow logic verified by test
- [ ] PhysicsIO force write verified

<risks>
  <risk id="R68" severity="MEDIUM" mitigation="Limit query frequency">
    AI proximity queries every frame for 100+ NPCs may be expensive.
  </risk>
</risks>

<notes>
AI runs in the standard `Update` or `FixedUpdate` schedule before physics.
</notes>

<output>
After completion, create `.planning/phases/08-npc-ai-combat/08-03-SUMMARY.md`
</output>
