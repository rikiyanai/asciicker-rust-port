---
phase: 09-inventory-items
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/inventory_ui.rs
autonomous: true
requirements:
  - ITEM-02

must_haves:
  truths:
    - "Inventory uses an 8x20 grid with bitmask-based collision detection."
    - "Directional focus navigation (arrow keys) uses the C++ penalty-scoring algorithm."
    - "Inventory UI renders as an overlay on the AsciiCellGrid using Font1."
    - "Smooth scroll animation is functional during navigation."
  artifacts:
    - path: "engine-port/src/game/inventory_ui.rs"
      provides: "Inventory component and UI rendering systems"
      min_lines: 250
---

**Requirements**: ITEM-02

<objective>
Implement the grid-based inventory UI, including bitmask collision, directional navigation, and overlay rendering.

Purpose: Provides the primary user interface for managing items. The grid system matches the C++ engine's layout and logic exactly, ensuring compatibility with the original's UX.

Output: `src/game/inventory_ui.rs` (Inventory component + UI systems).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/09-inventory-items/09-RESEARCH.md

Key references:
- C++ inventory.cpp: Grid layout and bitmask collision
- C++ inventory.cpp: FocusNext algorithm (distance-weighted scoring)
- Phase 7: Font1 system for text rendering
</objective>

<tasks>
  <task id="9.2.1" title="Implement Inventory Component and Bitmask" est="M">
    Define `Inventory` Component.
    Requirement: ITEM-02.
    Include `bitmask: [u8; 20]` (160 bits for 8x20 grid).
    Include `items: Vec<OwnedItem>` where `OwnedItem` has grid pos and entity handle.
    Implement `is_slot_free(x, y, w, h)` using bitmask operations.
    Implement `mark_slots(x, y, w, h, occupied: bool)`.
  </task>
  <task id="9.2.2" title="Implement Directional Navigation (FocusNext)" est="L">
    Port `FocusNext` algorithm from `inventory.cpp`.
    Requirement: ITEM-02.
    Calculate focus point on current item's leading edge.
    Score candidates using `vx^2 + 4*vy^2` formula (4x perpendicular penalty).
    Handle major axis determination and dot product rejection.
  </task>
  <task id="9.2.3" title="Implement Inventory Rendering" est="L">
    Implement `render_inventory_system` in `PostUpdate`.
    Requirement: ITEM-02.
    Draws inventory background frame on `AsciiCellGrid`.
    Paints item icons using `Font1` (or sprite blit if appropriate).
    Renders focus highlight and item descriptions.
    Implements `smooth_scroll` interpolation (±1 per frame).
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 9.2.A:** Inventory Grid UX.
- *Action:* Run the game, open the inventory, move focus between items using arrow keys.
- *Requirement:* The focus highlight must move correctly, and the scroll position must follow the focus.
- *Submission:* A screenshot of the inventory showing a non-1x1 item (e.g., a 3x2 sword) correctly highlighted.

### Layer 2: Integration (ECS Contract)
Run the navigation scoring test:
```bash
cargo test --test inventory_navigation_integration -- --nocapture
```
- *Input:* Current focus at (0,0), move RIGHT. Item A at (2,0), Item B at (2,5).
- *Expected Output:* `Focus changed to: Item A`.
- *Agent Action:* Log the scores calculated for Item A vs Item B.

### Layer 1: Logic (Data-Only)
Run the bitmask collision unit tests:
```bash
cargo test inventory_ui::tests::test_bitmask_collision -- --nocapture
```
- *Input:* Insert 2x2 item at (0,0). Test slot (1,1).
- *Expected Output:* `slot_free: false`.
- *Agent Action:* Print the 160-bit bitmask as a hex string for verification.
</verification>

<success_criteria>
- Inventory uses an 8x20 grid with bitmask-based collision detection.
- Directional focus navigation (arrow keys) uses the C++ penalty-scoring algorithm.
- Inventory UI renders as an overlay on the AsciiCellGrid using Font1.
- Smooth scroll animation is functional during navigation.
</success_criteria>

## Contract Check

- ITEM-02: Task 9.2.1, 9.2.2, and 9.2.3 implement the grid logic and UI rendering.

## Consistency Diff

- Bitmask collision logic is ported exactly from inventory.cpp to maintain O(1) performance.
- FocusNext uses the identical distance-weighted formula to preserve original UX "feel".

## Blocking Checklist

- [ ] ITEM-02
- [ ] Bitmask collision verified by test
- [ ] Navigation scoring verified by test
- [ ] Smooth scroll ±1 behavior verified

<risks>
  <risk id="R71" severity="MEDIUM" mitigation="Use explicit layout constants">
    Inventory layout must match CP437 character boundaries for correct alignment.
  </risk>
</risks>

<notes>
The FocusNext algorithm is subtle; follow the C++ logic exactly to preserve the feel.
</notes>

<output>
After completion, create `.planning/phases/09-inventory-items/09-02-SUMMARY.md`
</output>
