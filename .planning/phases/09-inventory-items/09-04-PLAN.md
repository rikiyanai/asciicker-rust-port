---
phase: 09-inventory-items
plan: 04
type: execute
wave: 4
depends_on: ["09-03", "06-02"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/equipment.rs
autonomous: true
requirements:
  - ITEM-04

must_haves:
  truths:
    - "Equipping items updates the character's 5D sprite lookup (SpriteReq)."
    - "Only one item per equipment slot (Weapon, Shield, Helmet, Armor) can be active."
    - "Unequipping an item resets the corresponding SpriteReq index."
    - "Item consumption triggers the correct animation state."
  artifacts:
    - path: "engine-port/src/game/equipment.rs"
      provides: "Systems for managing equipment slots and visual updates"
      min_lines: 100
---

**Requirements**: ITEM-04

<objective>
Implement the equipment lifecycle, connecting inventory items to character visual states and animations.

Purpose: Handles the "Equip" action, which modifies the character's appearance by updating the `SpriteReq` component. This connects the data-driven items to the 5D sprite lookup system established in Phase 6.

Output: `src/game/equipment.rs` (Equipment systems).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/09-inventory-items/09-RESEARCH.md

Key references:
- Phase 6: Character state machine and 5D equipment lookup
- C++ game.cpp: Equipment slot update functions (SetWeapon, SetShield, etc.)
- C++ inventory.h: PLAYER_WEAPON_INDEX enums
</context>

<tasks>
  <task id="9.4.1" title="Implement Equip/Unequip Systems" est="M">
    Implement `item_equip_system`.
    Requirement: ITEM-04.
    Identifies item slots ('W', 'S', 'H', 'A').
    Updates `SpriteReq` component on the player entity.
    Handles mutually exclusive slots (e.g., swapping a sword for a crossbow).
    Marks item as `in_use` in the `Inventory` component.
  </task>
  <task id="9.4.2" title="Update Visual Sprite Lookup" est="S">
    Ensures that changes to `SpriteReq` trigger immediate sprite handle updates via `query_character_sprites` (from Phase 6).
    Requirement: ITEM-04.
    Validates that NPC equipment (set in Phase 8) also uses this system.
  </task>
  <task id="9.4.3" title="Integrate Consume Animations" est="S">
    When an item is used (e.g., potion), trigger a temporary 'Use' animation state.
    Requirement: ITEM-04.
    Coordinate with `AnimationState` to play the appropriate frame sequence.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 9.4.A:** Equipment Visual Update.
- *Action:* Equip a Sword and a Helmet.
- *Requirement:* The character's CP437 representation must change to reflect the new equipment.
- *Submission:* Screenshot of the character with equipment vs without.

### Layer 2: Integration (ECS Contract)
Run the SpriteReq update test:
```bash
cargo test --test equipment_sprite_sync -- --nocapture
```
- *Input:* Equip Weapon (index 2).
- *Expected Output:* `SpriteReq.weapon: 2`.
- *Agent Action:* Verify the 5D lookup table indices match the C++ enum values.

### Layer 1: Logic (Data-Only)
Run the slot exclusivity test:
```bash
cargo test equipment::tests::test_slot_swapping -- --nocapture
```
- *Input:* Weapon A equipped. Equip Weapon B.
- *Expected Output:* `Weapon A unequipped: true`, `Weapon B equipped: true`.
</verification>

<success_criteria>
- Equipping items updates the character's 5D sprite lookup (SpriteReq).
- Only one item per equipment slot (Weapon, Shield, Helmet, Armor) can be active.
- Unequipping an item resets the corresponding SpriteReq index.
- Item consumption triggers the correct animation state.
</success_criteria>

## Contract Check

- ITEM-04: Task 9.4.1, 9.4.2, and 9.4.3 implement equip logic and visual synchronization.

## Consistency Diff

- SpriteReq updates are integrated with the existing Phase 6 visual pipeline.
- NPC equipment uses the identical logic path as player equipment.

## Blocking Checklist

- [ ] ITEM-04
- [ ] Visual update verified by integration test
- [ ] Mutually exclusive slot logic verified
- [ ] Consume animation trigger verified

<risks>
  <risk id="R73" severity="LOW" mitigation="Validate indices">
    Invalid item sub_kind could lead to out-of-bounds 5D array access; must validate against `SPRITE_LOOKUP` bounds.
  </risk>
</risks>

<notes>
NPCs and Players share the same equipment visual logic; ensure NPC spawning correctly sets initial SpriteReq.
</notes>

<output>
After completion, create `.planning/phases/09-inventory-items/09-04-SUMMARY.md`
</output>
