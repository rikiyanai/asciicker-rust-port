---
phase: 07-game-systems
plan: 01
type: execute
wave: 1
depends_on: []
# Phase 5 and 6 must be complete before Phase 7 starts (ROADMAP ordering)
# R14-F127 FIX: Phase 5 and 6 must be FULLY complete before any Phase 7 plan.
# ROADMAP ordering enforces this — do NOT execute Phase 7 plans out of order.
files_modified:
  - engine-port/Cargo.toml
  - engine-port/src/audio/mod.rs
  - engine-port/src/audio/mixer.rs
  - engine-port/src/lib.rs
  - engine-port/src/main.rs
autonomous: true
requirements:
  - AUD-01
  - AUD-02

must_haves:
  truths:
    - "AudioPlugin initializes without error and bevy_kira_audio AudioPlugin is active"
    - "16 named dynamic audio channels exist (track_0 through track_15)"
    - "A sound effect can be played on a specific track with volume control"
    - "Round-robin track assignment cycles through all 16 tracks"
  artifacts:
    - path: "engine-port/src/audio/mod.rs"
      provides: "AudioPlugin with bevy_kira_audio setup and DynamicAudioChannels"
      min_lines: 30
    - path: "engine-port/src/audio/mixer.rs"
      provides: "AudioMixer resource with 16-track round-robin, volume control, play_sound API"
      min_lines: 60
  key_links:
    - from: "engine-port/src/audio/mod.rs"
      to: "bevy_kira_audio::AudioPlugin"
      via: "app.add_plugins(AudioPlugin)"
      pattern: "add_plugins.*AudioPlugin"
    - from: "engine-port/src/audio/mixer.rs"
      to: "bevy_kira_audio::DynamicAudioChannels"
      via: "Res<DynamicAudioChannels> in play systems"
      pattern: "DynamicAudioChannels"
    - from: "engine-port/src/main.rs"
      to: "engine-port/src/audio/mod.rs"
      via: "app.add_plugins(AsciickerAudioPlugin)"
      pattern: "AsciickerAudioPlugin"
---

<objective>
Implement the audio subsystem with bevy_kira_audio 0.25 integration and a 16-track dynamic channel mixer matching the C++ engine's PlyTrack architecture.

Purpose: Sound effects are essential for gameplay feedback. The 16-track mixer maps to the C++ engine's round-robin `PlyTrack[16]` array, enabling concurrent sound playback without channel starvation.

Output: `src/audio/mod.rs` (AudioPlugin), `src/audio/mixer.rs` (AudioMixer resource with play/stop/volume), updated Cargo.toml with bevy_kira_audio dependency.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
<!-- L1-AUDIT-FIX: Verify these external workflow files exist before Phase 7 execution.
     If missing, autonomous execution may fail or proceed without intended workflow constraints.
     Pre-check: ls /Users/r/.claude/get-shit-done/workflows/execute-plan.md
                ls /Users/r/.claude/get-shit-done/templates/summary.md -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-game-systems/07-RESEARCH.md

Key references:
- bevy_kira_audio 0.25 API: DynamicAudioChannels, AudioChannel, Volume
- C++ audio.cpp: PlyTrack[16] round-robin mixer with CallAudio queue
- STATE.md blocker: "bevy_kira_audio must be 0.25 (not 0.24) for Bevy 0.18 compatibility"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bevy_kira_audio dependency and create AudioPlugin with DynamicAudioChannels</name>
  <files>
    engine-port/Cargo.toml
    engine-port/src/audio/mod.rs
    engine-port/src/lib.rs
    engine-port/src/main.rs
  </files>
  <action>
1. Add `bevy_kira_audio = "0.25"` to `[dependencies]` in Cargo.toml. Pin exactly 0.25 (Bevy 0.18 compatible per research).
   **P7-063 FIX:** `bevy_kira_audio = "0.25"` allows compatible 0.25.x patches (correct). For a
   true exact pin, use `"=0.25.0"`. The `"0.25"` form is appropriate and is the recommended form.
   **P7-044 FIX:** bevy_kira_audio 0.25 uses `Volume::Amplitude(f64)`, NOT `Volume::new(f64)`.
   All volume API calls throughout this plan must use `Volume::Amplitude(value as f64)`.
   **P7-049 FIX:** Verify `DefaultPlugins` feature set does NOT include `bevy_audio`. Current
   Cargo.toml features (`2d_api`, `bevy_render`, `bevy_core_pipeline`, `bevy_shader`,
   `default_app`, `default_platform`) do not include `bevy_audio` — should be safe. If
   `bevy_audio` is ever added, use `.build().disable::<AudioPlugin>()`.
   **R6-M02 FIX:** Add active verification: `grep "bevy_audio" engine-port/Cargo.toml` as part of Task 1 verify block. The hardcoded feature list (2d_api, bevy_render, bevy_core_pipeline, bevy_shader, default_app, default_platform) may be stale if Phases 5 or 6 added features.

2. Create `engine-port/src/audio/mod.rs`:
   - `pub mod mixer;`
   - Define `pub struct AsciickerAudioPlugin;` (name avoids conflict with bevy_kira_audio's AudioPlugin)
   - `impl Plugin for AsciickerAudioPlugin`:
     - Add `bevy_kira_audio::AudioPlugin` as a sub-plugin via `app.add_plugins(bevy_kira_audio::AudioPlugin)`
     - Init AudioMixer resource: `app.init_resource::<mixer::AudioMixer>()`
     - Add PlaySoundEvent as event: `app.add_event::<mixer::PlaySoundEvent>()`
     **P7-101 FIX (CRITICAL):** `add_event` does not exist in Bevy 0.18. The correct API is
     `app.add_message::<mixer::PlaySoundEvent>()`. Using `add_event` will produce a compile error.
     The codebase already uses `add_message` (confirmed in engine-port/src/output/mod.rs:
     `app.add_message::<WindowResized>()`).
     **XP-120 FIX:** Verify Bevy 0.18 event API: run `cargo doc --open` and confirm `add_message`
     is the correct method. The full rename is: `add_event` → `add_message`,
     `EventReader` → `MessageReader`, `EventWriter` → `MessageWriter`.
     - Register play_sound_system in Update schedule
   - Re-export key types for convenience

3. Add `pub mod audio;` to `engine-port/src/lib.rs` (must be added alongside existing module declarations).

4. In `engine-port/src/main.rs`, add `AsciickerAudioPlugin` alongside existing plugins.

**R13-018 FIX:** If bevy_kira_audio 0.25 does not exist for Bevy 0.18:
   Fallback 1: Try latest bevy_kira_audio compatible with Bevy 0.18
   Fallback 2: Try bevy_seedling or bevy_audio with kira backend
   Fallback 3: Implement minimal audio wrapper around kira 0.8 directly

IMPORTANT: Use `bevy_kira_audio = "0.25"` exactly. Version 0.24 is Bevy 0.17 only and will NOT compile.
  </action>
  <verify>
`cargo build` succeeds with no errors. `cargo test --lib` passes. The audio module compiles and the plugin can be instantiated.
  </verify>
  <done>
bevy_kira_audio 0.25 is a dependency, AsciickerAudioPlugin exists and compiles, lib.rs exports the audio module.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement AudioMixer resource with 16-track round-robin and play/stop/volume API</name>
  <files>
    engine-port/src/audio/mixer.rs
  </files>
  <action>
Create `engine-port/src/audio/mixer.rs` implementing the 16-track mixer:

1. Define constants:
   - `const PLY_TRACKS: usize = 16;`
   - Channel names: "track_0" through "track_15"

2. Define `AudioMixer` resource:
   - master_volume: f32 (0.0-1.0), track_round_robin: usize, track_volumes: [f32; PLY_TRACKS]
   - Implement `Default` with master_volume=1.0, track_round_robin=0, all track_volumes=1.0

3. Define `PlaySoundEvent` as a Bevy Message (NOT "Bevy Event"):
   **P7-216 FIX (RESOLVED):** Use `#[derive(Message)]` (from `bevy::ecs::message::Message`). In Bevy 0.18, `#[derive(Event)]` implements a DIFFERENT trait for the Observer/trigger system and is incompatible with `add_message`/`MessageReader`. `#[derive(Message)]` is available and confirmed in bevy_ecs_macros 0.18.0. No conditional verification needed.
   - asset_path: String, volume: Option f32, track: Option usize

4. Implement `play_sound_system`:
   - Takes MessageReader PlaySoundEvent, Res DynamicAudioChannels, ResMut AudioMixer, Res AssetServer
   **R7-HIGH-002 FIX:** Step 4 description updated from "EventReader" to "MessageReader" to match
   the P7-102 FIX below. The old text said "EventReader" before the fix corrected it, creating
   contradictory instructions for an executor reading sequentially.
   **P7-102 FIX (CRITICAL):** `EventReader` does not exist in Bevy 0.18. The system parameter
   must be `MessageReader<PlaySoundEvent>`, NOT `EventReader<PlaySoundEvent>`. Using `EventReader`
   will not compile against Bevy 0.18. The full corrected signature:
   ```rust
   fn play_sound_system(
       mut sound_events: MessageReader<PlaySoundEvent>,
       dynamic_channels: Res<DynamicAudioChannels>,
       mut mixer: ResMut<AudioMixer>,
       asset_server: Res<AssetServer>,
   )
   ```
   All references to `EventReader`/`EventWriter` in this plan must use `MessageReader`/`MessageWriter`.
   - For each event: determine track (event.track or round-robin), create channel name, load asset, play with volume
   - Advance round-robin counter after each play
   **P7-055 FIX:** Events accumulate if `play_sound_system` runs during non-Playing states. Do
   NOT gate with `in_state(Playing)` here — GameState may not be initialized when
   AsciickerAudioPlugin registers. Instead: either drain events unconditionally (play nothing if
   not Playing), or ensure GamePlugin is registered BEFORE AsciickerAudioPlugin in main.rs.
   **P7-121 INFO:** See Plan 07-02 which creates GamePlugin. After both 07-01 and 07-02 complete,
   verify that main.rs registers GamePlugin BEFORE AsciickerAudioPlugin to avoid PlaySoundEvent
   accumulation outside Playing state.
   **R13-020 FIX:** P7-121 is post-hoc verification only, NOT a registration requirement. AsciickerAudioPlugin MUST unconditionally drain events (per P7-055 FIX) regardless of plugin ordering. P7-121 downgraded to informational.

5. Implement AudioMixer methods:
   - set_master_volume, set_track_volume, next_track, effective_volume

6. Unit tests: round-robin cycles 0..15 and wraps, volume clamping, effective_volume multiplication, Default init
   **R13-019 FIX:** Add integration test verifying all 16 DynamicAudioChannels can be created and addressed without channel starvation.
   **R16-F203 FIX:** Setup: send 16 PlaySoundEvents in a single frame via MessageWriter. Run App::update(). Assert all 16 DynamicAudioChannels have active playback handles. Send 17th event → assert channel 0 is reused (round-robin wraps).
  </action>
  <verify>
`cargo test --lib` passes all AudioMixer unit tests. `cargo clippy -- -D warnings` clean.
  </verify>
  <done>
AudioMixer resource exists with 16-track round-robin, per-track and master volume, PlaySoundEvent for ECS playback, and unit tests passing.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds with bevy_kira_audio 0.25 dependency
2. `cargo test --lib` passes all AudioMixer tests
3. `cargo clippy -- -D warnings` clean
4. AudioPlugin registers in the Bevy app without panics
5. PlaySoundEvent is registered via `app.add_message::<PlaySoundEvent>()` (Bevy 0.18 API)

**R16-F202 FIX (CRITICAL):** Manual QA Gate — Audio Verification:
1. Run game, trigger a PlaySoundEvent → verify audio output from speakers
2. Set master volume to 0.5, verify audibly quieter than 1.0
3. Fire 17 rapid sound events → verify round-robin handles gracefully (no channel starvation, no silence)
4. Mute master (0.0) → verify complete silence
Log results in FAILURE_LOG.md.

**R14-PG01 FIX:** After successful execution, update `.planning/ROADMAP.md`:
mark this plan `[x]`, increment the "Plans Complete" counter for this phase.
</verification>

<success_criteria>
- bevy_kira_audio 0.25 compiles with Bevy 0.18
- 16 dynamic audio channels available via DynamicAudioChannels
- Round-robin track selection verified by unit test
- Volume control (master + per-track) verified by unit test
- PlaySoundEvent provides ECS-idiomatic sound playback API
</success_criteria>

## Contract Check
- AUD-01 (bevy_kira_audio integration): Task 1 adds bevy_kira_audio 0.25 and creates AsciickerAudioPlugin
- AUD-02 (16-track mixer): Task 2 implements AudioMixer with 16 DynamicAudioChannels and round-robin

## Consistency Diff
- Cargo.toml is also modified by Plans 03, 04, 05 (adding their own deps). No conflict: each adds different crate entries.
- lib.rs is also modified by Plan 03 (adding network module). No conflict: each adds a different `pub mod` line.
- main.rs may need updates from Plan 02 (GamePlugin). No conflict: each adds a different plugin.
  **M4-AUDIT-FIX:** Plans 07-01 and 07-02 are BOTH wave 1 with no mutual dependency and both
  modify `main.rs` (07-01 adds `AsciickerAudioPlugin`, 07-02 adds `GamePlugin`). These additions
  are independent lines (different plugins) and can be merged automatically by git. However, if
  both plans execute truly in parallel, the second to commit will see a merge conflict on main.rs.
  Recommendation: execute 07-01 and 07-02 sequentially (either order) to avoid main.rs conflicts,
  or accept that manual merge resolution may be needed.
- No dependency on prior Phase 7 plans. Wave 1, fully independent.
- **XP-007 FIX:** Plans 07-01 and 07-03 both modify `lib.rs`, `main.rs`, and `Cargo.toml` and are both wave 1. They MUST execute sequentially (not in parallel) to avoid merge conflicts, even though they have no logical dependency.
- **P7-065 FIX (XP-007 addendum):** NOTE: Plan 07-04 also modifies Cargo.toml (adding kiddo, lru). All wave-1 plans modifying Cargo.toml (07-01, 07-03, 07-04) must execute sequentially.

## Blocking Checklist
- [x] AUD-01 covered (Task 1: bevy_kira_audio plugin)
- [x] AUD-02 covered (Task 2: 16-track mixer)
- [x] bevy_kira_audio version pinned to 0.25 (not 0.24)
- [x] No file overlap with Plans 02-05 (except Cargo.toml and lib.rs which add non-conflicting lines)
- [x] DynamicAudioChannels used (not 16 typed channels, per Pitfall 7)

<output>
After completion, create `.planning/phases/07-game-systems/07-01-SUMMARY.md`
</output>
