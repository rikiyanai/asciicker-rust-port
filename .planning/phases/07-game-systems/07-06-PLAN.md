---
phase: 07-game-systems
plan: 06
type: execute
wave: 5
depends_on:
  - "07-05"
files_modified:
  - engine-port/src/game/weather.rs
  - engine-port/src/game/mod.rs
autonomous: true
gap_closure: true
requirements:
  - GAME-03

must_haves:
  truths:
    - "Pressing F5 during gameplay cycles WeatherState: Clear -> LightSnow -> HeavySnow -> Blizzard -> Clear"
    - "Weather particles become visible on screen after pressing F5 once (transitions to LightSnow)"
  artifacts:
    - path: "engine-port/src/game/weather.rs"
      provides: "cycle_weather_debug_system that reads F5 keypress and cycles WeatherState"
      contains: "KeyCode::F5"
  key_links:
    - from: "engine-port/src/game/weather.rs"
      to: "engine-port/src/game/weather.rs"
      via: "cycle_weather_debug_system calls set_weather_state"
      pattern: "set_weather_state"
    - from: "engine-port/src/game/mod.rs"
      to: "engine-port/src/game/weather.rs"
      via: "GamePlugin registers cycle_weather_debug_system in Update with run_if Playing"
      pattern: "cycle_weather_debug_system"
---

<objective>
Add a debug keybind (F5) to cycle weather states so weather particles are actually triggerable at runtime.

Purpose: The verification found that WeatherState starts at Clear (spawn rate 0) and no system ever calls set_weather_state(). Running the game produces zero weather particles. This gap closure adds the minimal trigger needed to exercise GAME-03.

Output: Updated `src/game/weather.rs` with debug cycle system, updated `src/game/mod.rs` to register it.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-game-systems/07-VERIFICATION.md
@.planning/phases/07-game-systems/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add F5 debug weather cycle system and register in GamePlugin</name>
  <files>
    engine-port/src/game/weather.rs
    engine-port/src/game/mod.rs
  </files>
  <action>
1. Add a `cycle_weather_debug_system` to `engine-port/src/game/weather.rs`:
   - System signature: `pub fn cycle_weather_debug_system(keys: Res<ButtonInput<KeyCode>>, mut weather: ResMut<Weather>)`
   - On `keys.just_pressed(KeyCode::F5)`: cycle to next state
   - Cycle order: Clear -> LightSnow -> HeavySnow -> Blizzard -> Clear (wrap around)
   - Use a match on `weather.state` to determine next state
   - Call `set_weather_state(&mut weather, next_state)` which already exists and handles target_intensity
   - Log the transition at info! level: `"Weather debug: {old:?} -> {new:?}"`
   - Add code comment: `// Debug keybind — not a gameplay feature. Remove or gate behind cfg(debug_assertions) for release.`

2. Register the system in `engine-port/src/game/mod.rs`:
   - In the GamePlugin::build fn, add the system to the Update schedule, gated on `in_state(GameState::Playing)`
   - Place it alongside or after the existing `weather::weather_update_system` registration
   - Use: `weather::cycle_weather_debug_system.run_if(in_state(GameState::Playing))`

3. Add a unit test in weather.rs `#[cfg(test)] mod tests`:
   - `test_cycle_weather_cycles_all_states`: Create a Weather resource, manually call cycle logic for each transition, assert the full cycle: Clear -> LightSnow -> HeavySnow -> Blizzard -> Clear
  </action>
  <verify>
`cargo build` succeeds. `cargo test --lib weather` passes including the new cycle test. `cargo clippy -- -D warnings` clean on weather.rs and mod.rs.
  </verify>
  <done>
F5 key cycles weather states during gameplay. Weather particles become visible after first press (LightSnow, spawn rate 10/sec). Full cycle verified by unit test.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds
2. `cargo test --lib weather` passes all tests including new cycle test
3. `cargo clippy -- -D warnings` clean
4. `grep 'cycle_weather_debug_system' engine-port/src/game/mod.rs` shows system registered in Update with Playing state gate
5. `grep 'KeyCode::F5' engine-port/src/game/weather.rs` confirms keybind wired

**R14-PG01 FIX:** After successful execution, update `.planning/ROADMAP.md`:
add 07-06-PLAN.md to the plans list, update plan count.
</verification>

<success_criteria>
- F5 keypress during Playing state cycles WeatherState through all 4 variants
- set_weather_state is called (reusing existing API, no new state logic)
- Unit test covers full cycle
- Weather particles will now appear after pressing F5 at runtime
</success_criteria>

## Contract Check
- GAME-03 (weather effects): Gap closure — adds the missing runtime trigger so weather particles can actually be observed

<output>
After completion, create `.planning/phases/07-game-systems/07-06-SUMMARY.md`
</output>
