---
phase: 07-game-systems
plan: 02
type: execute
wave: 1
depends_on: []
# **P7-203 FIX (CRITICAL):** `depends_on: []` does NOT mean Phase 5 is irrelevant.
# `render/pipeline.rs` from Phase 5 Plan 05-05 is an IMPLICIT prerequisite for Option (a)
# of the render_pipeline_system state-gating (P7-040/P7-113 FIX in Task 2 below).
# If Phase 5 is not complete, executor MUST use Option (b) (move registration to GamePlugin)
# and document the deferral. Do NOT silently fall back without notifying the orchestrator.
# **XP-201 FIX (LOW):** Logical write dependency: if Phase 6 HAS been executed, game/mod.rs
# was modified by Plan 06-03. Running 07-02 concurrently with 06-03 causes write conflicts.
# If orchestrating in parallel, ensure 06-03 completes before 07-02 runs.
# **XP-208 FIX (MEDIUM):** pipeline.rs three-way ownership: created by 05-05, modified by 06-03
# (adds Stage 5 REFLECTION), modified again here (adds .run_if state-gating). Before modifying
# pipeline.rs in this plan, verify Stage 5 REFLECTION call is present (i.e., Plan 06-03 has
# executed). If Stage 5 is still a comment stub, defer pipeline.rs modification until after
# Phase 6 completes — the P7-040 Option (b) fallback applies in that case too.
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/state.rs
  - engine-port/src/game/menu.rs
  - engine-port/src/render/pipeline.rs
autonomous: true
requirements:
  - GAME-01
  - GAME-02

must_haves:
  truths:
    - "Game starts in MainMenu state by default"
    - "Transitioning to Loading state triggers asset loading systems"
    - "Transitioning to Playing state enables gameplay systems"
    - "Pause toggle switches between Playing and Paused states"
    - "Main menu has navigable items (Start Game, Quit)"
  artifacts:
    - path: "engine-port/src/game/state.rs"
      provides: "GameState enum with MainMenu/Loading/Playing/Paused states and transition systems"
      min_lines: 40
    - path: "engine-port/src/game/menu.rs"
      provides: "MainMenu resource with item definitions and navigation logic"
      min_lines: 50
    - path: "engine-port/src/game/mod.rs"
      provides: "GamePlugin registering states, menu systems, and transition handlers"
      min_lines: 30
  key_links:
    - from: "engine-port/src/game/state.rs"
      to: "bevy::state::States"
      via: "#[derive(States)] on GameState"
      pattern: "derive.*States"
    - from: "engine-port/src/game/mod.rs"
      to: "engine-port/src/game/state.rs"
      via: "app.init_state::<GameState>()"
      pattern: "init_state.*GameState"
    - from: "engine-port/src/game/menu.rs"
      to: "engine-port/src/game/state.rs"
      via: "NextState<GameState> for menu item activation"
      pattern: "NextState.*GameState"
---

<objective>
Implement the game state machine (Loading/Playing/Paused) and main menu system using Bevy's native States, matching the C++ mainmenu.cpp loading FSM.

Purpose: The game state machine controls flow from startup (menu) through loading to gameplay, with pause support. This is the orchestration backbone that all other systems (audio, weather, rendering) gate on.

Output: `src/game/state.rs` (GameState enum, transitions), `src/game/menu.rs` (MainMenu resource, navigation), updated `src/game/mod.rs` (GamePlugin wiring).
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-game-systems/07-RESEARCH.md

Key references:
- Bevy States derive macro with OnEnter/OnExit schedules
- C++ mainmenu.cpp: MainMenu struct, loading FSM (3 stages), Paint/Scale
- Research Pattern 1: Bevy Game State Machine
- Research: Game State Loading Flow code example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GameState enum with Bevy States and transition systems</name>
  <files>
    engine-port/src/game/state.rs
  </files>
  <action>
Create `engine-port/src/game/state.rs`:

1. Define GameState enum with Bevy States derive:
   - States: MainMenu (default), Loading, Playing, Paused
   - All required derives: States, Clone, Copy, PartialEq, Eq, Hash, Debug, Default

2. Define LoadingProgress resource (maps to C++ loading FSM stages):
   - stage: u8 (3=init, 2=terrain_patches, 1=world_rebuild, 0=done)
   - items_loaded: u32, items_total: u32
   - Implement Default with stage=3, items=0

3. Implement transition systems:
   - on_enter_loading: insert LoadingProgress resource
   - check_loading_complete: transition to Playing when stage==0
     **P7-054 FIX:** LoadingProgress.stage is decremented by Phase 5 asset loading systems (not
     this plan). Without Phase 5 integration, the game will never leave Loading state. For testing:
     add a dev-only system that sets stage=0 immediately, gated by `#[cfg(debug_assertions)]` or
     a feature flag.
     **XP-127 FIX (LOW):** No Phase 5 plan specifies how to decrement LoadingProgress.stage, because
     LoadingProgress does not exist until this plan creates it. To bridge the gap, add an explicit
     system `advance_loading_progress_system` that:
       1. Reads `AssemblyState` resource from Plan 05-05
       2. When `assembly.assembled == true`, sets `LoadingProgress.stage = 0`
     This uses AssemblyState.assembled as the loading-complete signal instead of requiring Phase 5
     to know about LoadingProgress (which it cannot, since Phase 7 creates it). Register this system
     alongside check_loading_complete in the Loading state.
   - on_enter_playing: log entering gameplay
   - toggle_pause: Escape key toggles Playing to Paused and back
     **P7-050 FIX:** System must read current state to toggle correctly:
     ```rust
     fn toggle_pause(
         state: Res<State<GameState>>,
         mut next_state: ResMut<NextState<GameState>>,
         keys: Res<ButtonInput<KeyCode>>,
     ) {
         if keys.just_pressed(KeyCode::Escape) {
             match state.get() {
                 GameState::Playing => next_state.set(GameState::Paused),
                 GameState::Paused => next_state.set(GameState::Playing),
                 _ => {}
             }
         }
     }
     ```
   - on_exit_loading: remove LoadingProgress

4. Unit tests:
   - GameState default is MainMenu
   - LoadingProgress default values correct
   - LoadingProgress stage decrement logic
  </action>
  <verify>
`cargo test --lib` passes all GameState tests. `cargo clippy -- -D warnings` clean.
  </verify>
  <done>
GameState enum with 4 states exists, LoadingProgress tracks loading FSM, transition systems handle state flow, pause toggle on Escape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement MainMenu resource and navigation, wire up GamePlugin</name>
  <files>
    engine-port/src/game/menu.rs
    engine-port/src/game/mod.rs
  </files>
  <action>
1. Create `engine-port/src/game/menu.rs`:

   Define types: MenuAction (StartGame, Quit), MenuItem (label + action), MainMenu resource (items Vec + selected_index).
   Default: two items "Start Game" and "Quit".

   Systems:
   - menu_navigation: Up/Down arrows change selected_index with wrap
   - menu_activate: Enter key activates selected item (StartGame -> Loading, Quit -> AppExit)
     **P7-103 FIX (CRITICAL):** AppExit must be sent via `MessageWriter<AppExit>` in Bevy 0.18,
     NOT `EventWriter<AppExit>` (removed). The correct system parameter and call:
     ```rust
     fn menu_activate(
         mut next_state: ResMut<NextState<GameState>>,
         mut app_exit: MessageWriter<AppExit>,
         // ... other params
     ) {
         // For Quit action:
         app_exit.write(AppExit::default());
     }
     ```
     Add a note: "AppExit requires `MessageWriter<AppExit>` in Bevy 0.18 — NOT `EventWriter<AppExit>`."
   - render_menu: writes menu text to AsciiCellGrid (title "ASCIICKER" centered, items below, highlight selected). Guard AsciiCellGrid access with Option for test environments.
     **P7-038 FIX:** `render_menu` text will be OVERWRITTEN if `render_pipeline_system` (Phase 5)
     runs in the same frame. The render pipeline clears and rebuilds AsciiCellGrid every frame.
     Fix: gate `render_pipeline_system` behind `.run_if(in_state(GameState::Playing))` so it
     does NOT run during MainMenu state. This lets `render_menu` own the grid during MainMenu.
     The P7-014 FIX (state-gating Phase 6 systems) must ALSO apply to render_pipeline_system.
     **P7-040 FIX:** PRECONDITION: `render_pipeline_system` is created by Phase 5 Plan 05-05 in
     `render/pipeline.rs`. It is registered by `CpuRasterizerPlugin`, NOT `GamePlugin`. To gate
     it with `.run_if(in_state(GameState::Playing))`, either: (a) modify `render/pipeline.rs`
     (add to `files_modified`), or (b) move the system registration to `GamePlugin` in
     `game/mod.rs`. Option (a) is preferred — add `engine-port/src/render/pipeline.rs` to
     this plan's `files_modified`. This file has been added to the frontmatter `files_modified`
     list above.
     **P7-113 FIX (MEDIUM):** PRECONDITION: `render/pipeline.rs` is created by Phase 5 Plan 05-05.
     If Phase 5 has NOT been executed, this file does not exist and cannot be modified. In that case,
     use Option (b): move the system registration to GamePlugin in `game/mod.rs` directly when
     pipeline.rs is created. Defer the state-gating of render_pipeline_system until Phase 5 completes.
     Note: plan 07-02 frontmatter lists `depends_on: []` — add a reminder that Phase 5 Plan 05-05
     is an implicit prerequisite for option (a).

   **P7-046 FIX:** Use `Option<ResMut<AsciiCellGrid>>` as system parameter (grid not present in
   MinimalPlugins test env). Write colors as direct RGBA `[u8;4]` arrays (e.g., `[255,255,255,255]`
   for white), NOT xterm-256 palette indices. `char_index` is the CP437 glyph value cast to `u16`.
   **P7-118 FIX (LOW):** The AsciiCellGrid field is `char_indices` (plural), not `char_index`.
   Write to `grid.char_indices[idx] = glyph_value as u16` where `idx = row * width + col`.
   Using `grid.char_index[...]` will fail to compile.
   **P7-207 FIX (HIGH):** `render_menu` uses `idx = row * width + col` indexing into `char_indices`.
   The `row`, `col`, and `width` variables must use consistent types. `AsciiCellGrid::set_cell`
   takes `x: u32, y: u32`. When using direct index arithmetic (`row * width + col`) instead of
   `set_cell`, compute `idx` as `usize` for slice indexing. If using `set_cell` for the font
   paint functions (Task 2 in 07-04), declare `x: u32, y: u32` parameters directly to match
   `AsciiCellGrid::set_cell(x: u32, y: u32, ...)`. Using `i32` or `usize` for coordinates
   requires explicit cast `x as u32` before calling `set_cell`. Mismatched types are a compile
   error; silent `usize`-to-`u32` truncation at negative values would be a runtime bug.
   **P7-064 FIX:** Full import: `use crate::output::ascii_cell_grid::AsciiCellGrid;`
   (output/mod.rs does not re-export at module root).

   Tests: navigation wraps, default has 2 items, selected starts at 0

2. Update `engine-port/src/game/mod.rs`:
   - **P7-006/P7-024 FIX:** Do NOT assume game/mod.rs is still a 9-line stub. Phase 6 Plan 03 modifies it to wire physics/character/water systems. This plan AUGMENTS the existing GamePlugin, adding state machine infrastructure while preserving all Phase 6 system registrations.
   - **P7-041 FIX:** CONDITIONAL EXECUTION: At execution time, read `game/mod.rs` first.
     **P7-112 FIX (MEDIUM):** The PRIMARY path (most likely given project state per MEMORY.md) is
     "Phase 6 NOT executed." Execute this path FIRST:
       PRIMARY PATH — Phase 6 NOT executed (file is the 9-line stub):
         Create fresh `GameState` enum with `States` derive and a fresh `GamePlugin`.
         No migration needed. Proceed to register all systems with `in_state(GameState::Playing)`.
       SECONDARY PATH — Phase 6 HAS been executed (file has physics/water/character systems):
         Follow the full migration steps: remove plain Resource GameState, replace with States
         enum, update all callers as described below.
     If
     Phase 6 has NOT been executed (file is still the 9-line stub), SKIP the migration steps —
     directly create the `GameState` enum with `States` derive and a fresh `GamePlugin`. If
     Phase 6 HAS been executed, follow the migration steps below to replace the plain Resource.
   - **XP-004 FIX (GameState type replacement):** Phase 6 Plan 03 creates a PLAIN `GameState`
     Resource enum with 3 variants (Loading, Playing, Paused). This plan FULLY REPLACES it with
     a Bevy `States`-derived enum adding `MainMenu` as the 4th variant and default. Steps:
     1. Remove the Phase 6 `GameState` Resource definition (plain enum)
     2. Replace with `#[derive(States, Clone, Copy, ...)] enum GameState { MainMenu, Loading, Playing, Paused }`
     3. Change `app.insert_resource(GameState::Loading)` to `app.init_state::<GameState>()`
     4. All Phase 6 systems that checked GameState as a Resource must now use `in_state(GameState::Playing)` run conditions
     5. **P7-034 FIX:** Phase 6 systems that access `Res<GameState>` directly must change to
        `Res<State<GameState>>` and dereference with `.get()`. Bevy States wraps the enum in a
        `State<S>` newtype — `Res<GameState>` will NOT compile after this migration. Example:
        `if *state.get() == GameState::Playing { ... }` instead of `if *state == GameState::Playing`.
        Alternatively, use `.run_if(in_state(GameState::Playing))` run conditions (preferred) which
        eliminates the need for manual state checking in system bodies entirely.
     **XP-101 FIX (CRITICAL):** After completing the GameState migration, add a MANDATORY verification
     step to this plan's verification checklist: "Grep the entire src/ tree for `Res<GameState>` and
     verify each usage is converted to `Res<State<GameState>>` with `.get()` OR replaced with a
     `.run_if(in_state(...))` condition." Command:
     `grep -rn "Res<GameState>" engine-port/src/`
     Any remaining `Res<GameState>` after this plan completes is a compile error. Known Phase 6
     systems that may have this pattern: apply_torque_to_camera, sync_camera_to_player,
     sync_water_to_physics, sync_water_to_render, sync_physics_to_character, sync_mount_to_physics.
     This is NOT an augmentation of GameState — it is a complete type replacement.
   - Read existing game/mod.rs before modifying. Preserve all Phase 6 systems.
   - Add `pub mod state;` and `pub mod menu;`
   - **P7-014 FIX:** After adding GameState, add `.run_if(in_state(GameState::Playing))` to ALL existing Phase 6 systems in GamePlugin (apply_torque_to_camera, sync_camera_to_player, sync_water_to_physics, sync_water_to_render, sync_physics_to_character, sync_mount_to_physics). This prevents Phase 6 systems from running during MainMenu or Loading states.
   - **P7-038 FIX:** ALSO gate `render_pipeline_system` (Phase 5) behind `.run_if(in_state(GameState::Playing))`. Without this, the render pipeline clears AsciiCellGrid every frame during MainMenu, overwriting `render_menu` output. The pipeline must not run when the menu owns the grid.
   - GamePlugin::build registers:
     - init_state::<GameState>()
     - init_resource::<MainMenu>()
     - OnEnter/OnExit for Loading state
     - Update systems gated by in_state() run conditions
     - Menu systems in MainMenu state, pause toggle in Playing and Paused states
  </action>
  <verify>
`cargo test --lib` passes menu and game state tests. `cargo build` succeeds. `cargo clippy -- -D warnings` clean.
  </verify>
  <done>
GamePlugin registers Bevy States, MainMenu navigable with keyboard, menu renders to AsciiCellGrid, state transitions work correctly.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds
2. `cargo test --lib` passes all state and menu tests
3. `cargo clippy -- -D warnings` clean
4. GameState::MainMenu is the default state on startup
5. Menu navigation wraps correctly
6. Enter on Start Game transitions to Loading
</verification>

<success_criteria>
- GameState with 4 states derives Bevy States correctly
- LoadingProgress resource tracks 3-stage loading FSM
- MainMenu has navigable items with keyboard input
- Pause toggle works on Escape key
- Menu renders title and items to AsciiCellGrid
</success_criteria>

## Contract Check
- GAME-01 (game state machine): Task 1 implements GameState with Loading/Playing/Paused transitions
- GAME-02 (main menu): Task 2 implements MainMenu with navigation and Start Game/Quit actions

## Consistency Diff
- game/mod.rs AUGMENTS whatever exists after Phase 6 (NOT a full replacement). Preserves all Phase 6 system registrations. Adds state gating with in_state(GameState::Playing).
- **XP-207 NOTE (HIGH — cross-phase):** Plan 06-03 Task 2 lists plugin registration order as
  `CpuRasterizerPlugin` BEFORE `AsciiOutputPlugin` in main.rs. This DIRECTLY CONTRADICTS
  P5-114 FIX (Plan 05-05) which mandates "AsciiOutputPlugin MUST be added BEFORE CpuRasterizerPlugin."
  If an executor follows the 06-03 listed order, Bevy panics at runtime ("resource not found:
  AsciiCellGrid"). The correct order for main.rs is:
  `AssetLoaderPlugin, WorldPlugin, TerrainPlugin, AsciiOutputPlugin, CpuRasterizerPlugin, PhysicsPlugin, CharacterPlugin, GamePlugin`
  AsciiOutputPlugin creates the AsciiCellGrid resource that CpuRasterizerPlugin reads. Reversal
  causes an immediate runtime panic on first frame. Cross-reference P5-114 FIX in Plan 05-05
  if Plan 06-03 has already been executed to verify main.rs ordering was applied correctly.
- **P7-059 FIX:** Wave-2 plans (07-05) will modify this file after this plan creates it. They must read the file before modifying, not assume structure.
- Phase 6 Plan 03 also modified game/mod.rs to wire physics+character+water. That plan's GamePlugin wiring may conflict. Resolution: Phase 7 Plan 02 creates the state machine foundation; Phase 6 Plan 03's physics/character wiring should be integrated as systems gated on in_state(Playing). If Phase 6 is not yet complete, this plan creates the structure that Phase 6 will build upon.
- AsciiCellGrid import from crate::output used for menu rendering (already exists from Phase 3).
- No Cargo.toml changes in this plan (Bevy States is built-in).

## Blocking Checklist
- [x] GAME-01 covered (Task 1: GameState enum + transitions)
- [x] GAME-02 covered (Task 2: MainMenu with navigation)
- [x] Bevy States derive used (not manual if-else, per Don't Hand-Roll)
- [x] game/mod.rs is the ONLY plan touching this file in Wave 1
- [x] No external dependencies added (States is built-in Bevy)
- [x] Pitfall 5 addressed: check_loading_complete only transitions when stage==0

<output>
After completion, create `.planning/phases/07-game-systems/07-02-SUMMARY.md`
</output>
