---
phase: 07-game-systems
plan: 05
type: tdd
wave: 4
# R17-F211 FIX: wave 3→4 because 07-05 depends on 07-04 (wave 3).
depends_on:
  - "07-02"
  - "07-04"
  # **P7-111 FIX (HIGH):** "07-04" added to depends_on. Both 07-04 and 07-05 are wave 2 and
  # both modify Cargo.toml (07-04 adds kiddo/lru, 07-05 adds noise). XP-007 class conflict:
  # parallel Cargo.toml writes cause merge conflicts. Adding "07-04" enforces sequential
  # execution. Note: if noise is already in Cargo.toml from Phase 6 Plan 03, no Cargo.toml
  # change is needed in 07-05, but the dependency still ensures lock-file stability.
  # **XP-112 FIX:** All phase-7 plans touching Cargo.toml must be sequenced: 07-01 -> 07-03
  # -> 07-04 -> 07-05.
  # **H4-AUDIT-FIX:** Full Cargo.toml chain: 07-01 -> 07-03 -> 07-04 -> 07-05. If any plan
  # in this chain fails mid-execution, Cargo.toml may be in an inconsistent state. Recovery:
  # if a plan fails, verify Cargo.toml parses correctly (`cargo check`) before attempting the
  # next plan. game/mod.rs has FOUR-way ownership (created by 06-03, modified by 07-02, 07-05)
  # but 07-02 dependency handles this correctly.
files_modified:
  - engine-port/Cargo.toml
  - engine-port/src/game/weather.rs
  - engine-port/src/game/mod.rs
autonomous: true
requirements:
  - GAME-03

must_haves:
  truths:
    - "Weather particles spawn as snow or rain based on WeatherState"
    - "Ring-buffer particle pool holds up to 512 particles without heap allocation during updates"
    - "Perlin noise drives wind variation affecting particle drift"
    - "Weather particles composite to AsciiCellGrid AFTER resolve stage"
    - "WeatherState transitions control spawn rate"
    - "weather_composite_system is registered in the SAME schedule as render_pipeline_system, after it."
  **R6-M01 FIX:** Add verification step: "grep game/mod.rs for `weather_composite_system` — must appear inside `add_systems(PostUpdate, ...)` or `add_systems(Update, ...)` matching whatever `render_pipeline_system` is registered in. Commands: `grep -A3 'weather_composite_system' engine-port/src/game/mod.rs` and `grep -A3 'render_pipeline_system' engine-port/src/render/mod.rs` — both must show the same schedule name."
  artifacts:
    - path: "engine-port/src/game/weather.rs"
      provides: "Weather resource, ParticlePool ring buffer, weather state machine, composite system"
      min_lines: 120
  key_links:
    - from: "engine-port/src/game/weather.rs"
      to: "noise::Perlin"
      via: "Perlin noise for wind vector variation"
      pattern: "Perlin|NoiseFn"
    - from: "engine-port/src/game/weather.rs"
      to: "engine-port/src/output/ascii_cell_grid.rs"
      via: "Writes particle glyphs to AsciiCellGrid post-resolve"
      pattern: "AsciiCellGrid|set_cell"
    - from: "engine-port/src/game/mod.rs"
      to: "engine-port/src/game/weather.rs"
      via: "Weather systems registered with run_if in_state Playing"
      pattern: "in_state.*Playing.*weather"
---

<objective>
Implement weather particle effects (snow and rain) using a ring-buffer particle pool with Perlin noise-driven wind, matching C++ weather.cpp architecture.

Purpose: Weather effects add atmospheric immersion. The ring-buffer ensures zero heap allocation during gameplay, matching C++ predictable performance.

Output: `src/game/weather.rs` (Weather resource, ParticlePool, composite system), updated `src/game/mod.rs`.
</objective>

<execution_context>
@.planning/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-game-systems/07-RESEARCH.md
@.planning/phases/07-game-systems/07-02-SUMMARY.md

Key references:
- C++ weather.cpp: WeatherState, ParticlePool (512), CompositeSnowParticles
- Research Pattern 4: Ring-Buffer Particle Pool
- Research Pitfall 4: composite AFTER resolve + sprite blit
- Research open question 4: rain as snow variant with different glyphs
- noise crate: Perlin noise for wind

Dependency: Plan 07-02 provides GameState and GamePlugin structure (in_state(Playing) run condition).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ParticlePool ring buffer and WeatherState machine (TDD)</name>
  <files>
    engine-port/Cargo.toml
    engine-port/src/game/weather.rs
  </files>
  <action>
TDD: Write tests FIRST, then implement.

1. Add `noise = "0.9"` to Cargo.toml (may already be present from Phase 6 Plan 03; if so, verify version).
   **P7-115 FIX (MEDIUM):** If `noise` is already in Cargo.toml, ensure the version is `>= 0.9.0`
   (the `NoiseFn` trait API is stable across 0.8-0.9). If the existing version is `< 0.9`, update
   to `"0.9"`. If it is already `"0.9"` or higher, make no change. Do NOT blindly overwrite an
   existing `noise` entry — check first with `grep noise engine-port/Cargo.toml`.

2. Create game/weather.rs:

RED -- failing tests:
- test_particle_pool_starts_empty
- test_particle_pool_spawn_increments_count
- test_particle_pool_wraps_at_capacity (512+1 spawns)
- test_particle_pool_iter_live_particles
- test_weather_state_spawn_rates
- test_weather_intensity_lerp
  > **R17-F224 FIX:** Specify expected value: starting at intensity=0.0 with target=1.0 and LERP_RATE=0.05, after 1 frame → intensity ≈ 0.05. After 20 frames → intensity ≈ 0.642 (1.0 - 0.95^20).

GREEN -- implement:
- Constants: PARTICLE_CAPACITY=512, SNOW_SPEEDS, RAIN_SPEED=25.0, SPAWN_RATES [0,10,30,60]
  **R19-001 FIX (MEDIUM):** C++ weather.cpp line 37 defines exact snow speeds:
  `SNOW_SPEEDS: [f32; 4] = [15.0, 12.0, 9.0, 6.0]` — indexed by glyph variant (larger glyphs fall
  slower). These are fall velocities in units/sec applied to `vel[2]` (negative = downward). The plan
  referenced SNOW_SPEEDS without listing values. Use these exact C++ values.
  **R13-031 FIX:** Use explicit CP437 codes for snow glyphs (4 glyphs, not 3):
  SNOW_GLYPHS: [u8; 4] = [0x2A, 0x2B, 0x2E, 0x2C]  // *, +, ., comma
  **R13-032 FIX:** Rain glyphs are a NEW FEATURE — C++ only has snow. RAIN_GLYPHS: [u8; 3] = [0x7C, 0x2F, 0x3A] (|, /, :) are design choices, not C++ ports. Document as extension.
  **R7-MED-002 FIX:** `SPAWN_RATES` is indexed by WeatherState discriminant: `SPAWN_RATES[state as usize]`.
  Discriminants are: Clear=0, LightSnow=1, HeavySnow=2, Blizzard=3. A `match` is also correct but
  the array-indexed form matches C++ source.
  **R8-LOW-001 FIX:** For compile-time safety, add a static assert:
  `const _: () = assert!(SPAWN_RATES.len() == WeatherState::Blizzard as usize + 1);`
  This ensures SPAWN_RATES has exactly as many entries as WeatherState variants. Alternatively, use
  a `match` expression for exhaustiveness.
- WeatherParticle: pos [f32;3], vel [f32;3], lifetime_remaining f32, glyph u8, fg [u8;3]
- WeatherState enum: Clear=0, LightSnow=1, HeavySnow=2, Blizzard=3
  WeatherState: Add #[derive(Default)] with #[default] on Clear variant.
- PrecipitationType: Snow, Rain
  PrecipitationType: Add #[derive(Default)] with #[default] on Snow variant.
  Both are needed for Weather to derive Default (required by init_resource::<Weather>()).
- ParticlePool: particles array, head, count
  - new(): zeroed
  **P7-205 FIX (HIGH):** `[WeatherParticle; 512]` arrays CANNOT auto-derive `Default` in stable
  Rust — `Default` is only auto-implemented for arrays up to `[T; 32]`. Implement `Default` for
  `ParticlePool` manually:
  ```rust
  impl Default for ParticlePool {
      fn default() -> Self {
          Self {
              particles: [WeatherParticle::default(); PARTICLE_CAPACITY],
              head: 0,
              count: 0,
          }
      }
  }
  ```
  This requires `WeatherParticle: Copy + Default`. Add `#[derive(Clone, Copy, Default)]` to
  `WeatherParticle` (all fields — pos, vel, lifetime_remaining, glyph, fg — are zero-initializable).
  Compile consequence: `#[derive(Default)]` on `ParticlePool` will fail with
  "the trait bound `[WeatherParticle; 512]: Default` is not satisfied".
  - spawn(particle): write at head, advance, cap
  - iter(): iterate live particles.
    **P7-029 FIX:** `iter()` MUST skip particles with `lifetime_remaining <= 0` (dead entries
    in the ring buffer). The ring buffer doesn't compact dead entries — new spawns overwrite
    them, but iteration must filter them out. Without this, the composite system would render
    dead particles (stale glyphs at stale positions).
  - update(dt): decrement lifetime, apply velocity
- Weather resource: state, precipitation, intensity, target_intensity, wind, perlin,
  **perlin_time: f64**, pool, spawn_accumulator
  **P7-209 FIX (MEDIUM):** `perlin_time: f64` was omitted from this field list but IS used by
  Task 2's `weather_update_system` ("Update perlin_time, compute wind from Perlin noise"). The
  RESEARCH.md Weather struct (line 424) shows `perlin_time: f64` explicitly. An executor reading
  only Task 1 will create `Weather` without this field, then fail to compile when Task 2 tries
  to mutate it. Add `perlin_time: f64` to the struct definition and initialize to `0.0` in Default.
  - Default: Clear, Snow, zero wind, perlin_time=0.0

REFACTOR: extract spawn logic.
  </action>
  <verify>
`cargo test --lib` passes all pool and weather tests. Ring buffer wraps correctly.
  </verify>
  <done>
ParticlePool wraps at 512, WeatherState controls spawn rates, noise crate compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement weather update and composite systems, register in GamePlugin</name>
  <files>
    engine-port/src/game/weather.rs
    engine-port/src/game/mod.rs
  </files>
  <action>
1. Add systems to game/weather.rs:

weather_update_system(weather, time):
- Lerp intensity toward target (0.05/frame)
- Update perlin_time, compute wind from Perlin noise
  **R19-003 FIX (LOW):** C++ weather.cpp lines 208-210 specify exact wind parameters:
  ```cpp
  w->wind[0] = (float)w->pn.noise(w->pn_time * 0.7, 0.0) * 2.0f * w->intensity;
  w->wind[1] = (float)w->pn.noise(0.0, w->pn_time * 0.7) * 2.0f * w->intensity;
  ```
  Use these constants: frequency multiplier = `0.7` (applied to pn_time), amplitude = `2.0 * intensity`.
  Wind is 2D (x and y axes only). Each axis samples a different noise coordinate to produce
  independent variation. Implement as:
  ```rust
  weather.wind[0] = weather.perlin.get([weather.perlin_time * 0.7, 0.0]) as f32 * 2.0 * weather.intensity;
  weather.wind[1] = weather.perlin.get([0.0, weather.perlin_time * 0.7]) as f32 * 2.0 * weather.intensity;
  ```
- Accumulate spawns: accumulator += SPAWN_RATES[state] * dt
- While accumulator >= 1.0: spawn particle (position above viewport, velocity from precipitation type + wind, appropriate glyph)
- Use deterministic pseudo-random for position (hash frame count + index)
- Update existing particles (velocity, lifetime)

weather_composite_system(weather, grid, sample_buffer, camera):
- For each live particle: project to grid coords, if in bounds write glyph + fg to AsciiCellGrid
  **R19-002 FIX (LOW):** C++ weather.cpp line 279 uses `ProjectCoords(r, p.pos, view)` for
  world-to-screen projection. The Rust port MUST use the same camera projection as the render
  pipeline: `project_world_to_screen(&particle.pos, &camera)` from `pipeline.rs`. This function
  already handles the architectural perspective projection correctly. Import it:
  `use crate::render::pipeline::project_world_to_screen;`
  Do NOT invent a separate projection — use the canonical one. The system parameter list must
  include `camera: Res<GameCamera>` to access the projection.
  **R19-005 FIX (HIGH):** Weather particles must respect scene depth to avoid rendering inside
  buildings or behind walls. The system parameter list must include `sample_buffer: Res<SampleBuffer>`.
  Before writing a particle glyph to AsciiCellGrid, check the SampleBuffer depth at the
  particle's projected grid cell. The particle's projected Z (from `project_world_to_screen`)
  provides depth. Compare against the SampleBuffer's stored depth for that cell: only composite
  if the particle is IN FRONT of the rendered surface. Pseudocode:
  ```rust
  if let Some((sx, sy)) = project_world_to_screen(&particle.pos, &camera) {
      let cell_x = sx as usize / 2; // 2x supersample
      let cell_y = sy as usize / 2;
      let sample_depth = sample_buffer.get_depth(cell_x, cell_y);
      if particle_depth < sample_depth { // particle is closer to camera
          grid.set_cell(cell_x as u32, cell_y as u32, glyph, 255, bg);
      }
  }
  ```
  Without this, snow/rain appears to fall inside caves, through roofs, and behind walls.
  C++ checks `ProjectCoords` return value (false = behind camera) but does NOT do explicit
  depth testing — C++ weather also has this visual artifact. The depth check is an IMPROVEMENT
  over C++. If depth data is not easily accessible from SampleBuffer at composite time, this
  check can be deferred as a polish item — document as known limitation if skipped.
- **P7-114 FIX (MEDIUM):** VERIFICATION STEP — Before registering weather_composite_system,
  check which schedule render_pipeline_system is registered in (from Plan 05-05 source or
  06-03 summary). The rule: both systems MUST be in the same schedule.
    - If render_pipeline_system is in PostUpdate (Phase 6 moved it): register weather_composite
      in PostUpdate with `.after(render_pipeline_system)`.
    - If render_pipeline_system is in Update (Phase 5 only, Phase 6 not executed): register
      weather_composite in Update with `.after(render_pipeline_system)`.
  Never use cross-schedule `.after()` — it silently does nothing in Bevy.
  **XP-R5-003 DECISION RULE:** grep render/mod.rs for render_pipeline_system registration.
    - If PostUpdate -> use PostUpdate for weather_composite_system
    - If Update -> use Update
    - If BOTH -> migration incomplete; escalate before proceeding
  Do NOT attempt registration until grep is unambiguous.
- **P7-009 FIX:** Must run AFTER render_pipeline_system. Register with explicit ordering:
  `.after(render_pipeline_system)` to prevent Bevy from scheduling it before resolve.
  **P7-042 FIX:** To use `.after(render_pipeline_system)` in `game/mod.rs`, import the function:
  `use crate::render::pipeline::render_pipeline_system;`
  This creates a compile-time dependency of `game` module on `render::pipeline`. The function
  must be `pub` in pipeline.rs.
  Without this, weather particles would be overwritten by the next resolve pass.
- **AUTHORITATIVE SCHEDULE DECISION (M6-AUDIT-FIX — consolidates P7-033, XP-103, XP-117, XP-R5-003):**
  <!-- R17-F215 FIX: Deleted struck-through P7-033 SUPERSEDED text. Decision below is authoritative. -->
  Phase 6 Plan 06-03 moves `render_pipeline_system` to PostUpdate. Therefore
  `weather_composite_system` MUST also be registered in PostUpdate with
  `.after(render_pipeline_system)`. At execution time, verify by grepping:
  `grep -n 'add_systems(PostUpdate' engine-port/src/render/mod.rs`
  If PostUpdate confirmed, register in PostUpdate. If Update (Phase 6 not executed), register in
  Update. If BOTH appear, migration is incomplete — escalate before proceeding.
  The correct registration is:
  ```rust
  app.add_systems(PostUpdate,
      weather_composite_system
          .after(render_pipeline_system)
          .run_if(in_state(GameState::Playing))
  );
  ```
  Before registering, verify which schedule render_pipeline_system is in (from Plan 05-05 source
  or 06-03 summary). If PostUpdate (expected after Phase 6), use PostUpdate for both.
  **R13-033 FIX (CRITICAL):** STOP-AND-CHECK before registering weather_composite_system:
  1. grep for render_pipeline_system schedule (Update or PostUpdate?)
  2. If PostUpdate: register weather_composite_system in PostUpdate.after(render_pipeline_system)
  3. If Update: register weather_composite_system in Update.after(render_pipeline_system)
  4. If BOTH appear: Phase 6 migration incomplete — ESCALATE, do NOT proceed
  **XP-203 NOTE (R6-001 FIX):** Plan 06-03 `files_modified` DOES list `render/mod.rs` (the PostUpdate migration IS planned in Phase 6). At execution time, grep to confirm the migration was applied: `grep -n 'add_systems(PostUpdate' engine-port/src/render/mod.rs`. If PostUpdate registration is confirmed, register `weather_composite_system` in PostUpdate.
  **R19-007 FIX (HIGH):** GPU extraction timing: Bevy's render pipeline extracts Main World
  resources to the Render World at the `ExtractSchedule` boundary, which runs AFTER PostUpdate.
  Since both `render_pipeline_system` and `weather_composite_system` run in PostUpdate (with
  weather AFTER render), the AsciiCellGrid will contain weather particles when extraction occurs.
  This is correct — weather will be visible on screen. HOWEVER: if the AsciiOutputPlugin's
  extraction system uses `Res<AsciiCellGrid>` (immutable read for extraction), both PostUpdate
  systems must complete before extraction starts. Bevy guarantees this: Extract runs after
  PostUpdate. Verify at execution time: `grep -n 'ExtractSchedule\|extract_resource\|Extract'
  engine-port/src/output/` to confirm the extraction pattern. If extraction uses a different
  mechanism (e.g., custom render node reading from Main World), the timing may differ — check
  and document.
- **P7-029 FIX:** `iter()` must skip particles with `lifetime_remaining <= 0` (dead entries
  in the ring buffer). The ring buffer doesn't compact dead entries — new spawns overwrite them,
  but iteration must filter them out.
- Composites OVER existing content (Pitfall 4: after resolve)
  **R19-004 FIX (INFO):** C++ weather.cpp lines 289-291 ALWAYS sets `cell.fg = 255` (white)
  regardless of the per-particle fg color set during spawn. The C++ ignores the particle's stored
  color and hardcodes white for all weather compositing. Decision: REPLICATE the C++ behavior
  (always use xterm-256 color 255 = white). This is simpler and matches the visual reference.
  If per-particle coloring is desired later (e.g., blue-tinted rain), it can be added as an
  extension. For now: `cell.fg = 255;` in the composite loop.
  **R20-W01 FIX (LOW): Preserve existing bg_color when compositing weather particles.**
  When writing a particle to `AsciiCellGrid`, read the cell's current bg_color first and
  pass it through to set_cell:
  ```rust
  let (_, _, existing_bg) = cell_grid.cell_at(px, py);
  cell_grid.set_cell(px, py, particle_glyph as u16, particle_fg_rgba, existing_bg);
  ```
  Without this, setting bg to black creates visible dark rectangles around each particle.

set_weather_state(weather, state): update state and target_intensity
  **R19-006 FIX (LOW):** No system in any plan calls `set_weather_state` at runtime. Weather
  initializes to `Clear` and never changes. In the C++ engine, weather changes are server-driven
  or timer-based. For MVP: expose `set_weather_state` as a public API but document that no
  automatic trigger exists. Optionally add a debug key (e.g., F5) to cycle weather states for
  testing: `Clear -> LightSnow -> HeavySnow -> Blizzard -> Clear`. This is NOT a gameplay
  feature — it is a debug/demo tool. Mark as deferred for Phase 8 or server-driven weather.

Tests:
- test_weather_update_spawns: HeavySnow + dt=1.0 produces particles
  > **R17-F225 FIX:** Expected particle count: SPAWN_RATES[HeavySnow]=30, dt=1.0, so exactly 30 particles should be spawned. Assert pool.active_count() == 30.
- test_weather_composite_writes: particle at known position writes to grid
- test_weather_clear_no_spawn: Clear spawns zero
  **R16-F209 FIX:** Acceptable as smoke test. SPAWN_RATES[0]=0 makes this true by construction. Meaningful weather coverage is in test_particle_pool_wraps_at_capacity and test_weather_composite_writes.
**R13-034 FIX:** Add test_rain_uses_rain_glyphs — verify PrecipitationType::Rain produces RAIN_GLYPHS [0x7C, 0x2F, 0x3A], not SNOW_GLYPHS.

2. Update game/mod.rs:
   **P7-052 FIX:** Read `game/mod.rs` at execution time before modifying. Plan 07-02 (wave 1)
   creates the GamePlugin structure. This plan (wave 2) adds weather systems to it. Do NOT
   assume the file structure — adapt to what 07-02 produced.
   - Add `pub mod weather;`
   - Register: init_resource Weather, add weather_update + weather_composite chained with run_if(in_state(Playing))
   - **P7-009 FIX:** `weather_composite_system` MUST have explicit `.after(render_pipeline_system)`
     ordering. Without this, Bevy may execute weather composite BEFORE resolve, and the particles
     would be overwritten by the next resolve pass (Research Pitfall 4). Both systems access
     `ResMut<AsciiCellGrid>`, so Bevy's scheduler prevents parallel execution but does NOT
     guarantee a specific order unless `.after()` is specified.
   - **P7-033 FIX (SUPERSEDED):** See AUTHORITATIVE SCHEDULE DECISION above (M6-AUDIT-FIX).
     Both systems MUST be in PostUpdate (after Phase 6 migration). Verify by grepping for
     render_pipeline_system in CpuRasterizerPlugin and GamePlugin to confirm its registration schedule.
  </action>
  <verify>
`cargo test --lib` passes all weather tests. `cargo build` succeeds. Systems registered in GamePlugin.
  </verify>
  <done>
Weather spawns particles via ring buffer with Perlin wind, composite writes to AsciiCellGrid, systems gated on Playing state.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds with noise crate
2. `cargo test --lib` passes all weather tests
3. `cargo clippy -- -D warnings` clean
4. ParticlePool wraps at 512 without allocation
5. Systems only run in Playing state
6. Composite writes to AsciiCellGrid at correct positions
7. **XP-R6-007 VERIFY:** grep `game/mod.rs` for `weather_composite_system` and `render/mod.rs` for `render_pipeline_system` — both must show the same schedule name (PostUpdate).

**R13-035 FIX:** Final accumulated render_pipeline_system signature after all phases:
```rust
fn render_pipeline_system(
    mut sample_buffer: ResMut<SampleBuffer>,
    mut grid: ResMut<AsciiCellGrid>,
    // resolve_buf: allocated locally per R14-F148 (NOT a system parameter)
    camera: Res<GameCamera>,
    mut config: ResMut<RenderConfig>,
    terrain: Option<Res<RuntimeTerrain>>,
    world: Option<Res<RuntimeWorld>>,
    materials: Option<Res<RuntimeMaterials>>,
    mesh_registry: Option<Res<MeshRegistry>>,
    sprite_queue: ResMut<SpriteQueue>,
    timing: ResMut<PipelineTiming>,
    water_config: Res<WaterConfig>,
    // WaterLevel reaches pipeline via WaterConfig.water_z (set by sync_water_to_render)
    time: Res<Time>,
    shape_vector: Option<ResMut<ShapeVectorMatcher>>,
)
```

**R14-PG05 FIX:** Run `cargo fmt -- --check` and `cargo clippy -- -D warnings`
on the entire project (not just this plan's files) to catch cross-file issues.

**R14-PG01 FIX:** After successful execution, update `.planning/ROADMAP.md`:
mark this plan `[x]`, increment the "Plans Complete" counter for this phase.

**R14-PG06 FIX:** Phase boundary verification — run `cargo test` (ALL tests,
no module filter) to verify zero regressions from all prior phases. All 188+
existing tests plus all new tests from this phase must pass.

**R14-PG12 FIX:** Final Acceptance Playtest:
After Phase 7 completes, run the game and verify ALL features:
- [ ] Game starts and shows main menu
- [ ] Loading transitions to gameplay
- [ ] World renders correctly (terrain, meshes, sprites)
- [ ] Character moves and rotates
- [ ] Collision works
- [ ] Water reflections visible
- [ ] Sound effects play
- [ ] Weather effects visible
- [ ] No frame drops below 30fps at 240x135
Log any issues in FAILURE_LOG.md.
</verification>

<success_criteria>
- Ring-buffer 512 particles with correct wrap
- Weather states control spawn rates
- Perlin noise drives wind
- Snow and rain particle types work
- Particles composite after resolve
- Zero heap allocation in update/spawn

**R13-036 FIX:** Phase 7 Day 0 verification checklist (before any plan starts):
1. cargo check with all Phase 5+6 code
2. Verify bevy_kira_audio version compatibility
3. Verify bevy_replicon + renet2 version compatibility
4. Verify kiddo version API
5. Verify States, NextState, Command, Message derive paths

**R13-037 FIX:** After all 5 plans complete, run combined performance benchmark:
All subsystems active (audio + network + weather + shape-vector + full scene).
Must stay under 16.7ms total frame time. Addresses R25 and R26.
</success_criteria>

## Contract Check
- GAME-03 (weather effects): Task 1 implements snow/rain particle types with ring buffer; Task 2 implements update and composite systems

## Consistency Diff
- game/mod.rs also touched by Plan 02 (GamePlugin structure). This plan depends on Plan 02 (wave 2). It adds `pub mod weather;` and weather system registrations to the GamePlugin that Plan 02 creates.
- Cargo.toml noise dependency may already exist from Phase 6 Plan 03. If so, no change needed. If Phase 6 not yet complete, this plan adds it.
- AsciiCellGrid import from output module (existing Phase 3 dependency).
- No file overlap with Plans 01, 03, 04.

## Blocking Checklist
- [x] GAME-03 covered (Tasks 1+2: snow and rain weather effects)
- [x] Depends on Plan 02 for GameState (wave 2)
- [x] Pitfall 4 addressed: composite AFTER resolve
- [x] Ring buffer design (not per-particle Bevy entities, per anti-pattern)
- [x] noise crate for Perlin wind
- [x] Both snow and rain precipitation types supported
- [x] game/mod.rs dependency on Plan 02 is explicit

<output>
After completion, create `.planning/phases/07-game-systems/07-05-SUMMARY.md`
</output>

## R20 Integration Audit Finding (2026-02-24)

| ID | Severity | Description | Source |
|----|----------|-------------|--------|
| R20-W01 | LOW | Preserve bg_color when compositing weather particles | magecore-gpu audit F-05 |
