---
phase: 07-game-systems
plan: 07
type: execute
wave: 5
depends_on:
  - "07-03"
files_modified:
  - engine-port/tests/network_integration.rs
autonomous: true
gap_closure: true
requirements:
  - NET-01

must_haves:
  truths:
    - "An #[ignore] integration test exists that builds a server App and a client App with bevy_replicon + renet2"
    - "The test verifies a JoinRequest can be sent from client to server over actual renet transport"
    - "Running `cargo test -- --ignored network` executes the connectivity test"
  artifacts:
    - path: "engine-port/tests/network_integration.rs"
      provides: "In-process server+client connectivity integration test"
      contains: "test_server_client_join_exchange"
      min_lines: 30
  key_links:
    - from: "engine-port/tests/network_integration.rs"
      to: "bevy_replicon::prelude::RepliconPlugins"
      via: "Both server and client Apps register RepliconPlugins"
      pattern: "RepliconPlugins"
    - from: "engine-port/tests/network_integration.rs"
      to: "engine-port/src/network/protocol.rs"
      via: "Uses JoinRequest and PoseUpdate types"
      pattern: "JoinRequest|PoseUpdate"
---

<objective>
Add an `#[ignore]` integration test that spins up a server and client App in-process and verifies they can exchange a JoinRequest message over renet transport.

Purpose: The verification found that no in-process connectivity integration test exists for NET-01. Unit tests cover protocol serialization and standalone mode, but no test exercises actual server-client communication. This gap closure adds the missing transport-level smoke test.

Output: `engine-port/tests/network_integration.rs` with an `#[ignore]` integration test.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-game-systems/07-VERIFICATION.md
@.planning/phases/07-game-systems/07-03-SUMMARY.md

Key references:
- bevy_replicon 0.39 + bevy_replicon_renet2 0.14 (actual versions from 07-03 execution)
- Existing network module: src/network/mod.rs, protocol.rs, server.rs, client.rs
- P7-045/P7-218: Dual-UDP in-process test is inherently flaky — use #[ignore]
- R7-LOW-002: Mark with #[ignore] by default, run with `cargo test -- --ignored`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create in-process server+client network integration test</name>
  <files>
    engine-port/tests/network_integration.rs
  </files>
  <action>
1. Create `engine-port/tests/network_integration.rs`:

2. Read the 07-03-SUMMARY.md first to understand exactly what was implemented and the actual bevy_replicon version used (0.39 per verification findings).

3. Read `engine-port/src/network/mod.rs`, `server.rs`, `client.rs`, `protocol.rs` to understand the actual API surface and plugin registration.

4. Write the test module with:

```rust
//! Network integration tests: server+client connectivity over renet transport.
//!
//! These tests are #[ignore] by default because they bind UDP sockets and are
//! timing-dependent. Run with: cargo test -- --ignored network
```

5. Test `test_server_client_join_exchange` (marked `#[ignore]`):
   - **Approach A (preferred):** Use bevy_replicon's test utilities if available. Check if `bevy_replicon` exposes `MemoryTransport` or test helpers by running `cargo doc --open` for bevy_replicon and searching for "test" or "memory".
   - **Approach B (fallback):** If no test transport exists, create two separate Bevy Apps:
     - Server App: `MinimalPlugins` + `RepliconPlugins` + `RepliconRenetPlugins` configured as server on localhost ephemeral port (port 0 or a high random port like 14235)
     - Client App: `MinimalPlugins` + `RepliconPlugins` + `RepliconRenetPlugins` configured as client connecting to the server's address
     - Run server `app.update()` a few times
     - Run client `app.update()` a few times
     - Verify the connection is established (check for connected client events or ConnectedPlayers resource on server)
   - **Approach C (minimal):** If renet2 cannot bind two Apps in the same process reliably, write a simpler test:
     - Create server App with RepliconPlugins + renet2 in server mode
     - Run a few update cycles
     - Assert the server starts without panic and the renet server resource exists
     - Add a comment: `// Full client connectivity requires separate processes; this verifies server-side initialization only.`
     - This is the minimum viable gap closure that proves the transport stack initializes.

6. Add a second test `test_protocol_types_are_replicon_compatible`:
   - NOT #[ignore] (deterministic, no network)
   - Verify PoseUpdate implements the required traits for bevy_replicon replication:
     - `Component` (required for replication)
     - `Serialize + Deserialize` (required for network transport)
     - `Default` (required for initial state)
   - Simple: create PoseUpdate::default(), assert fields are zero, serialize/deserialize round-trip

7. Mark all UDP-dependent tests with `#[ignore]` and add comment:
   `// Timing-dependent test — run with: cargo test -- --ignored network_integration`

**IMPORTANT:** Read the actual bevy_replicon and renet2 API at execution time. The exact setup for creating a renet server/client from code varies by version. The 07-03-SUMMARY.md will document what was actually implemented. Adapt to the real API, not the plan's pseudocode.

**Error handling:** Bevy system functions must return `()`. In test code, you can use `unwrap()` or `expect()` freely since test panics are the expected failure mode.
  </action>
  <verify>
`cargo build --tests` succeeds (integration test compiles). `cargo test network_integration -- --ignored` runs the connectivity test. `cargo test test_protocol_types_are_replicon_compatible` passes (non-ignored test).
  </verify>
  <done>
An `#[ignore]` integration test exists at `tests/network_integration.rs` that exercises the renet transport stack. A non-ignored test verifies PoseUpdate trait compliance for replication.
  </done>
</task>

</tasks>

<verification>
1. `cargo build --tests` succeeds
2. `cargo test test_protocol_types_are_replicon_compatible` passes (non-ignored, deterministic)
3. `cargo test network_integration -- --ignored` runs without compile errors (may timeout in CI, acceptable for #[ignore])
4. `cargo clippy -- -D warnings` clean

**R14-PG01 FIX:** After successful execution, update `.planning/ROADMAP.md`:
add 07-07-PLAN.md to the plans list, update plan count.
</verification>

<success_criteria>
- Integration test file exists at `engine-port/tests/network_integration.rs`
- At least one #[ignore] test exercises server+client renet transport initialization
- At least one non-ignored test verifies PoseUpdate replication trait compliance
- Test compiles and links against the actual bevy_replicon + renet2 versions from 07-03
</success_criteria>

## Contract Check
- NET-01 (client-server multiplayer): Gap closure — adds the missing integration test proving transport-level connectivity works

<output>
After completion, create `.planning/phases/07-game-systems/07-07-SUMMARY.md`
</output>
