---
phase: 07-game-systems
plan: 03
type: execute
wave: 1
depends_on:
  - "07-01"
  # **P7-117 FIX (LOW):** depends_on "07-01" is for Cargo.toml write serialization (XP-007),
  # NOT a logical dependency. Audio output is not required for networking to function.
  # This ordering prevents parallel Cargo.toml conflicts when 07-01 and 07-03 execute concurrently.
files_modified:
  - engine-port/Cargo.toml
  - engine-port/src/network/mod.rs
  - engine-port/src/network/protocol.rs
  - engine-port/src/network/server.rs
  - engine-port/src/network/client.rs
  - engine-port/src/lib.rs
  - engine-port/src/main.rs
autonomous: true
requirements:
  - NET-01
  - NET-02

must_haves:
  truths:
    - "Server can accept client connections and track connected players"
    - "Client can connect to a server and send/receive position updates"
    - "Binary message types (Join/Exit/Pose/Talk) serialize and deserialize correctly"
    - "Entity replication syncs position data between server and clients"
    - "In-process integration test verifies a client connects to a server via renet transport and exchanges a join message"
  artifacts:
    - path: "engine-port/src/network/mod.rs"
      provides: "NetworkPlugin with bevy_replicon + renet setup for client and server modes"
      min_lines: 40
    - path: "engine-port/src/network/protocol.rs"
      provides: "Binary message types matching C++ protocol (JoinRequest, PoseUpdate, TalkMessage, ExitNotice)"
      min_lines: 60
    - path: "engine-port/src/network/server.rs"
      provides: "Server systems: accept connections, broadcast state, handle disconnects"
      min_lines: 50
    - path: "engine-port/src/network/client.rs"
      provides: "Client systems: send local pose, receive remote state, apply replication"
      min_lines: 50
  key_links:
    - from: "engine-port/src/network/mod.rs"
      to: "bevy_replicon::RepliconPlugins"
      via: "app.add_plugins(RepliconPlugins)"
      pattern: "RepliconPlugins"
    - from: "engine-port/src/network/protocol.rs"
      to: "serde + bincode"
      via: "Serialize/Deserialize derives on message structs"
      pattern: "derive.*Serialize.*Deserialize"
    - from: "engine-port/src/network/server.rs"
      to: "engine-port/src/network/protocol.rs"
      via: "Uses message types for broadcast"
      pattern: "PoseUpdate|JoinRequest"
    - from: "engine-port/src/main.rs"
      to: "engine-port/src/network/mod.rs"
      via: "app.add_plugins(NetworkPlugin)"
      pattern: "NetworkPlugin"
---

<objective>
Implement basic client-server multiplayer networking using bevy_replicon 0.38 with bevy_renet transport, including binary message types inspired by the C++ WebSocket protocol.

Purpose: Multiplayer is a core Asciicker feature. The C++ engine uses 7 message types over WebSockets for position sync, chat, and join/exit. bevy_replicon provides server-authoritative entity replication that maps to this model.

Output: `src/network/mod.rs` (NetworkPlugin), `src/network/protocol.rs` (message types), `src/network/server.rs` (server), `src/network/client.rs` (client), updated Cargo.toml.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-game-systems/07-RESEARCH.md

Key references:
- bevy_replicon 0.38 (Bevy 0.18): RepliconPlugins, Replication component
- bevy_renet / bevy_replicon_renet2: network transport bridge
- C++ network.h: STRUCT_REQ_JOIN, STRUCT_BRC_POSE, STRUCT_BRC_TALK
- Research Pitfall 2: Network protocol endianness (use bincode LE)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add networking dependencies and create protocol message types</name>
  <files>
    engine-port/Cargo.toml
    engine-port/src/network/protocol.rs
    engine-port/src/lib.rs
  </files>
  <action>
1. Add to Cargo.toml:
   - bevy_replicon = "0.38"
     **P7-045 FIX:** bevy_replicon version must be verified at execution time: `cargo search bevy_replicon`.
     Version 0.38 may be outdated. The dual-UDP in-process test is inherently flaky — prefer
     `MemoryTransport` if available, or use channel-based synchronization instead of `thread::sleep`.
   - **P7-005 FIX:** The bevy_replicon transport bridge crate version must be verified at
     implementation time. The ecosystem evolved rapidly: bevy_replicon_renet -> bevy_replicon_renet2.
     At execution, run `cargo search bevy_replicon_renet` and check crates.io for the exact crate
     name and version compatible with bevy_replicon 0.38 / Bevy 0.18. Pin whatever version compiles.
     If no compatible transport crate exists, use raw renet2 with manual channel setup as fallback.
     **P7-109 FIX (HIGH):** Per MEMORY.md, use `bevy_replicon_renet2 = "0.13"` as the transport
     crate (the version validated for Bevy 0.18 during Phase 6 planning). If `cargo search` shows
     a newer compatible version, prefer that, but 0.13 is the known baseline. Do NOT use
     `bevy_replicon_renet` (without "2") — that crate does not support Bevy 0.18.
   - serde = { version = "1", features = ["derive"] }
   - bincode = "1"
     **P7-053 FIX:** Use `bincode = "=1.3.3"` (exact pin). bincode 2.x has a completely incompatible
     API (uses Encode/Decode instead of serde). The `"1"` semver spec could theoretically resolve to
     a breaking 1.x release.
     **P7-123 INFO:** The explanation above is slightly imprecise. Corrected: Use `bincode = "=1.3.3"`
     (exact pin) for reproducibility. bincode 2.x has a completely incompatible API (uses
     Encode/Decode instead of serde). While `"1"` semver would NOT select 2.x (it only allows
     compatible 1.x updates), minor 1.x updates could subtly change serialization output. Exact pin
     ensures byte-identical protocol messages across builds.

2. Add `pub mod network;` to lib.rs.

3. Create network/protocol.rs with 4 message types:
   - JoinRequest: name String (max 31 chars)
   - ExitNotice: reason u8
   - PoseUpdate: anim u8, frame u8, action_mount u8, pos [f32;3], dir f32, sprite u16 -- also derive Component for replication
   - TalkMessage: text String (max 256 bytes)
   - NetMessage enum wrapping all 4 types
   - encode_message/decode_message using bincode serialize/deserialize
   - Default for PoseUpdate with zeroed fields

4. Unit tests: round-trip encode/decode for each message type, max-length name, float preservation
  </action>
  <verify>
`cargo build` succeeds with new dependencies. `cargo test --lib` passes protocol round-trip tests.
  </verify>
  <done>
4 message types with serde derives, bincode encode/decode, round-trip tests pass, networking crates compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement NetworkPlugin with server and client systems, wire into main.rs</name>
  <files>
    engine-port/src/network/mod.rs
    engine-port/src/network/server.rs
    engine-port/src/network/client.rs
    engine-port/src/main.rs
  </files>
  <action>
1. Create network/mod.rs:
   - pub mod protocol, server, client
   - NetworkMode enum: Standalone, Server { port: u16 }, Client { addr: String }
   - NetworkConfig resource (default: Standalone)
   - NetworkPlugin: add RepliconPlugins + RepliconRenet2Plugins, init NetworkConfig, register PoseUpdate as replicated, conditionally add server/client systems
   **P7-219 INFO:** The plugin group name `RepliconPlugins` may differ in bevy_replicon 0.38 for
   Bevy 0.18. Possible alternatives: `RepliconCorePlugins`, `ServerPlugin`/`ClientPlugin` as
   separate additions. Run `cargo doc --open` for bevy_replicon 0.38 to confirm the exact plugin
   group name before using `RepliconPlugins`. P7-045 FIX elsewhere in this plan already mandates
   verifying versions at execution time — apply the same verification to the plugin name.

2. Create network/server.rs:
   - ConnectedPlayers resource: HashMap u64->String
   - handle_join: listen for join messages, add to connected players, spawn entity with PoseUpdate + Replication
   - handle_disconnect: remove players, despawn entities

3. Create network/client.rs:
   - LocalPlayer resource: entity ID
   - send_local_pose: read local Transform, write PoseUpdate component
   - apply_remote_poses: read PoseUpdate on remote entities, update Transform
   **P7-108 FIX (HIGH):** IMPORTANT: Bevy system functions must return `()`. Do NOT use `?` in
   system bodies — it will fail to compile because Bevy systems must return `()`, not `Result`.
   Use `let Ok(...) = ... else { return; }` or explicit match for error handling inside systems.
   The `?` operator is only valid inside test helper functions annotated with `-> Result<(), E>`,
   not in system functions registered with `app.add_systems()`.

4. Wire NetworkPlugin into main.rs:
   - Add `use asciicker_engine::network::NetworkPlugin;` import
   - Add `NetworkPlugin` to the app.add_plugins tuple alongside existing plugins

5. In-process connectivity integration test (addresses roadmap success criterion: "Two clients can connect to a server and see each other's characters move"):
   **P7-206 FIX (HIGH):** `StatesPlugin` may not be importable by that name in a `MinimalPlugins`
   test context in Bevy 0.18. Before compiling the integration test, verify the correct import path:
   run `cargo doc --open` and search for `StatesPlugin`. In Bevy 0.18 the likely path is
   `bevy::state::app::StatesPlugin`. Use `app.add_plugins(bevy::state::app::StatesPlugin)` if
   `StatesPlugin` is not re-exported at the `bevy` crate root. If `MinimalPlugins` already includes
   states support in Bevy 0.18, the separate `StatesPlugin` addition may be redundant (or cause a
   double-registration panic). Confirm before compiling the test.
   - Create a test in network/mod.rs (or network/server.rs) that:
     a. Builds a Bevy App with MinimalPlugins + StatesPlugin (see P7-206 FIX above) + RepliconPlugins + RepliconRenet2Plugins in server mode (bind to localhost ephemeral port)
     b. Builds a second App with MinimalPlugins + StatesPlugin (see P7-206 FIX above) + RepliconPlugins + RepliconRenet2Plugins in client mode (connect to server port)
     c. Runs both apps for a few update cycles
     d. Sends a JoinRequest from client, verifies server's ConnectedPlayers contains the new player
     e. Verifies the server spawns an entity with PoseUpdate + Replication for the joined client
   - This exercises actual transport-level connection (renet UDP) in-process, not just unit-level struct tests
   - If bevy_renet's transport cannot bind two apps in the same process (port conflict or runtime limitation), use an alternative: spawn server in a background thread with a dedicated Bevy App, client in main thread, with a short sleep for handshake. Document the pattern.
   **P7-218 INFO:** `thread::sleep`-based fallback is inherently flaky in CI (timing-dependent).
   P7-045 FIX (elsewhere in this plan) already recommends preferring `MemoryTransport` if
   available, or channel-based synchronization. Neither a concrete `MemoryTransport` nor a
   channel-based sync approach is specified as the replacement. If using `thread::sleep`, document
   the chosen sleep duration and mark the test as `#[ignore]` in CI or gate with a feature flag
   to prevent intermittent CI failures.

Note: Character entities from Phase 6 provide the actual data. This plan creates the scaffold. Use a stub NetworkTestMarker component for testing without Phase 6 dependency.

Unit tests: NetworkConfig defaults to Standalone, ConnectedPlayers add/remove
  </action>
  <verify>
`cargo build` succeeds. `cargo test --lib` passes unit tests. `cargo test --test` or `cargo test network` passes the in-process connectivity integration test (server accepts client join). NetworkPlugin in Standalone mode works without panics.
  </verify>
  <done>
NetworkPlugin registers bevy_replicon + RepliconRenet2Plugins, supports 3 modes, server tracks players, client syncs poses, PoseUpdate replicated. NetworkPlugin is wired into main.rs. In-process integration test verifies a client can connect to a server and exchange a join message over actual renet transport.
  </done>
</task>

</tasks>

<verification>
1. `cargo build` succeeds with bevy_replicon 0.38 + transport bridge crate (version TBD per P7-005)
2. `cargo test --lib` passes all protocol and networking unit tests
3. `cargo test network` (or equivalent) passes in-process connectivity integration test (server+client join exchange)
4. `cargo clippy -- -D warnings` clean
5. NetworkPlugin in Standalone mode adds no transport overhead
6. Message round-trip verified for all 4 types
7. NetworkPlugin is registered in main.rs alongside existing plugins
</verification>

<success_criteria>
- bevy_replicon 0.38 + transport bridge crate compiles with Bevy 0.18 (P7-005: version TBD)
- 4 binary message types with round-trip correctness
- Server accepts connections and tracks players
- Client sends/receives pose updates
- Standalone mode works without network transport
- In-process integration test proves client can connect to server and send a join message via renet transport
- NetworkPlugin wired into main.rs app builder
</success_criteria>

## Contract Check
- NET-01 (client-server multiplayer): Task 2 implements NetworkPlugin with server/client modes and entity replication via bevy_replicon; in-process integration test verifies two-endpoint connectivity per roadmap success criterion
- NET-02 (binary protocol): Task 1 defines 4 message types with bincode serialization matching C++ protocol structure

## Consistency Diff
- **XP-036 FIX:** Plans 07-01 and 07-03 share Cargo.toml, lib.rs, and main.rs. Added 07-01 as
  dependency to prevent parallel execution conflicts (per XP-007 FIX in 07-01). The `depends_on`
  frontmatter has been updated accordingly.
- Cargo.toml also touched by Plans 01, 04, 05 (different crates, no conflict)
- lib.rs also touched by Plan 01 (audio module). No conflict: each adds a different `pub mod` line.
- main.rs also touched by Plan 01 (AsciickerAudioPlugin). No conflict: each adds a different plugin to the tuple.
- network/ directory is new; no file overlap with any other plan.
- PoseUpdate Component derive is needed for bevy_replicon replication; this does not conflict with Phase 6 character components.

## Blocking Checklist
- [x] NET-01 covered (Task 2: NetworkPlugin with replication + in-process connectivity test)
- [x] NET-02 covered (Task 1: binary protocol with bincode)
- [x] bevy_replicon 0.38 pinned (not 0.37 which is Bevy 0.17)
- [ ] bevy_replicon transport crate: verify at execution time (P7-005 FIX — exact crate/version TBD)
- [x] Pitfall 2 addressed: bincode uses LE by default
- [x] Standalone mode as default (no forced network dependency at startup)
- [x] NetworkPlugin wired into main.rs (key_link added)
- [x] No file overlap with Plans 02, 04, 05 (main.rs and Cargo.toml shared with Plan 01, non-conflicting additions)

<output>
After completion, create `.planning/phases/07-game-systems/07-03-SUMMARY.md`
</output>
