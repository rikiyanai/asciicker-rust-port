---
phase: 12-full-networking
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "09-03"]
files_modified:
  - engine-port/src/network/mod.rs
  - engine-port/src/network/item_replication.rs
autonomous: true
requirements:
  - FNET-03

must_haves:
  truths:
    - "Item ownership (inventory vs world) is authoritative on the server."
    - "Pickup and drop actions are validated by the server."
    - "SpatialGrid contents for items are synchronized across the network."
    - "Consumable use events are broadcast to clients."
  artifacts:
    - path: "engine-port/src/network/item_replication.rs"
      provides: "Item ownership sync and world interaction protocol"
      min_lines: 120
---

**Requirements**: FNET-03

<objective>
Implement item replication and interaction synchronization, ensuring consistent item states across all clients.

Purpose: Manages the lifecycle of world items and inventory contents in a multiplayer setting. The server maintains authoritative ownership data, preventing items from being duplicated or lost during network events.

Output: `src/network/item_replication.rs` (Item sync).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/12-full-networking/12-RESEARCH.md

Key references:
- Phase 9: Item interaction and ownership model
- Phase 8: SpatialGrid for entity queries
- C++ netplay item protocol
</context>

<tasks>
  <task id="12.3.1" title="Implement Authoritative Item Ownership" est="M">
    Implement `server_item_ownership_system`.
    Requirement: FNET-03.
    Tracks which items belong to which character entities.
    Handles `Pickup` and `Drop` requests from clients.
    Updates `Item::PURPOSE` and BSP/SpatialGrid linkage on the server.
  </task>
  <task id="12.3.2" title="Synchronize Item Spatial States" est="M">
    Ensure that item entities in the world are correctly replicated via `bevy_replicon`.
    Requirement: FNET-03.
    Synchronize `SpatialGrid` membership based on server-side position updates.
  </task>
  <task id="12.3.3" title="Replicate Item Use Events" est="S">
    Define `ItemUseEvent` for network broadcast.
    Requirement: FNET-03.
    Triggers animations or sound effects on clients when an item is consumed.
  </task>
</tasks>

<verification>
### Layer 4: Manual Verification (Authoritative)
**GATE 12.3.A:** Item Ownership.
- *Action:* Client A picks up an item. Client B tries to pick up the same item at the same time.
- *Requirement:* Only one client should succeed; the other must receive a "Failed" message.
- *Submission:* Console logs showing the server's rejection of the second request.

### Layer 2: Integration (ECS Contract)
Run the item replication test:
```bash
cargo test --test item_ownership_replication -- --nocapture
```
- *Input:* Item dropped by Client A.
- *Expected Output:* `Client B Entity Spawned: true`.

### Layer 1: Logic (Data-Only)
Run the protocol enum tests:
```bash
cargo test item_replication::tests::test_mutation_kinds -- --nocapture
```
- *Expected Data:* Numeric mapping of the 8 mutation kinds.
</verification>

<success_criteria>
- Item ownership (inventory vs world) is authoritative on the server.
- Pickup and drop actions are validated by the server.
- SpatialGrid contents for items are synchronized across the network.
- Consumable use events are broadcast to clients.
</success_criteria>

## Contract Check

- FNET-03: Task 12.3.1, 12.3.2, and 12.3.3 implement item ownership and interaction sync.

## Consistency Diff

- Authoritative ownership prevents "item duplication" glitches that were possible in older C++ network versions.

## Blocking Checklist

- [ ] FNET-03
- [ ] Item pickup validation verified
- [ ] World item replication verified

<risks>
  <risk id="R84" severity="LOW" mitigation="Inventory snapshots">
    Full inventory replication can be large; only replicate changes (pickup/drop/reorder).
  </risk>
</risks>

<notes>
Item entities in the world should be low-bandwidth ghosts; only position and proto ID are critical.
</notes>

<output>
After completion, create `.planning/phases/12-full-networking/12-03-SUMMARY.md`
</output>
