---
phase: 12-full-networking
plan: 01
type: execute
wave: 1
depends_on: ["07-03", "08-02"]
files_modified:
  - engine-port/Cargo.toml
  - engine-port/src/network/mod.rs
  - engine-port/src/network/replication.rs
autonomous: true
requirements:
  - FNET-01

must_haves:
  truths:
    - "Entities with the Replication component are correctly synced between server and client."
    - "Replicated components include Position, AnimationState, and SpriteReq."
    - "Authoritative server tick is used to order state snapshots."
    - "Client spawns remote entities correctly upon receipt of snapshots."
  artifacts:
    - path: "engine-port/src/network/replication.rs"
      provides: "Entity replication registry and sync systems"
      min_lines: 120
---

**Requirements**: FNET-01

<objective>
Implement full entity snapshot replication using bevy_replicon, enabling authoritative world state synchronization.

Purpose: Provides the foundation for multiplayer gameplay. The snapshot system ensures that all clients see the same characters and entities at the same positions, using an authoritative server model.

Output: `src/network/replication.rs` (Replication logic).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/12-full-networking/12-RESEARCH.md

Key references:
- Phase 7: Initial Networking skeleton (07-03)
- C++ netplay snapshot system
- `bevy_replicon` documentation for component registration
</context>

<tasks>
  <task id="12.1.1" title="Register Replicated Components" est="M">
    Update `network/mod.rs` to register components for replication.
    Requirement: FNET-01.
    Include: `Transform`, `AnimationState`, `ActionState`, `SpriteReq`, `CombatState`.
    Ensure all components implement `Serialize` and `Deserialize`.
  </task>
  <task id="12.1.2" title="Implement Authoritative Tick Sync" est="S">
    Define `NetworkClock` Resource.
    Requirement: FNET-01.
    Synchronizes the authoritative server tick to all clients.
    Used for ordering snapshots and handling late-arriving packets.
  </task>
  <task id="12.1.3" title="Implement Remote Entity Spawning" est="M">
    Implement `client_entity_sync_system`.
    Requirement: FNET-01.
    Handles spawning of "Ghost" entities for remote players and NPCs.
    Ensures correct component composition based on server archetypes.
  </task>
</tasks>

<verification>
### Layer 4: Manual Verification (Authoritative)
**GATE 12.1.A:** Multiplayer Sync.
- *Action:* Run two instances of the game. Move Client A.
- *Requirement:* Client B must see Client A's character moving smoothly.
- *Submission:* Console logs from both instances showing matching `authoritative_tick` values.

### Layer 2: Integration (ECS Contract)
Run the replication sync test:
```bash
cargo test --test entity_replication_sync -- --nocapture
```
- *Input:* Change HP on Server.
- *Expected Output:* `Client HP: X` (matching server).
- *Agent Action:* Provide the packet size dump for a standard pose update.

### Layer 1: Logic (Data-Only)
Run the tick ordering unit tests:
```bash
cargo test replication::tests::test_tick_sequence -- --nocapture
```
- *Input:* Received ticks [10, 12, 11].
- *Expected Output:* `Tick 11 discarded: true`.
</verification>

<success_criteria>
- Entities with the Replication component are correctly synced between server and client.
- Replicated components include Position, AnimationState, and SpriteReq.
- Authoritative server tick is used to order state snapshots.
- Client spawns remote entities correctly upon receipt of snapshots.
</success_criteria>

## Contract Check

- FNET-01: Task 12.1.1, 12.1.2, and 12.1.3 implement snapshot synchronization.

## Consistency Diff

- Replication uses the `bevy_replicon` ecosystem, which provides a more robust delta-compression model than the original custom protocol.

## Blocking Checklist

- [ ] FNET-01
- [ ] Snapshot ordering verified by test
- [ ] Remote entity spawning verified

<risks>
  <risk id="R82" severity="MEDIUM" mitigation="Use delta compression">
    Full snapshots every frame can saturate bandwidth; rely on `bevy_replicon`'s internal delta compression.
  </risk>
</risks>

<notes>
Replication is the "what", while Input-Based Movement (12-04) handles the "how" of player control.
</notes>

<output>
After completion, create `.planning/phases/12-full-networking/12-01-SUMMARY.md`
</output>
