---
phase: 12-full-networking
plan: 02
type: execute
wave: 2
depends_on: ["12-01", "08-04"]
files_modified:
  - engine-port/src/network/mod.rs
  - engine-port/src/network/combat_protocol.rs
autonomous: true
requirements:
  - FNET-02

must_haves:
  truths:
    - "Combat actions (swing, hit, death) are validated by the server."
    - "Hit detection uses the server's authoritative SpatialGrid."
    - "Damage and knockback are broadcast to all relevant clients."
    - "Combat protocol matches the C++ message flow (SWING -> DAMAGE -> DEATH)."
  artifacts:
    - path: "engine-port/src/network/combat_protocol.rs"
      provides: "Combat network messages and server validation logic"
      min_lines: 150
---

**Requirements**: FNET-02

<objective>
Implement the networked combat protocol, ensuring authoritative validation of hits and synchronized visual feedback.

Purpose: Prevents cheating and ensures combat consistency in a multiplayer environment. The server validates melee ranges and timing before broadcasting damage events to clients.

Output: `src/network/combat_protocol.rs` (Combat sync).
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/12-full-networking/12-RESEARCH.md

Key references:
- Phase 8: Melee combat hit detection logic
- C++ game.cpp: combat networking (BRC_SWING, BRC_DAMAGE)
- `bevy_replicon` event channel documentation
</context>

<tasks>
  <task id="12.2.1" title="Define Combat Network Events" est="S">
    Define `CombatActionEvent` (Server-to-Client) and `CombatRequestEvent` (Client-to-Server).
    Requirement: FNET-02.
    Messages: `Swing`, `DamageApply`, `DeathTrigger`, `RespawnNotify`.
  </task>
  <task id="12.2.2" title="Implement Server Hit Validation" est="M">
    Implement `server_combat_validation_system`.
    Requirement: FNET-02.
    Receives `Swing` requests from clients.
    Checks range and timing against the server's `SpatialGrid`.
    If valid, applies damage to target and emits `DamageApply` broadcast.
  </task>
  <task id="12.2.3" title="Implement Client Combat Feedback" est="M">
    Implement `client_combat_feedback_system`.
    Requirement: FNET-02.
    Triggers local animations and effects based on server messages.
    Ensures knockback is applied locally to maintain visual consistency.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 12.2.A:** Network Combat.
- *Action:* Client A attacks Client B.
- *Requirement:* Client B must react (play hit animation) and take damage.
- *Submission:* Screenshots of both clients showing matching HP values after the hit.

### Layer 2: Integration (ECS Contract)
Run the hit validation test:
```bash
cargo test --test combat_network_validation -- --nocapture
```
- *Input:* Client sends hit at distance 10.0.
- *Expected Output:* `Server Result: Rejected` (distance > 3.0).
- *Agent Action:* Log the exact server-side distance calculation.

### Layer 1: Logic (Data-Only)
Run the message serialization test:
```bash
cargo test combat_protocol::tests::test_binary_layout -- --nocapture
```
- *Expected Output:* Hex dump of a `DamageApply` message matching the C++ protocol size.
</verification>

<success_criteria>
- Combat actions (swing, hit, death) are validated by the server.
- Hit detection uses the server's authoritative SpatialGrid.
- Damage and knockback are broadcast to all relevant clients.
- Combat protocol matches the C++ message flow (SWING -> DAMAGE -> DEATH).
</success_criteria>

## Contract Check

- FNET-02: Task 12.2.1, 12.2.2, and 12.2.3 implement combat events and server validation.

## Consistency Diff

- Server validation uses the 3D SpatialGrid for proximity checks, improving upon the 2D BSP checks in some C++ branches.

## Blocking Checklist

- [ ] FNET-02
- [ ] Server-side hit validation verified
- [ ] Combat event broadcast verified

<risks>
  <risk id="R83" severity="MEDIUM" mitigation="Use client prediction">
    Network latency can make combat feel sluggish; consider predictive swing animations while waiting for server ACK.
  </risk>
</risks>

<notes>
Hit frame 21 must be respected by the server during validation.
</notes>

<output>
After completion, create `.planning/phases/12-full-networking/12-02-SUMMARY.md`
</output>
