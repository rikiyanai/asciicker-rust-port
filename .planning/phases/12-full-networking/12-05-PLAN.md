---
phase: 12-full-networking
plan: 05
type: execute
wave: 4
depends_on: ["12-04"]
files_modified:
  - engine-port/src/network/mod.rs
  - engine-port/src/network/lag_comp.rs
autonomous: true
requirements:
  - FNET-05

must_haves:
  truths:
    - "Ping is measured at 10Hz and used for interpolation timing."
    - "Remote players are interpolated smoothly using a target position buffer."
    - "Interpolation handles network jitter without visual snapping."
    - "Latency compensation algorithm matches C++ interpolation logic."
  artifacts:
    - path: "engine-port/src/network/lag_comp.rs"
      provides: "Ping tracking and remote entity interpolation"
      min_lines: 100
---

**Requirements**: FNET-05

<objective>
Implement lag compensation and remote entity interpolation.

Purpose: Ensures that remote entities (other players and NPCs) move smoothly on the local client, even with network latency and jitter. Smoothing algorithms hide packet variance, providing a high-fidelity multiplayer experience.

Output: `src/network/lag_comp.rs` (Smoothing systems).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/12-full-networking/12-RESEARCH.md

Key references:
- C++ netplay interpolation logic
- `bevy_replicon` remote entity sync
</context>

<tasks>
  <task id="12.5.1" title="Implement Ping Measurement" est="S">
    Define `LatencyTracker` Resource.
    Requirement: FNET-05.
    Sends heartbeats at 10Hz and calculates RTT (Round Trip Time).
    Exposes `network_delay` for interpolation systems.
  </task>
  <task id="12.5.2" title="Implement Remote Entity Interpolation" est="M">
    Define `RemoteInterpolation` Component for ghost entities.
    Requirement: FNET-05.
    Stores a ring buffer of `(pos, tick)` pairs received from the server.
    Implement `interpolate_remote_entities` system.
    Calculates display position by interpolating between the two most relevant buffered states.
  </task>
  <task id="12.5.3" title="Handle Jitter and Packet Loss" est="M">
    Implement smoothing logic for when packets arrive out of order or are missing.
    Requirement: FNET-05.
    Extrapolate briefly if the buffer is empty.
    Limit the maximum visual speed to prevent snapping.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 12.5.A:** Interpolation Fidelity.
- *Action:* Observe a remote player moving in a circle.
- *Requirement:* The remote player's movement must be fluid, even if their packets are arriving in bursts.
- *Submission:* Console log showing the `interpolation_delay` adapting to network conditions.

### Layer 2: Integration (ECS Contract)
Run the jitter smoothing test:
```bash
cargo test --test remote_entity_interpolation -- --nocapture
```
- *Input:* Snapshot arrives with 50ms jitter.
- *Expected Data:* `visual_pos` velocity remains constant.
- *Agent Action:* Log the `visual_pos` delta vs `actual_pos` delta.

### Layer 1: Logic (Data-Only)
Run the ring buffer unit tests:
```bash
cargo test lag_comp::tests::test_buffer_wrap -- --nocapture
```
- *Expected Output:* Ordered list of ticks in the target buffer.
</verification>

<success_criteria>
- Ping is measured at 10Hz and used for interpolation timing.
- Remote players are interpolated smoothly using a target position buffer.
- Interpolation handles network jitter without visual snapping.
- Latency compensation algorithm matches C++ interpolation logic.
</success_criteria>

## Contract Check

- FNET-05: Task 12.5.1, 12.5.2, and 12.5.3 implement ping tracking and entity smoothing.

## Consistency Diff

- Smoothing uses a linear interpolation buffer identical to the C++ netplay implementation to preserve character movement weight.

## Blocking Checklist

- [ ] FNET-05
- [ ] Ping measurement frequency verified
- [ ] Interpolation buffer smoothing verified

<risks>
  <risk id="R86" severity="MEDIUM" mitigation="Adjust buffer delay">
    Too little delay leads to snapping; too much delay feels unresponsive. Tune buffer size based on measured ping.
  </risk>
</risks>

<notes>
Interpolation is critical for the "feel" of other players; refer to the netplay skill pack for the target buffer algorithm.
</notes>

<output>
After completion, create `.planning/phases/12-full-networking/12-05-SUMMARY.md`
</output>
