---
phase: 13-npc-scripting
plan: 01
type: execute
wave: 1
depends_on: ["08-02"]
files_modified:
  - engine-port/Cargo.toml
  - engine-port/src/game/mod.rs
  - engine-port/src/game/scripting.rs
autonomous: true
requirements:
  - SCRIPT-01

must_haves:
  truths:
    - "Lua 5.4 runtime is embedded and initialized."
    - "Execution environment is sandboxed (no OS/IO access)."
    - "Lua scripts can be loaded from the assets directory."
    - "NPC entities can have an associated Lua script handle."
  artifacts:
    - path: "engine-port/src/game/scripting.rs"
      provides: "Lua runtime management and script loader"
      min_lines: 100
---

**Requirements**: SCRIPT-01

<objective>
Implement the core Lua runtime integration using the mlua crate, providing a sandboxed environment for NPC scripting.

Purpose: Enables user-extensible NPC behavior without recompiling the game engine. Lua provides a high-level, safe way to define complex AI logic, replacing the hardcoded V8 bridge from the C++ original with a more idiomatic Rust-to-Lua interface.

Output: `src/game/scripting.rs` (Lua runtime).
</objective>

<execution_context>
 @.planning/workflows/execute-plan.md @.planning/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/13-npc-scripting/13-RESEARCH.md

Key references:
- Decision D4: NPC Scripting — Lua (mlua) by default
- C++ game.cpp: V8 bridge and shared memory approach
- `mlua` documentation for sandboxing and function handles
</context>

<tasks>
  <task id="13.1.1" title="Embed Lua and Initialize Sandbox" est="M">
    Add `mlua = { version = "0.9", features = ["lua54", "vendored"] }` to Cargo.toml.
    Requirement: SCRIPT-01.
    Define `LuaRuntime` Resource.
    Initialize Lua with minimal libraries (base, table, string, math).
    Disable `io`, `os`, `debug` for security.
    Implement per-NPC environment tables (`setfenv` equivalent) to prevent global state leaks across different NPC entities.
  </task>
  <task id="13.1.2" title="Implement Script Loader" est="M">
    Implement `script_loader_system`.
    Requirement: SCRIPT-01.
    Loads `.lua` files from `assets/scripts/`.
    Compiles scripts into function handles.
    Caches handles in a `ScriptRegistry` resource.
  </task>
  <task id="13.1.3" title="Define NpcScript Component" est="S">
    Define `NpcScript` Component.
    Requirement: SCRIPT-01.
    Stores the name of the script to run for this entity.
    Includes a reference to the compiled Lua function.
  </task>
</tasks>

<verification>
### Layer 4: Manual Verification (Authoritative)
**GATE 13.1.A:** Lua Integration.
- *Action:* Create a script `test.lua` that returns "Asciicker Rust".
- *Requirement:* The game console must print the string when the script is executed.
- *Submission:* Copy the printed output from the console.

### Layer 2: Integration (ECS Contract)
Run the script loader test:
```bash
cargo test --test scripting_loader_integration -- --nocapture
```
- *Input:* Load 3 separate scripts.
- *Expected Output:* `Registry size: 3`, `Unique Env Handles: 3`.
- *Agent Action:* Verify per-NPC environment isolation by checking pointer addresses in the log.

### Layer 1: Logic (Data-Only)
Run the sandbox unit tests:
```bash
cargo test scripting::tests::test_sandbox_io_blocked -- --nocapture
```
- *Input:* Lua script attempting `io.open`.
- *Expected Output:* `Lua Error: attempt to index a nil value (global 'io')`.
</verification>

<success_criteria>
- Lua 5.4 runtime is embedded and initialized.
- Execution environment is sandboxed (no OS/IO access).
- Lua scripts can be loaded from the assets directory.
- NPC entities can have an associated Lua script handle.
</success_criteria>

## Contract Check

- SCRIPT-01: Task 13.1.1, 13.1.2, and 13.1.3 implement runtime embedding and loading.

## Consistency Diff

- Embedded Lua (`mlua`) replaces the custom V8 shared-memory bridge, providing a more idiomatic and safe Rust interface.

## Blocking Checklist

- [ ] SCRIPT-01
- [ ] Sandboxing verified (OS/IO libraries stripped)
- [ ] Script registry caching verified

<risks>
  <risk id="R87" severity="MEDIUM" mitigation="Strict sandboxing">
    Embedded scripts could theoretically access the host system; ensure all unsafe Lua libraries are stripped.
  </risk>
</risks>

<notes>
mlua handles most of the FFI boilerplate; focus on the Bevy resource integration.
</notes>

<output>
After completion, create `.planning/phases/13-npc-scripting/13-01-SUMMARY.md`
</output>
