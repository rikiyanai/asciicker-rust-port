---
phase: 13-npc-scripting
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - engine-port/src/game/mod.rs
  - engine-port/src/game/script_api.rs
autonomous: true
requirements:
  - SCRIPT-02

must_haves:
  truths:
    - "Lua scripts can access entity position, health, and target data."
    - "Lua can issue movement commands (move_to) and combat actions (attack)."
    - "Callback hooks (on_spawn, on_tick, on_death) are functional."
    - "World queries (terrain height, nearby entities) are exposed to Lua."
  artifacts:
    - path: "engine-port/src/game/script_api.rs"
      provides: "FFI bindings and NPC hook execution systems"
      min_lines: 200
---

**Requirements**: SCRIPT-02

<objective>
Implement the NPC Script API, exposing engine functionality and data to the Lua runtime.

Purpose: Bridges the gap between the Rust engine and Lua scripts. Provides the necessary hooks and functions for scripts to observe the world and control NPC behavior, matching the depth of the original C++ V8 integration.

Output: `src/game/script_api.rs` (Lua bindings).
</objective>

<execution_context>
 @/Users/r/.claude/get-shit-done/workflows/execute-plan.md @/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
 @.planning/PROJECT.md @.planning/ROADMAP.md @.planning/STATE.md @.planning/phases/13-npc-scripting/13-RESEARCH.md

Key references:
- Phase 8: NPC AI behavior framework
- Phase 5: Terrain and World queries
- `mlua` documentation for userdata and global functions
</context>

<tasks>
  <task id="13.2.1" title="Expose Entity Data to Lua" est="M">
    Register Rust functions in Lua global scope: `get_pos(entity)`, `get_health(entity)`, `get_target(entity)`.
    Requirement: SCRIPT-02.
    Implement Lua `UserData` for common types like `Vec3`.
  </task>
  <task id="13.2.2" title="Implement Movement and Action API" est="M">
    Expose NPC control functions: `move_to(entity, x, y)`, `attack(entity)`, `say(entity, text)`.
    Requirement: SCRIPT-02.
    These functions update the NPC's `PhysicsIO` or `ActionState` components.
  </task>
  <task id="13.2.3" title="Implement World Query API" est="L">
    Expose read-only world queries: `get_terrain_height(x, y)`, `get_nearby_entities(pos, radius)`.
    Requirement: SCRIPT-02.
    Use the `SpatialGrid` and `RuntimeTerrain` resources.
  </task>
  <task id="13.2.4" title="Execute Script Hooks" est="M">
    Implement `execute_npc_scripts_system`.
    Requirement: SCRIPT-02.
    Iterates NPCs with `NpcScript` component.
    Calls `on_tick` Lua function every frame.
    Ensure errors are caught and logged without crashing the engine.
  </task>
</tasks>

<verification>
### Layer 4: Manual Visual Verification (Authoritative)
**GATE 13.2.A:** Scripted Movement.
- *Action:* Assign a Lua script to an NPC that calls `move_to(player_pos)`.
- *Requirement:* The NPC must autonomously follow the player.
- *Submission:* Provide the NPC's `PhysicsIO` force log generated by the Lua API calls.

### Layer 2: Integration (ECS Contract)
Run the API binding test:
```bash
cargo test --test npc_script_api_bindings -- --nocapture
```
- *Input:* Lua calls `get_pos()`.
- *Expected Output:* `Vec3 { x: 10.0, y: 5.0, z: 0.0 }` matching the character's transform.

### Layer 1: Logic (Data-Only)
Run the world query test:
```bash
cargo test script_api::tests::test_nearby_entities_lua -- --nocapture
```
- *Expected Data:* List of entity IDs returned to the Lua stack.
</verification>

<success_criteria>
- Lua scripts can access entity position, health, and target data.
- Lua can issue movement commands (move_to) and combat actions (attack).
- Callback hooks (on_spawn, on_tick, on_death) are functional.
- World queries (terrain height, nearby entities) are exposed to Lua.
</success_criteria>

## Contract Check

- SCRIPT-02: Task 13.2.1, 13.2.2, 13.2.3, and 13.2.4 implement the script API and hook execution.

## Consistency Diff

- API functions match the original C++ V8 hook list to ensure existing script logic can be easily ported.

## Blocking Checklist

- [ ] SCRIPT-02
- [ ] Movement API verified by integration test
- [ ] World query results verified

<risks>
  <risk id="R88" severity="MEDIUM" mitigation="Batch query results">
    Frequent world queries from multiple NPCs can be slow; ensure data exposure is efficient.
  </risk>
</risks>

<notes>
Refer to the C++ V8 bridge for the exact function list used by the original scripters.
</notes>

<output>
After completion, create `.planning/phases/13-npc-scripting/13-02-SUMMARY.md`
</output>
