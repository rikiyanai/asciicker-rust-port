---
phase: 05-pipeline-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
# **XP-106 FIX (HIGH):** Added 05-03 dependency. Integration tests in this plan construct a
# GameCamera to obtain a valid view_tm for render_patch/render_mesh. If Plan 05-03 has not
# run and GameCamera type does not exist, integration tests will not compile. Add 05-03 here
# to enforce the correct build order even though the shaders themselves receive view_tm as
# a plain [f64; 16] parameter and do not import GameCamera directly.
files_modified:
  - engine-port/src/render/terrain_shader.rs
  - engine-port/src/render/mesh_shader.rs
  - engine-port/src/render/resolve_bridge.rs
  - engine-port/src/render/mod.rs
  - engine-port/src/output/ascii_cell_grid.rs
  - engine-port/src/render/math.rs
autonomous: true
requirements:
  - REND-08

must_haves:
  truths:
    - "TerrainShader implements RasterShader trait and rasterizes terrain patch triangles into SampleBuffer with material indices and diffuse lighting"
    - "MeshShader implements RasterShader trait and rasterizes mesh instance triangles into SampleBuffer with RGB555 colors"
    - "resolve_to_grid function bridges Phase 4 resolve output (Vec<AnsiCell>) to Phase 3 AsciiCellGrid (RGBA colors) via xterm-256 palette lookup"
    - "MaterialTable extracted from loaded A3D asset and registered as standalone Bevy Resource"
    # R14-F130 FIX: This plan defines the MaterialTable STRUCT and type.
    # Plan 05-05 owns the Bevy Resource REGISTRATION (insert_resource).
    # Do NOT register MaterialTable as a Resource in this plan.
  artifacts:
    - path: "engine-port/src/render/terrain_shader.rs"
      provides: "TerrainShader implementing RasterShader, render_patch function"
      min_lines: 100
    - path: "engine-port/src/render/mesh_shader.rs"
      provides: "MeshShader implementing RasterShader, render_mesh function"
      min_lines: 60
    - path: "engine-port/src/render/resolve_bridge.rs"
      provides: "resolve_to_grid converting AnsiCell palette indices to AsciiCellGrid RGBA"
      min_lines: 40
  key_links:
    - from: "engine-port/src/render/terrain_shader.rs"
      to: "engine-port/src/render/rasterizer.rs"
      via: "TerrainShader calls rasterize() for each terrain triangle"
      pattern: "rasterize\\("
    - from: "engine-port/src/render/mesh_shader.rs"
      to: "engine-port/src/render/rasterizer.rs"
      via: "MeshShader calls rasterize() for each mesh face"
      pattern: "rasterize\\("
    - from: "engine-port/src/render/resolve_bridge.rs"
      to: "engine-port/src/render/resolve.rs"
      via: "Bridge calls resolve() then converts AnsiCell to AsciiCellGrid"
      pattern: "resolve\\("
    - from: "engine-port/src/render/resolve_bridge.rs"
      to: "engine-port/src/output/ascii_cell_grid.rs"
      via: "Bridge writes to AsciiCellGrid with RGBA colors"
      pattern: "AsciiCellGrid"
---

<objective>
Implement TerrainShader and MeshShader (the concrete RasterShader implementations that connect Phase 4's rasterizer to Phase 5's terrain/world data) and the AnsiCell-to-AsciiCellGrid format bridge.

Purpose: This resolves CRITICAL gap #1 (placeholder rendering) and CRITICAL gap #3 (format mismatch). The Phase 4 rasterizer EXISTS and is complete -- these shaders CALL it with real geometry. The resolve bridge converts the Phase 4 output format (xterm-256 palette indices) to the Phase 3 input format (RGBA [u8; 4]).

Output: TerrainShader that triangulates HEIGHT_CELLS grid per patch and rasterizes via Phase 4. MeshShader that transforms mesh vertices and rasterizes per-face. resolve_to_grid that converts AnsiCell -> AsciiCellGrid.

KEY CONSTRAINT: Do NOT modify SampleBuffer, AnsiCell, RasterShader trait, rasterize(), or resolve(). Build ON TOP of Phase 4.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-integration/05-RESEARCH.md

Phase 4 interfaces (DO NOT MODIFY):
@engine-port/src/render/rasterizer.rs (RasterShader trait, rasterize(), bresenham())
@engine-port/src/render/resolve.rs (resolve() function)
@engine-port/src/render/sample_buffer.rs (Sample, SampleBuffer)
@engine-port/src/render/material.rs (Material, auto_mat_lookup)
@engine-port/src/render/types.rs (AnsiCell)
@engine-port/src/render/quantize.rs (rgb8_to_rgb5, rgb2pal, pack_rgb555)

Phase 5 dependencies:
@engine-port/src/terrain/patch_runtime.rs (RuntimePatch from Plan 01)
@engine-port/src/output/ascii_cell_grid.rs (AsciiCellGrid from Phase 3)

Reference C++ architecture:
@docs/skills/engine-render.md (RenderPatch, RenderFace shader implementations)
@docs/arch/render_cpp_part1.md (render.cpp:404-557 shader blend, RenderPatch triangulation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TerrainShader and render_patch function</name>
  <files>
    engine-port/src/render/terrain_shader.rs
  </files>
  <action>
Create `terrain_shader.rs` implementing the C++ RenderPatch equivalent (~200 lines in C++). This is the core terrain rendering logic that connects Phase 4's rasterizer to Phase 5's terrain data.

- `TerrainShader` struct implementing `RasterShader`:
  ```rust
  pub struct TerrainShader {
      pub material_index: u16,  // visual[u][v] from the terrain patch
      pub diffuse_base: u8,     // base diffuse lighting (can be modulated by normal)
  }
  ```
  - `blend(&self, sample: &mut Sample, z: f32, _bc: [f32; 3])`:
    - Depth test: if `sample.height > z || sample.height == Sample::CLEAR_HEIGHT`
    - **P5-055 FIX:** ~~**WRONG** — "larger z = closer to camera"~~ is BACKWARDS. See P5-101 FIX below for correction. The inline `height > z || CLEAR_HEIGHT` test IS correct, but the direction description here is wrong. Do NOT use `depth_test_ro()` which uses a different epsilon tolerance. Both terrain and mesh shaders MUST use the same inline pattern.
      <!-- XP-206 FIX (LOW): P5-055's original "larger z = closer" claim was not marked WRONG inline,
      only corrected by the P5-101 FIX block below it. An executor reading sequentially might
      internalize the wrong direction before reaching the correction. Now struck through and
      labeled WRONG at the point of the incorrect statement. -->
      **P5-101 FIX (CRITICAL):** The P5-055 depth convention statement above is BACKWARDS. The
      correct convention is: SMALLER z = closer to camera (wins depth test). The FlatShader
      integration tests in rasterizer.rs confirm this: a near triangle at z=50 overwrites a far
      triangle at z=200 via the test `sample.height (200) > z (50) = TRUE`, so the fragment with
      z=50 (smaller) overwrites the one with z=200 (larger). Do NOT write code that assumes
      "larger z = closer." The inline pattern `sample.height > z || CLEAR_HEIGHT` is CORRECT
      but the semantic description "larger z = closer" is WRONG and will mislead future
      developers. Correct statement: "smaller z = closer to camera."
      **P5-109 FIX (HIGH):** P5-055 and P5-031 both reference the same inline pattern but P5-055
      misidentifies the direction. An implementer reading P5-055 may flip comparisons or
      misinterpret world Z-coordinates. P5-031 (which simply says "matching Phase 4 FlatShader
      tests") is the safe reference — check rasterizer.rs FlatShader tests directly for ground truth.
    - **P5-031 FIX (depth test pattern):** This uses the inline `height > z || CLEAR_HEIGHT` pattern
      (matching Phase 4 FlatShader tests), NOT the `depth_test_ro()` method which includes
      HALF_HEIGHT_SCALE epsilon tolerance. Both shaders (terrain + mesh) MUST use the same pattern.
      The inline pattern is chosen because Phase 4's rasterizer tests all use it. Do NOT mix
      `depth_test_ro()` calls with inline comparisons — pick one and apply consistently.
      **P5-302 FIX (CRITICAL):** The reason to avoid `depth_test_ro()` is NOT only the epsilon tolerance — it is primarily a SEMANTIC INVERSION. `depth_test_ro()` returns TRUE when the EXISTING sample wins (the new fragment does NOT overwrite). The inline pattern `sample.height > z || CLEAR_HEIGHT` guards the WRITE path (condition is true when NEW fragment wins). These are opposite conditions. Using `depth_test_ro()` in a shader blend without negation would silently SKIP all fragment writes. To use it correctly you would need `if !sample.depth_test_ro(z) { write fragment }`. This inversion is error-prone. Stick with the inline pattern. Note: `depth_test_ro()` IS used correctly in `bresenham()` (see P5-320 INFO below) because bresenham's OR-bit logic has the opposite sense — it sets grid bits when the existing sample does NOT block the line.
      **P5-320 INFO:** `depth_test_ro()` IS used correctly in `bresenham()` (rasterizer.rs line ~264): `if buf[idx].depth_test_ro(z) { buf[idx].spare |= or_bits; }`. This is correct because bresenham logic ORs grid bits when the existing sample does NOT block the line (height <= z + epsilon means the line is in front-of or at the existing surface). This INTENTIONALLY uses the epsilon tolerance for line painting. The distinction: shader blends use the strict inline pattern; bresenham grid lines use the tolerant `depth_test_ro`. Do NOT apply P5-031's "use inline pattern" guidance to bresenham — the epsilon behavior is deliberate there.
    - Write: `sample.visual = self.material_index` (terrain uses material index, NOT RGB555)
    - Write: `sample.diffuse = self.diffuse_base`
    - Write: `sample.spare = 0` (NO MESH_FLAG -- this is terrain)
    - Write: `sample.height = z`
    - TRAP-R01: spare bit 3 (MESH_FLAG) must be 0 for terrain. The resolve pass branches on this.
    - **P5-106 FIX (CRITICAL):** WARNING: `Sample::clear_state()` initializes `spare = spare_bits::MESH_FLAG`
      (confirmed from sample_buffer.rs). ALL initially-cleared samples therefore have MESH_FLAG set.
      Do NOT add a guard like `if sample.spare & MESH_FLAG == 0` before writing terrain — this would
      incorrectly skip ALL uninitialized (clear) terrain regions. The CLEAR_HEIGHT check in the depth
      test is the only correct gate. The `sample.spare = 0` write in TerrainShader overrides the
      MESH_FLAG from clear_state, which is the intended behavior.
    - **P5-116 FIX (MEDIUM):** NOTE: In 2x2 blocks where terrain only partially covers the buffer
      (e.g., patch edges), combined_spare in resolve() ORs in MESH_FLAG from adjacent clear samples,
      causing edge blocks to use the mesh resolve path. This is expected behavior consistent with
      Phase 4 design. The TerrainShader sets spare=0 for each written sample — only samples that
      were never touched by TerrainShader retain the clear_state MESH_FLAG.

  **R50 FIX:** TerrainShader must also read `patch.dark` bitmask to modulate diffuse lighting for shadowed cells. In `render_patch`, after computing material_index for each visual cell:
  ```rust
  let cell_bit = u + v * VISUAL_CELLS; // visual cell index within patch (0..63)
  // R5-014 NOTE: Use u + v * VISUAL_CELLS (not literal 8) for future-proofing. VISUAL_CELLS = 8.
  let is_shadowed = (patch.dark >> cell_bit) & 1 != 0;
  let diffuse = if is_shadowed { diffuse_base / 2 } else { diffuse_base };
  ```
  This halves diffuse for shadowed cells, matching the C++ behavior where shadowed terrain renders darker. Without this fix, shadows are computed (Plan 05-06) but never visually applied.

- `render_patch` function: The core terrain-to-rasterizer bridge.
  **P5-050 FIX:** Callers must drop the `&mut [Sample]` borrow before calling `resolve_to_grid(&SampleBuffer, ...)`. Use `{}` scoping blocks to explicitly end the mutable slice borrow. See P5-033 FIX in 05-05.
  ```rust
  /// buf_w, buf_h: SAMPLE buffer dimensions (2*ascii_w+4, 2*ascii_h+4), NOT ASCII dimensions
  pub fn render_patch(
      buf: &mut [Sample],
      buf_w: i32,
      buf_h: i32,
      patch: &RuntimePatch,
      patch_x: i32,
      patch_y: i32,
      view_tm: &[f64; 16],
  )
  ```
  Implementation (port of C++ RenderPatch):
  - The patch has HEIGHT_CELLS=4 quads per edge (5x5 vertices)
  - For each quad (hx, hy) in 0..HEIGHT_CELLS:
    - Get 4 corner heights from `patch.height[hy][hx]`, `[hy][hx+1]`, `[hy+1][hx]`, `[hy+1][hx+1]`
    - Compute world-space vertex positions: each height cell spans (VISUAL_CELLS/HEIGHT_CELLS = 2) visual cells
    - World X = patch_x * VISUAL_CELLS + hx * (VISUAL_CELLS/HEIGHT_CELLS)
    - World Y = patch_y * VISUAL_CELLS + hy * (VISUAL_CELLS/HEIGHT_CELLS)
    - World Z = height_value (raw u16, HEIGHT_SCALE applied later in projection)
    - Transform each vertex through view_tm to get sample-buffer [x, y, z] integer coords
    - Look up material from `patch.visual[vy][vx]` where vy/vx map from the height cell to the visual grid (scale factor 2)
      **P5-059 FIX:** Axis convention for `patch.visual[first][second]`: verify against C++ terrain.cpp whether first index is X or Y. Rust `[[T; N]; M]` means `arr[row][col]`. If C++ uses `visual[u][v]` with u=X, Rust must use `visual[col][row]` or the material lookup will be transposed.
      **P5-121 FIX (MEDIUM):** The "verify against C++ terrain.cpp" above is now resolved. The
      TerrainPatch is parsed as row-major: `visual[row][col]` = `visual[vy][vx]` where vy is the
      Y visual cell index (0..8) and vx is the X index (0..8). Access as `patch.visual[vy][vx]`.
      Do NOT use `patch.visual[vx][vy]` — that would transpose the material layout.
    - Check `patch.diag` bit to determine triangle split direction for this quad
    - Create TerrainShader with the material_index and a diffuse value (compute from surface normal or use 0xFF for now)
    - Call `rasterize(buf, buf_w, buf_h, &shader, [&v0, &v1, &v2], false)` for each triangle (2 per quad)
    - Also call `bresenham()` for grid lines if grid rendering is enabled (spare_bits::GRID)

  Helper: `transform_vertex(world: [f64; 3], view_tm: &[f64; 16]) -> [i32; 4]`:
  - Apply 4x4 matrix multiply: screen_x = tm[0]*wx + tm[4]*wy + tm[8]*wz + tm[12], etc.
  - **P5-057 FIX:** All transform matrices are row-major (matching C++ render.cpp). Multiply: `out[i*4+j] = sum_k(A[i*4+k] * B[k*4+j])`. Apply `instance_tm` to model_pos first, then `view_tm` to the result.
  - Convert to integer sample-buffer coordinates
  - Compute cull_flags from screen bounds (bit 0=left, bit 1=right, etc.)
  **P5-310 FIX (MEDIUM):** `transform_vertex` is defined in terrain_shader.rs (Task 1) but is also needed by mesh_shader.rs (Task 2) for `render_mesh` vertex transformation. These are SEPARATE files. The plan is silent on the sharing strategy. Use ONE of these approaches and be explicit: (a) declare `transform_vertex` as `pub(crate)` in terrain_shader.rs and import it in mesh_shader.rs with `use crate::render::terrain_shader::transform_vertex;`, OR (b) extract it to a new `render/math.rs` utility module and import from both. Option (b) is preferred if the function is used in more than two places. Option (a) creates an awkward semantic dependency (mesh imports from terrain). Do NOT silently duplicate the function in mesh_shader.rs — that creates a divergence risk for the matrix layout fix (P5-057).
  **F038/F039 FIX — DECISION:** Extract `transform_vertex` to `engine-port/src/render/math.rs` as a shared utility. Both `terrain_shader.rs` and `mesh_shader.rs` import from `render::math`. Do NOT duplicate the function. `render/math.rs` is added to `files_modified` (see frontmatter). In `render/mod.rs`, add `pub mod math;`. Both shaders use `use crate::render::math::transform_vertex;`.

Unit tests:
- `test_terrain_shader_writes_material_index`: Verify blend writes material index (not RGB555) with spare=0 (no MESH_FLAG)
- `test_terrain_shader_depth_test`: Closer terrain overwrites farther
- `test_render_patch_produces_samples`: Create a small flat patch, render it, verify non-clear samples exist
- `test_render_patch_material_mapping`: Verify visual[u][v] material indices appear in sample.visual
- `test_render_patch_shadow_modulates_diffuse` (R50): Patch with dark bit set produces lower diffuse than unshadowed cell
  </action>
  <verify>
`cargo test --lib -- render::terrain_shader` passes all tests. `cargo clippy -- -D warnings` clean.
  </verify>
  <done>
TerrainShader implements RasterShader trait, writing material indices (not RGB555) with spare=0 for correct resolve path selection. render_patch triangulates HEIGHT_CELLS grid and calls Phase 4 rasterize() for each triangle. Grid lines rendered via bresenham().
  </done>
</task>

<task type="auto">
  <name>Task 2: MeshShader, resolve_to_grid bridge, and module wiring</name>
  <files>
    engine-port/src/render/mesh_shader.rs
    engine-port/src/render/resolve_bridge.rs
    engine-port/src/render/mod.rs
  </files>
  <action>
Create `mesh_shader.rs`:
- `MeshShader` struct implementing `RasterShader`:
  ```rust
  pub struct MeshShader {
      pub rgb555: u16,   // per-face color packed as RGB555
      pub diffuse: u8,   // lighting intensity
  }
  ```
  - `blend(&self, sample: &mut Sample, z: f32, _bc: [f32; 3])`:
    - Depth test: if `sample.height > z || sample.height == Sample::CLEAR_HEIGHT`
    - Write: `sample.visual = self.rgb555` (mesh uses RGB555 direct color)
    - Write: `sample.diffuse = self.diffuse`
    - Write: `sample.spare = spare_bits::MESH_FLAG` (MESH_FLAG set for mesh)
    - Write: `sample.height = z`
    - TRAP-R01: spare bit 3 (MESH_FLAG) must be SET for mesh. The resolve pass uses this to select auto_mat path.

- `render_mesh` function:
  ```rust
  pub fn render_mesh(
      buf: &mut [Sample],
      buf_w: i32,
      buf_h: i32,
      mesh: &AkmMesh,       // mesh geometry from Phase 2
      instance_tm: &[f64; 16],  // instance transform matrix
      view_tm: &[f64; 16],     // camera view matrix
  )
  ```
  **P5-127 FIX (INFO):** Before implementing render_mesh, read the AkmMesh data format:
  `@engine-port/src/asset_loader/akm_mesh.rs` — this file specifies whether face vertex
  indices are u16/u32, how per-vertex colors are stored, and how face normals are laid out.
  Do NOT guess the face format — check the source.

  Implementation (port of C++ RenderFace):
  - For each face in mesh.faces:
    - Get 3 vertex positions from mesh.vertices by face vertex indices
    - Transform each vertex: world_pos = instance_tm * model_pos, then screen_pos = view_tm * world_pos
    - Convert to integer [x, y, z, cull_flags] format expected by rasterize()
    - Get face color from mesh face data (vertex colors averaged, or face color if available)
    - Convert face color to RGB555 via rgb8_to_rgb5 and pack_rgb555
    - Compute diffuse from face normal dot light direction (simplified: use 0xFF or basic n.l)
    - Create MeshShader with rgb555 and diffuse
    - Call `rasterize(buf, buf_w, buf_h, &shader, [&v0, &v1, &v2], true)` (double-sided for meshes)

**R47 FIX: Add GlyphSelector trait for resolve() extensibility.**
**P5-052 FIX:** Both `resolve_to_grid` and `select_glyph` take `&SampleBuffer` (immutable). This compiles correctly — multiple immutable borrows are allowed. The mutable `&mut sample_buffer.samples` borrow from render stages MUST be dropped (via scoping) before this point.
In `resolve_bridge.rs`, define a trait that Phase 7 shape-vector can implement:
```rust
/// Trait for pluggable glyph selection at RESOLVE stage.
/// Default implementation uses auto_mat glyph. Phase 7 shape-vector
/// provides an alternative implementation that samples the SampleBuffer
/// for 6D shape vectors and matches via k-d tree.
pub trait GlyphSelector {
    /// Given a 2x2 cell region in the SampleBuffer, return a CP437 glyph index.
    /// Returns None to fall back to auto_mat glyph selection.
    /// P5-324 INFO: `cell_x` and `cell_y` are ASCII GRID coordinates (0..ascii_w, 0..ascii_h),
    /// NOT sample-buffer coordinates. To access the corresponding 2x2 sample block in a
    /// Phase 7 shape-vector implementation, compute: `sx = 2 + 2 * cell_x`, `sy = 2 + 2 * cell_y`.
    /// This matches resolve.rs's internal offset logic (2-sample border + 2 samples per ASCII cell).
    /// M-06 NOTE: `&mut self` is intentional — allows stateful implementations in Phase 7
    /// (e.g., k-d tree caching). AutoMatGlyphSelector is stateless but uses `&mut self`
    /// for trait uniformity. The ergonomic friction of requiring mutable references at call
    /// sites is accepted as a design tradeoff for future extensibility.
    fn select_glyph(&mut self, sample_buffer: &SampleBuffer, cell_x: usize, cell_y: usize) -> Option<u8>;
}

/// Default selector that always falls back to auto_mat (returns None).
pub struct AutoMatGlyphSelector;
impl GlyphSelector for AutoMatGlyphSelector {
    fn select_glyph(&mut self, _buf: &SampleBuffer, _cx: usize, _cy: usize) -> Option<u8> {
        None
    }
}
```
The `resolve_to_grid` function takes `glyph_selector: &mut dyn GlyphSelector` parameter. When `select_glyph` returns `Some(glyph)`, use that glyph instead of auto_mat's glyph (but keep auto_mat's fg/bg colors). When it returns `None`, use auto_mat glyph as before. This makes the resolve pipeline extensible without modifying Phase 4's resolve() function.
**P5-118 FIX (MEDIUM):** The `dyn GlyphSelector` signature adds dynamic dispatch overhead:
32,400 vtable calls per frame (240x135 cells). For better performance consistency with the
Phase 4 rasterizer (which uses `impl RasterShader`), prefer the generic form:
`pub fn resolve_to_grid<G: GlyphSelector>(... glyph_selector: &mut G ...)`.
With monomorphization, the default `AutoMatGlyphSelector` path compiles to a no-op (all calls
inlined/eliminated). Use `dyn GlyphSelector` ONLY if object safety or downstream binary size
is a concern. If you keep `dyn GlyphSelector`, add `#[inline(always)]` to
`AutoMatGlyphSelector::select_glyph` to help the optimizer eliminate the call.
**P5-306 FIX (HIGH):** CONTRADICTION — the CANONICAL SIGNATURE block above uses `&mut dyn GlyphSelector` but P5-118 recommends `<G: GlyphSelector>`. Choose ONE form and apply it consistently. Recommendation: use the GENERIC form `pub fn resolve_to_grid<G: GlyphSelector>(sample_buffer: &SampleBuffer, materials: &[Material], grid: &mut AsciiCellGrid, glyph_selector: &mut G, resolve_buf: &mut Vec<AnsiCell>)`. Remove the `dyn` from the canonical signature block and update all call sites in 05-05, 06-03, and 07-04 to pass `&mut AutoMatGlyphSelector` (or any concrete `G`) directly. If a caller holds a `Box<dyn GlyphSelector>`, it must coerce: `resolve_to_grid(..., &mut *boxed_sel, ...)`. The P5-061 FIX canonical block above is the stale `dyn` form; treat P5-118 + this P5-306 as the authoritative override — use the generic form.

Create `resolve_bridge.rs`:
- **CRITICAL GAP #3 RESOLUTION**: Bridges AnsiCell (xterm-256 palette u8) to AsciiCellGrid (RGBA [u8; 4]).

- `XTERM_256_PALETTE: [[u8; 3]; 256]` constant: Full xterm-256 palette RGB values.
  - Indices 0-15: standard terminal colors
  - Indices 16-231: 6x6x6 color cube: `r = (idx - 16) / 36 * 51`, `g = ((idx - 16) % 36) / 6 * 51`, `b = (idx - 16) % 6 * 51`
  - Indices 232-255: grayscale ramp: `v = 8 + (idx - 232) * 10`
  - NOTE (P5-020): The formula uses evenly-spaced levels [0,51,102,153,204,255] which differs from the real xterm-256 spec [0,95,135,175,215,255]. This is INTENTIONAL — the C++ engine's `rgb2pal()` uses `(c + 25) / 51` which assumes evenly-spaced levels. The palette table must be the inverse of `rgb2pal` for correct round-tripping.
  - **P5-065 FIX:** The GPU display layer receives these palette-approximate RGBA values. If perceptually correct xterm-256 colors are needed later, the real spec values [0,95,135,175,215,255] must be used and `rgb2pal` updated to match.
  - **P5-078 FIX:** Palette index 0 maps to RGBA [0,0,0,255] (terminal default black).

- `resolve_to_grid` function — **CANONICAL SIGNATURE** (all consumers must match this):
  **P5-061 FIX:** The `resolve_buf` parameter will be populated from `ResolveBuffer` resource (defined in 05-05 pipeline.rs). For unit tests, pass `&mut Vec::new()` directly.
  ~~SUPERSEDED by P5-306 FIX~~ The previous `dyn GlyphSelector` form (from earlier plan rounds) is stale. The CURRENT canonical form is the generic below:
  ```rust
  pub fn resolve_to_grid<G: GlyphSelector>(
      sample_buffer: &SampleBuffer,
      materials: &[Material],
      grid: &mut AsciiCellGrid,
      glyph_selector: &mut G,
      resolve_buf: &mut Vec<AnsiCell>,
  )
  ```
  NOTE (P5-001/P5-002/P5-017/XP-001 FIX): This is the single source-of-truth signature.
  Plans 05-05, 06-03, and 07-04 MUST call with all 5 parameters.
  Default callers pass `&mut AutoMatGlyphSelector` and `&mut Vec::new()` (or a pre-allocated buffer).
  The `resolve_buf` (R62 optimization) avoids re-allocating the `Vec<AnsiCell>` every frame.

  Implementation:
  **P5-002 FIX:** The canonical 4-parameter signature hides the underlying resolve() params.
  Internally, resolve_to_grid derives them from SampleBuffer fields:
  ```rust
  let dw = sample_buffer.width as i32;  // sample width (e.g. 484)
  let dh = sample_buffer.height as i32; // sample height (e.g. 274)
  let ascii_w = (sample_buffer.width as i32 - 4) / 2;  // e.g. 240
  let ascii_h = (sample_buffer.height as i32 - 4) / 2;  // e.g. 135
  ```
  **XP-107 FIX (HIGH):** `sample_buffer.height` must be a FIELD access (not a method call).
  Project MEMORY.md confirms `SampleBuffer` has `pub width: u32` as a field. Before writing
  any code that accesses `sample_buffer.height`, verify in sample_buffer.rs that BOTH
  `pub width: u32` AND `pub height: u32` are declared as struct fields. If `height` is derived
  (e.g., `samples.len() / width`), add the field or use the correct accessor. The 05-05
  verification checklist item "Verify `sample_buffer.height` compiles as a field access (not
  method call)" applies here too — resolve_bridge.rs uses `sample_buffer.height as i32` in
  the dh calculation above.
  **P5-001 FIX:** Validate grid dimensions match before indexing:
  ```rust
  assert_eq!(grid.width as i32, ascii_w, "AsciiCellGrid width mismatch");
  assert_eq!(grid.height as i32, ascii_h, "AsciiCellGrid height mismatch");
  ```
  1. **P5-032 FIX:** Resize `resolve_buf` using `Vec::resize()` (NOT `reserve()`):
     `resolve_buf.resize((ascii_w * ascii_h) as usize, AnsiCell::default());`
     This is required because `resolve()` checks `output.len()`, not `output.capacity()`.
     Grow only, never shrink.
     **P5-113 FIX (HIGH):** The "grow only, never shrink" strategy is INCORRECT. The Phase 4
     `resolve()` function has `debug_assert_eq!(output.len(), (ascii_w * ascii_h) as usize)`.
     This PANICS in debug builds if the slice is LARGER than `ascii_w * ascii_h`, which happens
     on a window resize DOWN (e.g., 240x135 shrinks to 160x90 — buffer stays 32,400 but only
     14,400 expected). Always resize to EXACTLY the target size — `Vec::resize` already handles
     both growth and shrinkage. Remove any comment saying "never shrink."
  2. Call Phase 4's `resolve(&sample_buffer.samples, dw, dh, ascii_w, ascii_h, materials, resolve_buf)`
     **P5-051 FIX:** Exact call: `resolve(&sample_buffer.samples, dw, dh, ascii_w, ascii_h, &materials.0, resolve_buf.as_mut_slice())`. The `materials.0` dereferences `RuntimeMaterials(pub Vec<Material>)`. The `resolve_buf` deref from `&mut Vec<AnsiCell>` to `&mut [AnsiCell]` is implicit via DerefMut.
     **R7-004 FIX:** Inside `resolve_to_grid`, the `materials` parameter is already `&[Material]` — do NOT write `materials.0` here. The `.0` unwrap of `RuntimeMaterials` happens at the CALL SITE in pipeline.rs (05-05), not inside this function. Correct internal call: `resolve(&sample_buffer.samples, dw, dh, ascii_w, ascii_h, materials, resolve_buf)`.
     **M-02 CLARIFICATION:** The `resolve_buf` parameter is `&mut Vec<AnsiCell>`. When passed to `resolve()` which takes `&mut [AnsiCell]`, Rust's `DerefMut` auto-coerces `&mut Vec<T>` to `&mut [T]`. However, for explicitness, callers MAY use `resolve_buf.as_mut_slice()`. The key requirement is that `resolve_buf.len()` EXACTLY matches `ascii_w * ascii_h` before the call (enforced by `Vec::resize()` in step 1), because `resolve()` has `debug_assert_eq!(output.len(), ...)` that panics on mismatch.
     **P5-103 FIX (CRITICAL):** The `materials.0` unwrap in the call above is the CORRECT pattern
     when called from pipeline.rs where `materials: Option<Res<RuntimeMaterials>>`. The
     `resolve_to_grid` function signature takes `materials: &[Material]` — the `.0` unwrap of
     `RuntimeMaterials(pub Vec<Material>)` must happen AT THE CALL SITE in pipeline.rs (05-05),
     NOT inside `resolve_to_grid` itself. The canonical call from pipeline.rs must be:
     `resolve_to_grid(&*sample_buffer, &materials.0, &mut cell_grid, &mut sel, &mut resolve_buf.0)`
     The explicit `&materials.0` (unwrap the newtype + borrow as slice) makes the type boundary
     unambiguous and prevents the "materials is &[Material] or &RuntimeMaterials?" confusion.
     **M-08 RESOLUTION — materials parameter FIX note consolidation:**
     Three overlapping FIX notes (P5-051, P5-103, R7-004) all address the same topic:
     how `materials` flows from `RuntimeMaterials` to `resolve()`. The AUTHORITATIVE resolution is:
     - **Inside `resolve_to_grid`**: `materials` parameter is `&[Material]`. Call `resolve(...)` with `materials` directly. Do NOT use `.0`.
     - **At the call site in pipeline.rs (05-05)**: Unwrap from `RuntimeMaterials`: `&mats.0` (where `mats: &RuntimeMaterials`).
     - P5-051's `&materials.0` example is from the CALL SITE perspective. R7-004 is from the FUNCTION INTERIOR perspective. Both are correct in their respective contexts. P5-103 clarifies the boundary.
  3. For each AnsiCell in the output:
     **P5-301 FIX (CRITICAL):** AnsiCell has exactly 4 fields: `fg: u8`, `bk: u8`, `gl: u8`, `spare: u8`. There is NO `flags` field on AnsiCell. Do NOT reference `cell.flags` — it does not exist and will produce a compile error ("no field `flags` on type `AnsiCell`"). The `spare` field is used internally by `resolve()` (0xFF = rendered cell, 0 = clear cell) but is an internal pipeline flag — it is NOT written to AsciiCellGrid. The XP-104 NOTE in 05-05 about Plan 06-03 referencing `AnsiCell.flags` is a cross-plan warning; the risk here is that an implementer mirrors that stale text into this plan's code.
     - First, call `glyph_selector.select_glyph(sample_buffer, cell_x, cell_y)`
     - If `Some(glyph)`, override `cell.gl = glyph` (keep auto_mat fg/bg colors)
     - If `None`, use auto_mat glyph as-is
     - Convert `cell.fg` (xterm-256 index) to RGBA via `XTERM_256_PALETTE[cell.fg as usize]` + alpha=255
     - Convert `cell.bk` (xterm-256 index) to RGBA via `XTERM_256_PALETTE[cell.bk as usize]` + alpha=255
     - Write to grid: `grid.char_indices[i] = cell.gl as u16`, `grid.fg_colors[i] = [r, g, b, 255]`, `grid.bg_colors[i] = [r, g, b, 255]`
  4. Handle clear cells: write glyph=32 (space, matching resolve()'s `gl: b' '`), fg=[0,0,0,255], bg=[0,0,0,255]
     (**P5-035 FIX:** renumbered from duplicate "3." to "4." for clarity)
  **P5-321 INFO:** AnsiCell.spare is an internal pipeline flag (0xFF = rendered cell, 0 = clear cell, set by resolve()). It is NOT written to AsciiCellGrid. To detect clear cells DOWNSTREAM of this bridge (in Phase 6 or 7), check AsciiCellGrid fields: a clear cell has `char_indices[i] == 32` (space glyph) AND `fg_colors[i] == [0,0,0,255]` AND `bg_colors[i] == [0,0,0,255]`. Do not try to propagate `cell.spare` through this bridge — there is no spare field in AsciiCellGrid.

Update `render/mod.rs`:
- Add `pub mod terrain_shader; pub mod mesh_shader; pub mod resolve_bridge;`

Unit tests for mesh_shader:
- `test_mesh_shader_writes_rgb555`: Verify blend writes RGB555 with MESH_FLAG set
- `test_mesh_shader_depth_test`: Closer mesh overwrites farther

Unit tests for resolve_bridge:
- `test_xterm_palette_black`: Index 16 -> [0, 0, 0]
- `test_xterm_palette_white`: Index 231 -> [255, 255, 255]
- `test_xterm_palette_red`: Index 196 -> [255, 0, 0]
- `test_resolve_to_grid_produces_rgba`: Create SampleBuffer with known content, resolve, verify AsciiCellGrid has RGBA colors (not palette indices).
  **C-01 PREREQUISITE:** This test constructs `AsciiCellGrid` outside Bevy ECS context. It requires `AsciiCellGrid::new(width, height)` standalone constructor (see 05-06 P5-308 FIX). If this constructor does not yet exist, add `pub fn new(width: u32, height: u32) -> Self` to `engine-port/src/output/ascii_cell_grid.rs` as part of this task. The constructor must initialize `char_indices` (Vec of u16, filled with 32/space), `fg_colors` and `bg_colors` (Vec of [u8;4], filled with [0,0,0,255]), all with length `(width * height) as usize`.
  **F031/F114 FIX:** Add `pub fn new(width: u32, height: u32) -> Self` standalone constructor to `AsciiCellGrid` in `engine-port/src/output/ascii_cell_grid.rs` for use in golden tests and resolve_bridge unit tests. This file is now listed in `files_modified` (see frontmatter). The constructor is required by both this plan's resolve_bridge tests AND Plan 05-06's golden pipeline tests.
- `test_resolve_to_grid_clear_cells`: Clear buffer produces zero/space cells
- `test_glyph_selector_none_uses_automat` (R47): AutoMatGlyphSelector always returns None, resolve uses auto_mat glyph
- `test_glyph_selector_override` (R47): Custom GlyphSelector returning Some('X') overrides glyph but keeps auto_mat fg/bg colors
  </action>
  <verify>
`cargo test --lib -- render::mesh_shader` passes. `cargo test --lib -- render::resolve_bridge` passes. `cargo clippy -- -D warnings` clean. `cargo build` succeeds.
  </verify>
  <done>
TerrainShader and MeshShader call Phase 4 rasterize() with real geometry. resolve_to_grid bridges AnsiCell xterm-256 indices to AsciiCellGrid RGBA via palette lookup table. Format mismatch resolved. No Phase 4 interfaces modified.
  </done>
</task>

</tasks>

<verification>
- `cargo test --lib -- render::terrain_shader` passes
- `cargo test --lib -- render::mesh_shader` passes
- `cargo test --lib -- render::resolve_bridge` passes
- `cargo clippy -- -D warnings` clean
- `cargo build` compiles
- Phase 4 files (rasterizer.rs, resolve.rs, sample_buffer.rs) NOT modified

**R14-PG05 FIX:** Run `cargo fmt -- --check` and `cargo clippy -- -D warnings`
on the entire project (not just this plan's files) to catch cross-file issues.

**R14-PG01 FIX:** After successful execution, update `.planning/ROADMAP.md`:
mark this plan `[x]`, increment the "Plans Complete" counter for this phase.
</verification>

## Contract Check

| Requirement | Plan Coverage | Verification |
|-------------|---------------|--------------|
| REND-08 (partial): Terrain rendering uses rasterizer | Task 1: TerrainShader calls rasterize() | test_render_patch_produces_samples |
| REND-08 (partial): Mesh rendering uses rasterizer | Task 2: MeshShader calls rasterize() | test_mesh_shader_writes_rgb555 |
| CRITICAL #1: No placeholder rendering | Task 1+2: Real rasterization via Phase 4 | Integration verified in Plan 05 |
| CRITICAL #3: AnsiCell->AsciiCellGrid bridge | Task 2: resolve_to_grid with palette lookup | test_resolve_to_grid_produces_rgba |
| HIGH #4: TerrainShader/MeshShader exist | Task 1+2: Both implement RasterShader | test_terrain_shader_writes_material_index + test_mesh_shader_writes_rgb555 |

## Consistency Diff

- RESEARCH.md Pitfall 2: Sample.visual Overloading (TRAP-R01) -- TerrainShader writes material index (spare=0), MeshShader writes RGB555 (spare=MESH_FLAG). Resolve branches correctly. (consistent)
- RESEARCH.md Pattern 4: Pipeline Orchestrator -- This plan provides the rendering implementations the orchestrator will call (consistent)
- RESEARCH.md Pitfall 4: HEIGHT_CELLS vs VISUAL_CELLS -- render_patch uses correct scale factor (consistent)
- KEY CONSTRAINT: Phase 4 interfaces NOT modified -- Plan builds on top (consistent)

## Blocking Checklist

- [x] TerrainShader implements RasterShader with material path (spare=0)
- [x] MeshShader implements RasterShader with mesh path (MESH_FLAG set)
- [x] resolve_to_grid bridges xterm-256 -> RGBA (CRITICAL #3 resolved)
- [x] No Phase 4 interfaces modified
- [x] File ownership: new files in render/ (no conflict)

<success_criteria>
- TerrainShader rasterizes terrain triangles with material indices
- MeshShader rasterizes mesh triangles with RGB555 colors
- resolve_to_grid converts AnsiCell palette indices to AsciiCellGrid RGBA
- Both shaders produce correct spare bits for resolve path selection
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-integration/05-04-SUMMARY.md`
</output>
