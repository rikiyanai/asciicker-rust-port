---
phase: 05-pipeline-integration
plan: 07
type: execute
wave: 1
depends_on: [05-05]
files_modified:
  - engine-port/assets/game_map_y8.a3d
  - engine-port/.gitignore
  - engine-port/src/render/pipeline.rs
  - engine-port/src/render/mod.rs
  - engine-port/tests/golden_pipeline.rs
autonomous: true
gap_closure: true
requirements: [REND-08]

must_haves:
  truths:
    - "game_map_y8.a3d exists in engine-port/assets/ and the assembly system can load it at runtime"
    - "Mesh instances in the world are rasterized via render_mesh() when MeshRegistry contains loaded AKM data"
    - ".a3d binary asset is .gitignored to prevent bloating the repository"
  artifacts:
    - path: "engine-port/assets/game_map_y8.a3d"
      provides: "Real Asciicker world file for runtime rendering"
    - path: "engine-port/.gitignore"
      provides: "Git exclusion for binary .a3d assets"
      contains: "*.a3d"
    - path: "engine-port/src/render/pipeline.rs"
      provides: "Stage 3 WORLD calls render_mesh() for mesh instances with loaded AKM data"
  key_links:
    - from: "engine-port/src/render/pipeline.rs"
      to: "engine-port/src/render/mesh_shader.rs"
      via: "render_mesh() call in Stage 3 WORLD Mesh branch"
      pattern: "render_mesh\\("
    - from: "engine-port/src/render/pipeline.rs"
      to: "engine-port/src/render/assembly.rs"
      via: "MeshRegistry Resource read in pipeline"
      pattern: "mesh_registry"
---

<objective>
Deploy game_map_y8.a3d asset and wire mesh rendering in the pipeline.

Purpose: Close GAP-1 (missing asset file blocks all runtime rendering) and GAP-2 (mesh instances are never rasterized despite MeshShader being fully implemented). These two gaps are the blockers preventing the Phase 5 goal of "rendering a real Asciicker .a3d world file in a window."

Output: game_map_y8.a3d in engine-port/assets/, .gitignore for binaries, render_mesh() wired in pipeline Stage 3.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pipeline-integration/05-05-SUMMARY.md
@engine-port/src/render/pipeline.rs
@engine-port/src/render/mesh_shader.rs
@engine-port/src/render/assembly.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy game_map_y8.a3d and configure .gitignore</name>
  <files>
    engine-port/assets/game_map_y8.a3d
    engine-port/.gitignore
    engine-port/tests/golden_pipeline.rs
  </files>
  <action>
    1. Copy `game_map_y8.a3d` from `/Users/r/Downloads/asciicker-Y9-2/a3d/game_map_y8.a3d` to `engine-port/assets/game_map_y8.a3d`.

    2. Add `*.a3d` to `engine-port/.gitignore` under a new section header `# Binary game assets (local only)`. This prevents the 1.8MB binary from bloating the git repo. The file must be manually deployed by each developer.

    3. In `engine-port/tests/golden_pipeline.rs`, update the `test_load_a3d_full_pipeline` test:
       - Remove the `panic!("Requires game_map_y8.a3d...")` line.
       - Keep the `#[ignore]` attribute (it still requires a full Bevy runtime which is not available in unit tests).
       - Replace the body with a comment documenting that this test validates with a full Bevy app and should be run manually: `cargo test -- --ignored test_load_a3d_full_pipeline`.

    NOTE: Do NOT commit the .a3d file itself (it's .gitignored). Only commit the .gitignore changes.
  </action>
  <verify>
    - `ls -la engine-port/assets/game_map_y8.a3d` shows the file exists (~1.8MB)
    - `grep '*.a3d' engine-port/.gitignore` shows the exclusion rule
    - `cargo test --lib` passes (no regressions)
  </verify>
  <done>game_map_y8.a3d is present in engine-port/assets/ for runtime loading; .gitignore prevents accidental commit of binary assets; test_load_a3d_full_pipeline no longer panics on entry</done>
</task>

<task type="auto">
  <name>Task 2: Wire render_mesh() in pipeline Stage 3 WORLD</name>
  <files>
    engine-port/src/render/pipeline.rs
    engine-port/src/render/mod.rs
  </files>
  <action>
    1. In `engine-port/src/render/pipeline.rs`, add `Res<MeshRegistry>` as a parameter to `render_pipeline_system`. Import `MeshRegistry` from `crate::render::assembly`.

    2. In the Stage 3 WORLD mesh branch (lines 192-202), replace the trace-log-only code with:
       - Look up the mesh_id in `mesh_registry.loaded` (HashMap<String, AkmMesh>).
       - If the mesh is found: call `render_mesh(buf, buf_w, buf_h, &mesh, tm, &camera.view_tm)`.
       - If the mesh is NOT found: keep the existing `trace!()` log at trace level (mesh AKM not yet loaded by AssetServer).
       - Remove the `let _ = (tm, buf_w, buf_h)` suppression line.

    3. Add the import for `render_mesh` from `crate::render::mesh_shader::render_mesh` at the top of pipeline.rs.

    4. In `engine-port/src/render/mod.rs`, verify `MeshRegistry` is already imported and registered as a resource in CpuRasterizerPlugin (it is — confirmed in 05-05-SUMMARY). No changes needed if already present.

    5. Add a unit test `test_pipeline_mesh_branch_calls_render_mesh` that:
       - Creates a small AkmMesh with 1 triangle
       - Creates a MeshRegistry with that mesh loaded under key "test_mesh"
       - Verifies that after calling render_mesh with identity transforms, at least one sample in the buffer has spare == MESH_FLAG

    IMPORTANT: The `render_pipeline_system` function signature change (adding `Res<MeshRegistry>`) is safe because Bevy automatically injects system parameters. No call-site changes needed.
  </action>
  <verify>
    - `cargo test --lib -- render::pipeline` passes (existing + new tests)
    - `cargo test --lib -- render::mesh_shader` passes
    - `cargo clippy -- -D warnings` clean
    - `grep -n "render_mesh" engine-port/src/render/pipeline.rs` shows the call in Stage 3
    - No `let _ = (tm, buf_w, buf_h)` remaining in pipeline.rs
  </verify>
  <done>render_mesh() is called in Stage 3 WORLD for mesh instances whose AKM data is loaded in MeshRegistry; mesh instances with unloaded AKM data are still logged at trace level; REND-08 is fully satisfied for mesh rendering</done>
</task>

</tasks>

<verification>
1. `ls -la engine-port/assets/game_map_y8.a3d` — file exists, ~1.8MB
2. `grep '*.a3d' engine-port/.gitignore` — binary assets excluded
3. `cargo test --lib` — all tests pass, no regressions
4. `cargo clippy -- -D warnings` — clean
5. `grep -c "render_mesh" engine-port/src/render/pipeline.rs` — at least 1 call (not just import)
6. `grep -c "let _ = (tm" engine-port/src/render/pipeline.rs` — zero matches (suppression removed)
</verification>

<success_criteria>
- game_map_y8.a3d is deployed to engine-port/assets/ and loadable by Bevy AssetServer
- Binary .a3d files are .gitignored
- Stage 3 WORLD calls render_mesh() for loaded mesh instances
- All existing tests pass with no regressions
- REND-08 gap (mesh rendering not wired) is closed
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-integration/05-07-SUMMARY.md`
</output>
