---
phase: 05-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine-port/src/terrain/mod.rs
  - engine-port/src/terrain/quadtree.rs
  - engine-port/src/terrain/patch_runtime.rs
autonomous: true
requirements:
  - TERR-01
  - TERR-02
  - TERR-03
  - TERR-04

must_haves:
  truths:
    - "Parsed A3dTerrain patches assemble into a quadtree with correct spatial layout"
    - "Quadtree leaf patches have 5x5 height vertices (HEIGHT_CELLS=4) and 8x8 visual cells (VISUAL_CELLS=8)"
    - "Height bounds propagate from leaves to interior nodes for frustum culling"
    - "Frustum query visits only patches whose AABB intersects the frustum planes"
    - "Known C++ bugs TERRAIN-001 through TERRAIN-004 are fixed and regression-tested"
  artifacts:
    - path: "engine-port/src/terrain/quadtree.rs"
      provides: "QuadNode enum, quadtree build, frustum query"
      min_lines: 150
    - path: "engine-port/src/terrain/patch_runtime.rs"
      provides: "RuntimePatch with height bounds, dark bitmask, helper methods"
      min_lines: 60
    - path: "engine-port/src/terrain/mod.rs"
      provides: "TerrainPlugin with RuntimeTerrain resource"
      min_lines: 50
  key_links:
    - from: "engine-port/src/terrain/quadtree.rs"
      to: "engine-port/src/terrain/patch_runtime.rs"
      via: "QuadNode::Leaf contains RuntimePatch"
      pattern: "QuadNode::Leaf\\(RuntimePatch"
    - from: "engine-port/src/terrain/patch_runtime.rs"
      to: "engine-port/src/asset_loader/a3d_terrain.rs"
      via: "RuntimePatch constructed from parsed TerrainPatch"
      pattern: "TerrainPatch"
---

<objective>
Build the terrain quadtree runtime system that converts parsed A3dTerrain patches into a spatial quadtree structure with frustum-culled traversal. Includes regression tests for all 4 known C++ terrain bugs.

Purpose: The terrain quadtree is the spatial data structure that enables efficient frustum culling during the TERRAIN stage of the 6-stage pipeline.

Output: `RuntimeTerrain` resource with quadtree built from parsed patches, frustum query with plane elimination, RuntimePatch with height bounds and shadow bitmask slots, and TERR-04 regression tests.
</objective>

<execution_context>
@/Users/r/.claude/get-shit-done/workflows/execute-plan.md
@/Users/r/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-integration/05-RESEARCH.md

@engine-port/src/asset_loader/a3d_terrain.rs
@engine-port/src/asset_loader/constants.rs
@engine-port/src/terrain/mod.rs

Reference C++ architecture:
@docs/skills/world-loading.md (Section 2: quadtree structure, Section 4: QueryTerrain callgraph)
@docs/arch/terrain_cpp_part1.md (quadtree CRUD, height bounds propagation)
@docs/arch/terrain_cpp_part2.md (QueryTerrain frustum culling)
</context>

<tasks>

<task type="auto">
  <name>Task 1: RuntimePatch, QuadNode, quadtree construction, and frustum query</name>
  <files>
    engine-port/src/terrain/patch_runtime.rs
    engine-port/src/terrain/quadtree.rs
  </files>
  <action>
Create `patch_runtime.rs` with:
- `RuntimePatch` struct containing:
  - `x: i32, y: i32` (world coordinates)
  - `height: [[u16; 5]; 5]` (HEIGHT_CELLS+1 = 5 vertices per edge)
  - `visual: [[u16; 8]; 8]` (VISUAL_CELLS = 8 material cells per edge)
  - `diag: u16` (triangle orientation bitfield)
  - `dark: u64` (shadow bitmask, 64 bits for 8x8 cells, initially 0)
  - `lo: u16, hi: u16` (min/max height bounds, computed from height array)
- `RuntimePatch::from_terrain_patch(patch: &TerrainPatch) -> Self` constructor that:
  - Copies all fields from parsed TerrainPatch
  - Computes `lo` and `hi` by scanning the 5x5 height array for min/max
  - Initializes `dark = 0`
- `RuntimePatch::sample_cell_center(&self, u: usize, v: usize, px: i32, py: i32) -> [f64; 3]`:
  - Computes world-space coordinates of center of visual cell (u,v)
  - FIX TERRAIN-002/003: boundary check uses correct variable (u < VISUAL_CELLS, v < VISUAL_CELLS). Add comment: `// FIX TERRAIN-002/003: C++ used wrong variable in boundary check`
  - FIX TERRAIN-004: height bounds comparison uses `>=` for inclusive boundary. Add comment: `// FIX TERRAIN-004: inclusive boundary (C++ had > instead of >=)`

Create `quadtree.rs` with:
- `QuadNode` enum: `Interior { children: [Option<Box<QuadNode>>; 4], lo: u16, hi: u16 }` and `Leaf(RuntimePatch)`
- `QuadNode::height_bounds(&self) -> (u16, u16)`
- `build_quadtree(patches: &[TerrainPatch]) -> (Option<QuadNode>, i32, i32, i32)`:
  - FIX TERRAIN-001: Y-axis check uses `y` variable not `x` in quadrant selection. Add comment: `// FIX TERRAIN-001: C++ terrain.cpp:613 used 'x' instead of 'y' for quadrant`
  - Returns (root, level, base_x, base_y)
- `FrustumResult` enum, `aabb_corners`, `dot_plane` helpers
- `query_terrain_frustum` with plane elimination (swap_remove) and per-child plane cloning (Pitfall 5)

**TERR-04 REGRESSION TESTS** (at least one test per known bug):
- `test_terrain_001_y_axis_check`: Patches at different Y but same X. Verify quadrant placement uses Y correctly.
- `test_terrain_002_003_boundary_check`: Call sample_cell_center at u=7, v=7 (max boundary). No panic, correct interpolation.
- `test_terrain_004_inclusive_boundary`: Adjacent patches with shared edge heights. Verify interpolation at exact boundary.

Additional unit tests:
- `test_single_patch_quadtree`, `test_multiple_patches_quadtree`, `test_height_bounds_propagation`
- `test_empty_patches`, `test_runtime_patch_from_terrain_patch`
- `test_frustum_contains_all`, `test_frustum_excludes_all`, `test_plane_elimination`, `test_frustum_partial_overlap`
  </action>
  <verify>
`cargo test --lib -- terrain` passes all new tests. `cargo clippy -- -D warnings` clean.
  </verify>
  <done>
QuadNode enum with Interior/Leaf variants. build_quadtree converts parsed patches to spatial quadtree. Height bounds propagate. Frustum query with plane elimination. RuntimePatch has all fields. C++ bugs fixed and regression-tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: RuntimeTerrain resource and TerrainPlugin integration</name>
  <files>
    engine-port/src/terrain/mod.rs
  </files>
  <action>
Update `mod.rs`:
- Add `pub mod quadtree; pub mod patch_runtime;`
- Define `RuntimeTerrain` resource (derive Resource, Default):
  - `root: Option<QuadNode>`, `level: i32`, `base_x: i32`, `base_y: i32`, `patch_count: usize`
- `build_from_parsed(terrain: &A3dTerrain)` -> builds quadtree, sets all fields
- `query_visible(&self, planes: &[[f64; 4]], callback)` -> clones planes into local Vec, calls query_terrain_frustum
- `for_each_patch(&self, callback)` -> recursive walk, no frustum
- `for_each_patch_mut(&mut self, callback)` -> mutable recursive walk
- `get_patch_at(&self, x, y) -> Option<&RuntimePatch>` -> coordinate lookup
- `interpolate_height(&self, world_x: f64, world_y: f64) -> Option<f64>` -> bilinear interpolation
- Register in `TerrainPlugin::build()`

Unit tests: `test_for_each_patch_visits_all`, `test_get_patch_at_existing`, `test_get_patch_at_missing`
  </action>
  <verify>
`cargo test --lib -- terrain` passes all tests. `cargo build` succeeds.
  </verify>
  <done>
RuntimeTerrain resource with quadtree build, frustum query, traversal, coordinate lookup, and height interpolation. TerrainPlugin registers resource.
  </done>
</task>

</tasks>

<verification>
- `cargo test --lib -- terrain` passes all quadtree, frustum, and bug-regression tests
- `cargo clippy -- -D warnings` produces no warnings
- `cargo build` compiles successfully
</verification>

## Contract Check

| Requirement | Plan Coverage | Verification |
|-------------|---------------|--------------|
| TERR-01: Quadtree heightmap with HEIGHT_CELLS=4 | Task 1: RuntimePatch `height: [[u16; 5]; 5]` | test_runtime_patch_from_terrain_patch |
| TERR-02: VISUAL_CELLS=8 material grid | Task 1: RuntimePatch `visual: [[u16; 8]; 8]` | test_runtime_patch_from_terrain_patch |
| TERR-03: Quadtree propagates height bounds | Task 1: build_quadtree + frustum query | test_height_bounds_propagation + frustum tests |
| TERR-04: Known C++ bugs fixed | Task 1: 3 dedicated regression tests | test_terrain_001/002_003/004 |

## Consistency Diff

- RESEARCH.md Pattern 1: Quadtree with Enum Nodes -- Plan uses QuadNode enum (consistent)
- RESEARCH.md Pattern 3: Frustum Culling with Plane Elimination -- Plan implements swap_remove (consistent)
- RESEARCH.md Pitfall 5: Plane clone per child -- Plan clones planes Vec (consistent)
- RESEARCH.md Pitfall 4: HEIGHT_CELLS vs VISUAL_CELLS -- Plan uses explicit constants (consistent)
- RESEARCH.md Pitfall 9: Known C++ Bugs -- Plan has regression tests, not just comments (improved over original)

## Blocking Checklist

- [x] TERR-01 mapped to Task 1 (RuntimePatch height array)
- [x] TERR-02 mapped to Task 1 (RuntimePatch visual array)
- [x] TERR-03 mapped to Task 1 (height propagation + frustum query)
- [x] TERR-04 mapped to Task 1 (3 regression tests, one per bug group)
- [x] No deferred ideas included
- [x] File ownership: terrain/* only in this plan

<success_criteria>
- QuadNode builds correct spatial quadtree from parsed patches
- Height bounds propagate from leaves to interior nodes
- Frustum query with plane elimination produces correct visible patch set
- TERR-04 regression tests would fail if any C++ bug were reintroduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-integration/05-01-SUMMARY.md`
</output>
