# /test-smoke

Run a fast browser smoke check with screenshot trail artifacts.

## Command

```bash
just test-smoke
```

Optional custom URL:

```bash
just test-smoke base_url=http://127.0.0.1:4173
```

Headed debug:

```bash
just test-smoke debug=1
```
